# Project Liberty Task Board

> Generated from `control/tasks.json`. Do not edit status here; use `npm run ai:*` commands.

Statuses: `BACKLOG`, `READY`, `CLAIMED`, `IN_PROGRESS`, `REVIEW`, `BLOCKED`, `DONE`, `CANCELED`, `SUPERSEDED`.

| ID | Priority | Lane | Status | Owner | Review | Task | Acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PL-0001 | P0 | Infra | DONE | - | APPROVED by gpt-architect | Generate and commit dependency lockfile | npm install and full npm run check are green; CI uses npm ci once lockfile exists |
| PL-0002 | P0 | Architecture | DONE | - | APPROVED by gpt-architect | Review scaffold contracts | No unresolved contradiction across product, architecture, API, security, and content-rights contracts |
| PL-0003 | P0 | Infra | DONE | - | APPROVED by gpt-architect | Environment validation | Missing required environment is reported clearly; local PostgreSQL and Redis health are documented |
| PL-0101 | P0 | Frontend | DONE | - | APPROVED by gpt-architect | Catalog home data contract | Home route consumes typed catalog response and handles loading, error, and empty states |
| PL-0102 | P0 | Frontend | DONE | - | APPROVED by claude-test | Search experience | Debounced accessible search with typed results and URL-addressable query |
| PL-0103 | P0 | Frontend | DONE | - | APPROVED by claude-test | Title details | Movie, series, and episode details with play CTA using normalized IDs |
| PL-0104 | P0 | Frontend | DONE | - | APPROVED by gpt-architect | Make catalog and search results navigable | Every catalog and search result opens its title details page, with an accessible link name |
| PL-0105 | P0 | Frontend | DONE | - | APPROVED by gpt-architect | Catalog metadata source port, and the surfaces still importing the fixture array | Discovery reads a metadata port instead of importing a fixture array. catalog-source.ts states the source contract in the published vocabulary - a CatalogItem plus a nullable rights basis - and refuses a record whose basis is undeclared or contradicts its item, by named reason, rather than defaulting either to something permissive; catalog-source-registry.ts is the only module that knows both the port and an implementation, and resolves to not-configured outside the NonDeploymentEnvironment allowlist so the fixtures are unconstructible in a deployment rather than withheld from one; and every remaining surface reads the registry rather than the demoCatalog array - app/search/search.ts, app/title/demo-title-details.ts, and app/api/v1/catalog/home/route.ts, which awaits loadHomeCatalog and maps catalog_source_not_configured onto its own status instead of serving empty rails at 200. When the last surface has moved, the raw demoCatalog export survives only for tests that must be able to name the fixture set, so search and the title page stop being fixture-backed on a build where the home rails are not. docs/CATALOG_SOURCE.md stays accurate about what the port still does not solve. |
| PL-0201 | P0 | Media | DONE | - | APPROVED by gpt-architect | Expand candidate score model | Score dimensions are documented, deterministic, explainable, and regression-tested |
| PL-0202 | P0 | Media | DONE | - | APPROVED by gpt-architect | Audio selection policy | Preferred language, codec, channels, and fallback reasons are deterministic and tested |
| PL-0203 | P0 | Media | DONE | - | APPROVED by gpt-architect | Subtitle selection policy | Preferred language, forced, default, and off policies are explicit and tested |
| PL-0204 | P0 | Media | DONE | - | APPROVED by gpt-architect | Candidate failover policy | Retry and fallback behavior is bounded, deterministic, observable, and testable |
| PL-0205 | P0 | Media | SUPERSEDED | - | - | Unknown media metadata semantics | StreamCandidate can represent genuinely unknown videoCodec, audioCodec, height and bitrateKbps, and the media engine treats unknown as unknown rather than as a value: eligibility must not pass on an unverified codec, and scoring must not award or penalise a fabricated number |
| PL-0206 | P0 | Media | DONE | - | APPROVED by gpt-architect | Macrolanguage and extlang equivalence in the shared language matcher | The shared languageMatch in packages/media-engine/src/audio.ts stops reducing a tag to its primary subtag where that reduction crosses a MACROLANGUAGE boundary. A spoken-language distinction encompassed by a macrolanguage is not reported as a preferred-language fallback: zh-yue against zh-cmn, and either against a bare zh preference, must not answer with a fallback strength, because a fallback there tells the caller it may play Cantonese for a Mandarin preference. Canonical primary and extlang spellings of the SAME language are treated as equivalent in both directions -- cmn and zh-cmn are one language, yue and zh-yue are one language -- and the equivalence survives a region or script suffix on either side. The rule is exercised through BOTH consumers of the shared matcher, the audio track selection in audio.ts and the subtitle selection policy in subtitles.ts, rather than through the helper alone, so neither consumer can be corrected while the other keeps the old behaviour. Any new outcome carries its own named reason: a distinction that is not regional must not be reported with a regional-fallback reason. |
| PL-0207 | P0 | Media | DONE | - | APPROVED by gpt-architect | Corrective, re-run: unknown media metadata semantics under an honest review range | StreamCandidate can represent genuinely unknown videoCodec, audioCodec, height and bitrateKbps, and unknown must be ASSERTED rather than reached by silence: the four facts are required-and-nullable, so an explicit null parses, an omitted or undefined field is refused, and no numeric sentinel stands in for a measurement. The media engine then treats unknown as unknown rather than as a value. Eligibility must never CERTIFY an unstated codec as supported; such a candidate remains attemptable and is labelled explicitly unverified, while a STATED codec the device cannot decode is still rejected and a STATED height above the ceiling is still refused. Rights are checked before anything unknown can excuse a candidate. Scoring must neither award nor penalise a fabricated number: every unknowable dimension scores zero on a positive weight, the attainable ceiling is reported rather than used as a divisor, and nothing renormalises for what could not be measured. |
| PL-0301 | P0 | Provider | DONE | - | APPROVED by gpt-architect | Authorized fixture provider | Server-side fixture adapter returns normalized authorized candidates only |
| PL-0302 | P0 | Provider | BLOCKED | - | - | First production provider | Licensed provider integrated through provider SDK with credentials isolated server-side |
| PL-0401 | P0 | Backend | SUPERSEDED | - | - | Auth integration decision | A recorded decision selects the auth approach and states the boundary it implies: a packages/auth seam that the rest of the application depends on instead of the vendor library, database-backed sessions, a deliberately small enabled surface with the advanced plugin stacks left off, and an exact-pinned dependency whose upgrades are treated as security-sensitive work rather than floated on a caret range |
| PL-0402 | P0 | Backend | DONE | - | APPROVED by gpt-architect | Profile model | Profiles are modelled above authentication rather than inside the auth library, with the active profile carried alongside the session rather than inside the identity record; profile scoping is present in the first migration rather than retrofitted, because that is the part that is expensive to reverse; profiles can be created and selected with authorization tests |
| PL-0403 | P0 | Backend | DONE | - | APPROVED by gpt-architect | Progress persistence | Progress rows are scoped to a profile id rather than a user id and keyed by (profileId, contentId) so a repeated write upserts idempotently, with read and write authorized against the active profile; two devices writing the same title are resolved by a server-issued writer epoch rather than by client timestamps or by a monotonically-increasing-position rule, because client clocks are unreliable and a monotonic rule wrongly refuses a legitimate rewind; and writes go directly to PostgreSQL, with no Redis write-behind introduced until a PostgreSQL problem has actually been measured |
| PL-0405 | P0 | Backend | DONE | - | APPROVED by gpt-architect | Corrective, re-run: auth integration decision, with a forgeable profile scope and an unsupported pin | A recorded decision selects the auth approach and states the boundary it implies: a packages/auth seam the rest of the application depends on instead of the vendor library, database-backed sessions, a deliberately small enabled surface with the advanced plugin stacks left off, and an exact-pinned dependency whose upgrades are treated as security-sensitive work rather than floated on a caret range. The pinned version must be one upstream currently supports, and the hand-written migration must agree with that version's account schema rather than an abandoned intermediate one. ProfileScope must be UNFORGEABLE by a holder of a genuine scope: a spread copy carrying a replaced profileId must not remain assignable, and any consumer that grants data access on the strength of a scope must establish that the scope was ISSUED rather than merely that it type-checks. The decision record must describe the routing that actually applies, or point at the task record as authoritative rather than restating it. |
| PL-0501 | P0 | Player | DONE | - | APPROVED by gpt-architect | Playback session API | Server resolves authorized provider candidates and clients cannot submit arbitrary media URLs; the session response is a discriminated union on outcome (granted, denied, unavailable) with reasons present on every branch rather than as an optional field, because a denial with no reason trail violates the reason-trail invariant exactly as much as a grant with none |
| PL-0502 | P0 | Player | DONE | - | APPROVED by gpt-architect | Player state machine | The machine models the session and candidate lifecycle (resolving, loading a candidate, playing, buffering, seeking, recovering, failing over, ended, fatal) rather than UI state, which the controls layer already owns; the video element and the playback engine stay authoritative and the machine is a validated projection of them, so every engine and media event has an inbound transition even in states where it supposedly cannot occur. MANDATORY CONSOLIDATION, added 2026-09-18 by gpt-architect's round-48 direction when it approved PL-0903 and PL-0904. The reviewer was explicit that these are NOT reasons to reopen either of those tasks: PL-0502 owns the whole player surface and finishes the consolidation while implementing the machine. (1) RENAME browser_unsupported to an engine-neutral lifecycle reason, host_unsupported, as a COMPLETE migration -- type member, controller, machine, tests, reason trail, docs and comments -- with NO alias kept. One lifecycle fact gets one name. Behaviour is unchanged: the engine loaded and ran far enough to determine that the current host cannot support it. (2) TIGHTEN EngineState's unavailable variant from `detail?: EngineUnavailableDetail` to `detail: EngineUnavailableDetail \| null`. Every producer must state whether detail was established; omission must not be the unknown representation. This is the same required-and-nullable discipline the contracts already apply to licenseUrl and to media facts. (3) UNIFY the duplicate engine-id vocabularies. PlaybackEngineId and PlaybackErrorEngine describe the same two values. Make one authoritative and derive or alias the other in the dependency-safe direction the code comments already identify. Two independently editable literal unions for one engine identity must not remain. (4) DO NOT WEAKEN PL-0904 while integrating: never make native code/category `number \| null`; do not flatten mpvError into Shaka-numbered fields; do not invent a native failure classification without evidence; keep aborted engine control flow out of candidate failure accounting. (5) PRESERVE the existing no-clock/deterministic state-machine discipline and the bounded failover policy already present in playback-machine.ts. |
| PL-0701 | P0 | Test | DONE | - | APPROVED by gpt-architect | Critical E2E harness | Catalog to title to player to progress fixture journey is reproducible in CI. MANDATORY ADDITION 2026-09-19, at gpt-architect's round-50 direction when it approved PL-0707, and placed here because THIS TASK OWNS e2e/**: expectedStatus in e2e/src/contract.ts must recognise request_body_too_large as HTTP 413; an E2E case must drive an oversized request and verify 413 PLUS the reason code; and THE E2E HELPER MUST REMAIN INDEPENDENTLY RESTATED rather than importing the server's status function. That last clause is the point of the whole item and must not be optimised away: the helper is a deliberately hand-written mirror, and importing playbackSessionHttpStatus would make the assertion a tautology that passes however wrong the server is. Today the helper derives request-level denials from only request_malformed and request_field_not_permitted, so it would infer 403 where the server correctly answers 413. Nothing fails yet, because no e2e spec sends an oversized body -- the mirror is simply out of date from f83bc65 onward, which is the failure mode a mirror has. |
| PL-0702 | P0 | Security | DONE | - | APPROVED by gpt-architect | Provider and URL security review | A findings register exists at docs/SECURITY_REVIEW_PROVIDER_URL.md enumerating every finding across the five named classes -- SSRF, secret exposure, redirect handling, allowlist enforcement, and rights bypass -- each with a disposition of RESOLVED (with the commit and the test that now fails without the fix) or ACCEPTED (with the reason, the residual risk, and who accepted it). AN EMPTY REGISTER IS NOT A PASS. The register must state, per class, what was examined and by what method, so that 'no finding' is distinguishable from 'not looked for'; a class examined by reading alone must say so rather than implying a test covered it. Every RESOLVED entry needs a regression that fails against the pre-fix tree, because a security fix without a test that would have caught the bug is indistinguishable from a refactor. Every ACCEPTED entry that touches credentials, licensing or an irreversible production change is an escalation under control/policies.json escalation.humanOnly and may NOT be accepted by an agent; it is recorded as OPEN-ESCALATED and named in the handoff. Out of scope and to be listed as such rather than silently skipped: anything requiring a real licensed provider or live credentials (PL-0302, PL-0602), and the desktop sidecar trust boundary already ruled on in PL-0901 and reviewed under PL-0501. |
| PL-0703 | P0 | Security | SUPERSEDED | - | - | Corrective: production rights-invariant breach in the watch route | The watch route serves no fabricated rights basis outside a non-deployment environment, and only one fixture provider exists; the environment classification is an allowlist expressed once and shared rather than restated; docs/E2E.md no longer blesses the divergence that produced the breach; the two affected Playwright specs assert the corrected behaviour and are EXECUTED in both production and development modes rather than only typechecked |
| PL-0704 | P0 | Frontend | DONE | - | APPROVED by gpt-architect | notFound() never reaches the wire: dead addresses answer 200 | An unknown title id and a malformed watch id each answer HTTP 404 rather than a skeleton served at 200, proven by an EXECUTED Playwright run ON THE PINNED BROWSER REVISION rather than by a route calling notFound(), and rather than by a run on a substituted revision. The cause is that a segment's loading file wraps that segment in Suspense, so React flushes the shell at 200 before the access-fallback error escapes the render and Next can set the status; the root loading file wraps every route in the application, so per-route fixes alone are insufficient. THE SKELETON CLAUSE, AMENDED. Any fix must RELOCATE the loading skeletons below the existence decision rather than deleting them to move a status code, ON EVERY ROUTE WHERE A SECTION'S DATA DOES NOT DEPEND ON THE ADDRESS EXISTING. The title route is exempt, and the exemption is reasoned rather than granted: a well-formed unknown title id is indistinguishable from a real one until the catalog answers, the status line precedes the first body byte, so no byte may be sent before that load resolves -- which is the definition of having nothing to stream. A full-page skeleton there IS the defect this task exists to remove, so the unamended clause required the defect on one of the three routes it governs. The exemption is EMPIRICAL AND REVERSIBLE, not permanent: the moment the title page grows a section whose data does not depend on the title existing -- recommendations, continue-watching, anything PL-0301 or PL-0501 fetches separately -- it gains an in-page Suspense below the decision exactly as the watch route has, and this clause binds again. Home and watch satisfy it today and a positive check proves each, because the original wording was satisfied by a repository with no skeletons in it at all: every assertion was a not.toContain on a string that exists nowhere. DEPENDS ON PL-0103 because the title route is that task's declared write surface; this is a lifecycle dependency, not a technical one. |
| PL-0705 | P0 | Frontend | DONE | - | APPROVED by gpt-architect | Search loses text typed before hydration | Text typed into the search field before the form hydrates is not lost: on mount the form adopts the DOM value if it differs from its own state, and a query typed at any point becomes an addressable URL. Proven by an EXECUTED Playwright run on WebKit, where the race reproduces, rather than only on Chromium where it does not. Separately the spec addresses the result heading exactly rather than by substring, because role-name matching is substring by default and a query equal to a title matches both the results heading and the card heading -- a spec defect that only appeared once the suite was first executed. |
| PL-0706 | P0 | Security | DONE | - | APPROVED by gpt-architect | Corrective, re-run: production rights-invariant breach in the watch route | Exactly ONE fixture provider exists in the repository, and the application CONSUMES the provider-sdk's rather than implementing its own: no route, no API handler and no watch-surface module declares a rights basis, composes a media URL, invents a candidate id or carries its own copy of the fixture variants. The runtime allowlist that decides which environments may construct fixtures is expressed exactly ONCE, at a shared lower boundary both the application and the SDK depend on rather than in either of them, and it is frozen at runtime so no consumer can widen what the classifier admits without editing that module. The capability is minted there by observing the running process, with no caller-supplied runtime name anywhere on the path, so a caller can neither state the classification nor alter the set of classifications that are admissible. The application OBTAINS that capability and passes it on; the SDK can neither classify for itself nor forge what it is handed, and therefore cannot admit itself into a deployment. The opaque rights-reference rule exists in exactly ONE place, with every other consumer importing it rather than restating it. A fabricated rights basis is structurally UNCONSTRUCTIBLE in a deployment environment -- unreachable because nothing that constructs one can be reached without a capability the shared boundary mints from the running process, and a deployment cannot obtain one -- rather than guarded by a condition that deleting one line would remove. Both Playwright modes, production and development, are EXECUTED rather than only typechecked, and the gate evidence names the run, the commit and the per-mode results. |
| PL-0710 | P0 | Security | DONE | - | APPROVED by gpt-architect | Provider outbound HTTP resolves, classifies and pins its destination | THE CANONICAL HOST AND NETWORK POLICY MOVES TO A DEPENDENCY-LEAF PACKAGE, @liberty/net-policy, and both existing classifiers consume it. This replaces the original design, which was 'provider-sdk adopts media-inspection's resolve-and-pin control'; that is preserved in acceptanceOriginal and is NOT to be implemented. WHY THE DESIGN CHANGED, because the reason is the whole point: PL-0709 had to import provider-sdk's classifyHost from media-inspection BY DEEP RELATIVE PATH to write its agreement test, since classifyHost is not exported from packages/provider-sdk/src/index.ts and that package's exports field is a bare ./src/index.ts with no subpaths. The reviewer accepted that import FOR THAT TEST ONLY AT THAT TREE and ruled it is not an acceptable permanent boundary. Making media-inspection depend on provider-sdk, or provider-sdk on media-inspection while media-inspection imports provider-sdk to compare, CREATES OR INVITES A WORKSPACE DEPENDENCY CYCLE -- PL-0710 has provider-sdk adopting media-inspection's authoriseFetchTarget, so the arrow already points the other way. A lower shared layer with NO dependency on provider-sdk or media-inspection is the shape that works. REQUIRED, extraction half: the canonical host and IP classification vocabulary and its normalisation move into @liberty/net-policy; provider-sdk consumes that shared classifier; media-inspection consumes that shared classifier; PL-0709's deep cross-package test import is REMOVED; and the agreement test becomes unnecessary as two implementations become one -- replace it with tests of the shared classifier PLUS package-boundary tests proving both consumers actually import it, because an extraction nothing imports is a third copy rather than a merge. The package name may follow repository convention, but the package must be a dependency leaf. REQUIRED, resolve-and-pin half, unchanged from the original and each clause a separate way host-literal checking fails: DNS resolution before the outbound connection; EVERY resolved address classified, not just the first; refusal if ANY resolved address is private, loopback, link-local or reserved, unless an explicitly permitted local-only path applies; the connection PINNED to the approved resolved address so DNS rebinding cannot change the destination between check and connect; resolution, classification and pinning repeated PER REDIRECT HOP; and TLS CERTIFICATE HOSTNAME VERIFICATION AND SNI REMAIN AGAINST THE ORIGINAL HOSTNAME, NOT THE PINNED IP -- pinning the transport must not turn into verifying the wrong name. Regressions required for public-name-to-private-IP, DNS rebinding, mixed public and private answer sets, and translated-address cases. ALSO IN SCOPE BECAUSE THE EXTRACTION PUTS IT IN PLAY: pin.ts's normaliseHost does not fold the DNS root label. It is safe today because both sides of that comparison come from the same URL object so they carry or omit the dot together, and a mismatch refuses the connection -- but it is a third canonicaliser, and this is the task that removes the others. WHY THIS IS P0: F1, F7 and F8 were three host spellings in one function, and F12 was a fourth spelling in a SECOND function written specifically to avoid duplicating the first. The duplication got in anyway -- not as a copied classifier but as a copied assumption about what a hostname string is. A check comparing a string against literals is only ever as complete as the list of spellings whoever wrote it thought of; resolve-and-pin classifies the address a resolver returned, which has one spelling. FOLDED IN 2026-09-20 from the PL-0305 handoff's remaining off-surface item, because this task already owns both packages and the packaging metadata: @liberty/media-inspection's exports field has no ./http subpath, so importing @liberty/catalog-ingestion from apps/web pulls hls.ts into the program and needs an ambient m3u8-parser shim reached by a triple-slash reference in the ingestion package's index. That reference is the smallest honest fix from inside that package and is not the fix. THE SAME DEFECT SHAPE AS THE ONE THIS TASK EXISTS FOR: provider-sdk's classifyHost is unreachable from outside its package for exactly the same reason, a bare ./src/index.ts exports field with no subpaths, which is why PL-0709 had to import it by deep relative path. Give @liberty/media-inspection the subpath exports its consumers actually need and remove the triple-slash workaround, and make sure @liberty/net-policy is published with the subpaths ITS two consumers need rather than repeating the omission in the new package. |
| PL-0901 | P0 | Architecture | DONE | - | APPROVED by gpt-architect | Windows desktop shell and the PlayerAdapter boundary | A recorded architecture decision states the Windows desktop target, the Tauri shell, the preserved Next.js application served by a standalone sidecar, and the LGPL-compatible libmpv/FFmpeg build pipeline, each with the evidence it rests on and the strongest argument against it. It specifies PlayerAdapter as an interface the application and domain layers depend on -- load an authorized playback session, play, pause, seek, stop, select audio track, select subtitle track, set volume, expose position and duration, expose buffering and stall events, expose recoverable and fatal errors, expose track metadata, expose A/V sync telemetry where available, dispose -- with no mpv, Electron, Tauri or Shaka type reachable from it, and names the two implementations behind it and the explicit capability routing that chooses between them, including the requirement that the mpv adapter refuse DRM-protected candidates with a named reason rather than attempt them. It states that media addresses continue to come only from authorized provider resolution and the playback-session boundary and that the client can never submit one, that stream candidate ranking stays in @liberty/media-engine, and that provider-specific logic stays in @liberty/provider-sdk. It states where provider resolution runs in the desktop build as an open question with a recommendation rather than deciding it silently, because that answer is a design input to PL-0501. It records what was verified, what was not, and which specific facts would reverse each decision. No shell, adapter or dependency is implemented here. |
| PL-0902 | P0 | Architecture | DONE | - | APPROVED by gpt-architect | DRM capability on the candidate and playback-session contract | A resolved stream candidate, or the playback session that carries it, states its content-protection requirement explicitly: the protection system where one applies, an unencrypted state that is asserted rather than inferred from an absent field, and an UNKNOWN state that is representable and does not read as unencrypted. PlayerAdapter.canPlay can decide routing from the contract alone, without inspecting a URL, a manifest or a provider-specific field, and can name the protection system in its refusal reason. Adding the field changes no existing candidate's meaning by accident: a candidate that omits it is refused by the schema rather than defaulted to clear. Provider-specific spellings stay inside @liberty/provider-sdk and are normalized before they reach the contract. No decryption, key handling or protection bypass is added. |
| PL-AI-0001 | P0 | Coordination | DONE | - | APPROVED by gpt-architect | Operationalize AI control plane in Claude Code Desktop | Claude Code uses machine-readable queues, claims tasks through the CLI, syncs status, and records handoffs rather than manually drifting task state |
| PL-AI-0004 | P0 | Coordination | DONE | - | APPROVED by gpt-architect | Review dependency scope and provenance reconciliation | SCOPE EXPANDED, deliberately and on the reviewer's ruling: this task now covers two trust-boundary features in the same two files, because the review system binds an approval to a path range rather than to a semantic hunk, so a current-head review of these files cannot truthfully exclude the second. A nominal second task over the identical write surface was rejected as a fake logical split. (1) allowedPaths remains the sole write, collision and staging surface while review fingerprints, stale-review checks, approvals and the DONE gate bind to allowedPaths union reviewDependencies; two tasks with disjoint allowedPaths and a shared reviewDependency can be active simultaneously, and editing that shared dependency invalidates both reviews; tasks without reviewDependencies behave identically to before; a declaration that reduces to the repository root is refused rather than dropped, in validation and at point of use, and the autonomy guard treats root ownership as orchestration. (2) Provenance reconciliation: an implementation already present in COMMITTED GIT HISTORY can be started with a true implementationBaseSha via explicit flags that are refused on an ordinary start, the base is validated against mechanical facts only, the semantic question of whether it is really this implementation's predecessor is published as evidence for the reviewer rather than guessed at by a heuristic, and the audit event is distinguishable from an ordinary start. The contract says committed rather than pushed deliberately: the mechanism inspects the local worktree and the local graph and consults no remote, so a clean branch of never-pushed commits satisfies it, and claiming otherwise was the same class of provenance overclaim this task exists to remove. Remote availability is enforced where it actually matters, at review and handoff. |
| PL-AI-0005 | P0 | Coordination | DONE | - | APPROVED by gpt-architect | Show reviewers the full review surface | The diff and briefing presented to a reviewer cover the same surface the approval will bind to; a reviewer is never asked to approve a fingerprint over files it was not shown; one canonical reviewSurface helper is consumed by fingerprinting, stale-review checking, the review diff builder and the briefing, and a regression asserts the file set shown equals the file set fingerprinted |
| PL-AI-0006 | P0 | Architecture | DONE | - | APPROVED by gpt-architect | Contract module boundaries | Shared vocabularies live in their own leaf modules, domain contracts live in one module each, index.ts is a re-export-only compatibility barrel rather than the authoritative public surface, and a wildcard exports map lets a new domain contract be added without touching package metadata or the barrel; no cycle-driven z.lazy remains and structural regressions prove the boundary rather than merely reflecting it; no schema behaviour changes |
| PL-AI-0007 | P0 | Coordination | DONE | - | APPROVED by gpt-architect | Supersession is a field, and a task cannot stay gated behind one | ai:validate refuses a task graph in which a task depends on a task that declares supersededBy and whose named successor is DONE, and warns when the successor is not yet DONE. It also refuses a supersededBy naming a missing task, naming itself, declared on a DONE task, or contradicted by the successor's supersedes field. The rule reports and never edits a dependency. The four existing supersession pairs validate clean, and a regression in scripts/test-ai-control-plane.mjs proves the rule fires -- both that it errors on a reconstructed PL-0301-behind-PL-0205 graph and that it stays silent on the repaired one, so it cannot pass by being inert. |
| PL-AI-0012 | P0 | Coordination | IN_PROGRESS | claude-lead | - | Enforce reviewer ownership of judgement gates | Recording a judgement gate is mechanically restricted to the reviewer who is entitled to make it, so that the round-83 PW-0203 incident is REFUSED rather than merely retracted. THE DEFECT, MEASURED. ai:gate today checks three things: that the task is IN_PROGRESS or REVIEW, that it has an owner, and that any --agent assertion matches who the control plane believes is recording. It does not ask whether the gate being recorded is one the recorder is entitled to conclude. So on 2026-09-23 the implementation owner of PW-0203 ran `gate PW-0203 architecture-review pass --agent claude-media "PLACEHOLDER-NOT-RECORDED"` expecting a refusal, and the control plane wrote a passing judgement gate whose entire evidence was that placeholder string. Every judgement gate in this repository's history was in fact transcribed from a gpt-architect verdict, but that is a convention the machine does not enforce, and product invariant 8 is currently defended by discipline alone. REQUIRED: judgement gates are named as a class in configuration rather than hard-coded at the call site -- at minimum architecture-review, security-review and rights-review -- so a gate added later is classified deliberately rather than defaulting to self-recordable. REQUIRED: a judgement gate may be recorded only by the task's configured reviewAgent, or by another reviewer made explicitly authorized under an existing fallback policy; the implementation owner cannot self-record one, and the check must consider BOTH the owner and any asserted implementationAgent, on the same rule the self-approval check already applies. REQUIRED: executable implementation gates -- typecheck, unit, e2e, build, performance and their kin -- remain recordable by the implementation owner exactly as they are today, where policy permits. This task must not make the ordinary loop harder. REQUIRED: a placeholder evidence string does not become a valid judgement merely because the CLI syntax was accepted. State the rule the implementation adopts and defend it; a length floor alone is not it, since a long placeholder passes one. REQUIRED: an unauthorized attempt is refused BEFORE anything is written -- no task.gate entry, no gateResults mutation, no audit line that could later read as a recorded result. A refusal that writes first and retracts after is the mechanism this task exists to replace. REQUIRED: a regression in scripts/test-ai-control-plane.mjs reproduces the exact round-83 attempt -- the task's own owner recording architecture-review with a placeholder -- and asserts refusal plus an unchanged gateResults. REQUIRED: legitimate transcribed gpt-architect verdict recording REMAINS SUPPORTED. This is the load-bearing constraint, not an afterthought: the GitHub write integration returns 403, so every judgement gate in this project is presently recorded by Claude on the reviewer's behalf. An enforcement that makes transcription impossible stops the project. Make transcription explicit rather than implicit -- the implementer chooses the spelling; a flag that records the verdict's provenance is the obvious shape -- and say in the record that it was transcribed. REQUIRED: release and retraction remain available as audit-recovery mechanisms and are not the primary enforcement. REQUIRED: the round-83 incident history is NOT erased, rewritten or compacted. The gate.fabricated_result_retracted event, the release, the re-claim and the re-records stay exactly as they are; this task adds a refusal, it does not tidy a record. |
| PW-0101 | P0 | Infra | DONE | - | APPROVED by gpt-architect | The application serves itself on loopback, hardened, as a standalone sidecar | The desktop target builds a Next standalone server that a shell can spawn, and every control docs/DESKTOP_PLAYBACK.md section 7 names is implemented and tested. REQUIRED: output 'standalone' is set for the desktop target and only for it -- the document specifies it in two places and next.config.ts sets it in none, which is the single largest gap between the recorded design and the code. REQUIRED: the server binds 127.0.0.1 explicitly and never 0.0.0.0, asserted by a test rather than by configuration alone, because Next reads HOSTNAME from the environment and an inherited value would silently expose the listener to the LAN. REQUIRED: a per-launch 256-bit CSPRNG bearer token passed through the environment, required on every request, with a constant-time comparison and a refusal that does not state whether the token was absent or wrong. REQUIRED: Host header validation against the exact expected literal, because a bearer token does not stop DNS rebinding. REQUIRED: the application emits its own Content-Security-Policy, because Tauri's CSP injection stops applying the moment the frontend is a URL rather than bundled assets -- this is a real loss the document names and nothing currently replaces it. REQUIRED: the sidecar reports its actually-bound port on stdout rather than being told which port to use; a pick-then-bind handshake is a TOCTOU race and is refused. NOT IN SCOPE: the Rust shell, which is PW-0102. |
| PW-0102 | P0 | Infra | READY | - | - | A Tauri v2 shell that owns the sidecar's lifetime | A Tauri v2 application starts, spawns the PW-0101 sidecar, reads its bound port from stdout, points the webview at it, and cannot leave the sidecar running. REQUIRED: the Tauri version is pinned exactly, on the rule this repository already applies to Shaka and Playwright -- a caret range means the shell a gate was recorded against can change under a lockfile refresh. REQUIRED: the sidecar is packaged as node.exe plus the standalone tree under bundle.resources and spawned as a child process; pkg, Node SEA and Bun --compile are documented as unable to swallow a Next standalone server and must not be attempted as a shortcut. REQUIRED: a Win32 Job Object with JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE, because Tauri provides no supervision and an orphaned node.exe holding a loopback port is the failure a user sees as 'it will not start the second time'. REQUIRED: supervision -- health check, restart with a bounded attempt budget, and an honest surfaced failure when the budget is spent, never a blank window. REQUIRED: the shell passes the bearer token to the sidecar through the environment and never on a command line, where it would be readable from the process list. STATED LIMITATION TO CARRY IN THE GATE EVIDENCE: no Windows binary can be produced or run from the cloud session, so this task's build gate is cargo check plus a windows-latest CI job, and 'it launches' is real-device evidence owed to PW-0601. |
| PW-0103 | P0 | Player | BACKLOG | - | - | Experiment 1a: can a child HWND composite beneath the webview | The pass/fail experiment docs/DESKTOP_PLAYBACK.md section 10 specifies is executed on real Windows hardware and its result recorded, because D1 -- the entire choice of Tauri -- rests on it and it has never been run. A sibling child HWND must render beneath a WebView2 whose DefaultBackgroundColor has alpha 0, with the web UI visible above it. REQUIRED: the result is recorded as a fact with the Windows build, GPU and WebView2 runtime version it was observed on, not as an expectation. REQUIRED: if it FAILS, stop and report rather than working around it -- section 11 names this as one of the specific facts that would reverse D1, and the honest outcome of a failed experiment is a reversed decision, not a hack that makes the demo look right. THIS TASK CANNOT BE COMPLETED WITHOUT THE COMMANDER'S WINDOWS MACHINE and its real-device half belongs in LAST_MILE the moment the code is ready. |
| PW-0201 | P0 | Player | DONE | - | APPROVED by gpt-architect | The PlayerAdapter boundary becomes a module instead of a specification | The ~260 lines of TypeScript in docs/DESKTOP_PLAYBACK.md section 3 exist as a module, and the four rules that make it a boundary are enforced mechanically rather than stated. REQUIRED: PlayerAdapterId, the closed PlayerRefusalCode union, canPlay returning a reason on BOTH branches, PlayerLoadRequest taking a PlaybackSession plus a candidateId and never a URL, getTracks, readAvSyncTelemetry, subscribe, and an idempotent dispose. REQUIRED: no shaka-player, mpv/libmpv, @tauri-apps/* or electron type is reachable from this module or anything it imports, enforced by an import-graph test on the model of the existing PROHIBITED_AV_INSTRUMENTATION scan and of build-target.test.ts -- and that test must be proven non-vacuous by planting an offender, the way build-target.test.ts already does. REQUIRED: no `any` anywhere in the module; no engine-handle passthrough of any name; no opaque config bag -- EngineConfig from engine.ts must not be promoted onto the boundary. THE EXISTING ShakaPlayerHandle IN engine.ts IS NOT THIS and is not replaced: it is the seam INSIDE the web implementation, one layer below, and section 3 says so explicitly. |
| PW-0202 | P0 | Player | DONE | - | APPROVED by gpt-architect | WebPlayerAdapter: the existing Shaka path, behind the boundary | The Shaka path that exists today satisfies PlayerAdapter without being rewritten. ADOPT, WRAP, ADAPT: PlaybackController, <liberty-video>, the XState machine and the failure classifier are working, reviewed code and are wrapped, not replaced. REQUIRED: canPlay answers for a DRM candidate with a reason on the accepting branch too, because this adapter is the DRM path on BOTH targets and 'it can play it' must be as legible as a refusal. REQUIRED: getTracks reports text and audio tracks from the Shaka player, which is the first time this repository reads them at all. REQUIRED: dispose is idempotent and releases the CDM session, a hazard three separate comments in the existing player already name. REQUIRED: no behaviour change observable at the existing e2e boundary -- the critical-journey and playback-session suites must pass unchanged, and that is the evidence that this was a wrap rather than a rewrite. |
| PW-0203 | P0 | Player | DONE | - | APPROVED by gpt-architect | Capability routing decides the engine before playback, with a reason on both branches | Which engine plays a candidate is decided before playback and recorded with a reason, satisfying the architecture invariant that already states this. REQUIRED: the routing function is PURE and deterministic -- no clocks, no I/O, no ambient state -- on the same rule ADR-003 and ADR-005 apply to ranking, so a routing decision can be reconstructed from a bug report. REQUIRED: a candidate whose contentProtection requires a CDM routes to the web adapter, always, and the native adapter REFUSES it with drm_required_no_cdm rather than falling back or degrading. mpv has no CDM; a native path that tried would be the DRM circumvention CONTENT_RIGHTS.md forbids, so this refusal is a rights property and not a capability detail -- which is why this task carries a rights-review gate. REQUIRED: the reason is recorded on the accepting branch as well, so 'why is this playing natively' is answerable. REQUIRED: routing consumes the protection state already on the session contract (PL-0902) rather than re-deriving it, because two opinions about a candidate's protection would eventually disagree. THE CANDIDATE-PROJECTION FINDING FROM PW-0201 IS THIS TASK'S, per gpt-architect's round-81 ruling, and no separate follow-up is filed unless this task proves one is needed. PW-0201 could not implement `canPlay(candidate: PlaybackCandidate)` as docs/DESKTOP_PLAYBACK.md §3 writes it: that client type is `{ id, providerId, source }`, it predates PL-0902, and it carries neither `protection` nor `compatibility` -- so an adapter handed one is STRUCTURALLY INCAPABLE of returning `drm_required_no_cdm`, which is the refusal this task exists to make. The boundary therefore declares a narrow adapter-facing projection, and that projection is ACCEPTED. REQUIRED HERE: routing consumes the protection state from the playback-session CONTRACT -- the `protection` field `playbackSessionCandidateSchema` already publishes, which PL-0902 put there -- and never re-derives it; the adapter capability input is that narrow projection and nothing wider; NO SECOND DRM OR PROTECTION DECISION IS INVENTED anywhere in the routing path, because two opinions about a candidate's protection would eventually disagree and the reason trail would then explain a decision nobody made; and the native refusal `drm_required_no_cdm` remains STRUCTURAL rather than conditional. DO NOT change the adapter back to the older type to match the §3 prose: the prose was written on 2026-09-15 and PL-0902 landed after it, so the listing is stale and the code is right. A cross-package contract change is only to be proposed if this task PROVES one is required, with the specific field and the specific decision that cannot be made without it. |
| PW-0204 | P0 | Player | BACKLOG | - | - | libmpv in the shell: a child HWND, gpu-next, and an event pump | The Rust side hosts libmpv in a child window and exposes exactly the operations PlayerAdapter needs, and nothing wider. REQUIRED: vo=gpu-next with gpu-context=d3d11, embedded by --wid into the child HWND proven by PW-0103. REQUIRED: the command surface is the adapter's, not mpv's -- load, stop, seek, pause, resume, select track, dispose -- and mpv property names never cross into TypeScript, which is the no-passthrough rule from section 3 applied at the process boundary. REQUIRED: the event pump handles MPV_EVENT_QUEUE_OVERFLOW by resynchronising from properties rather than by assuming the queue is authoritative, which section 5 names specifically. REQUIRED: the shell accepts a candidateId and resolves the URI from the session it was given; it must never accept an arbitrary URL from the webview, because that would make the shell an open proxy and would contradict the transport controls the web path already enforces. STATED LIMITATION: this cannot be compiled for Windows from the cloud session; the build gate is cargo check on the Linux host plus a windows-latest CI job, and playback evidence is owed to PW-0601. |
| PW-0205 | P0 | Player | BACKLOG | - | - | NativePlayerAdapter translates mpv into the existing state machine | mpv's events become the existing machine's events, and the machine stays the single source of playback truth. REQUIRED: the mapping in docs/DESKTOP_PLAYBACK.md section 6 is implemented against the ACTUAL machine -- 11 phases and the 21 PlaybackEvent members in playback-machine.ts -- and any event mpv produces that the machine has no member for is either mapped to an existing one with a stated reason or reported as an explicit gap, never silently dropped. REQUIRED: 'ready' is a CORRELATION and not a single mpv event, as section 5 states; the adapter must not claim readiness from one property. REQUIRED: the adapter imports no @tauri-apps type directly -- the invoke boundary is native-bridge.ts, which is the one module the import-graph guard permits to know the shell exists, and the guard is extended to say so. REQUIRED: no new machine state and no new PlaybackEvent member unless a concrete mpv behaviour cannot be expressed without one, in which case the addition is justified in the gate evidence; the machine is deliberately narrow and widening it to fit a second engine is how it stops being a mirror. |
| PW-0301 | P0 | Frontend | DONE | - | APPROVED by gpt-architect | An application shell: one layout, one navigation model, one token layer | The application has a real shell, and the eight hand-copied topbars become one. MEASURED STARTING POINT: layout.tsx is sixteen lines -- html, body, children -- and the topbar is duplicated by hand in eight files, each with a slightly different status badge and a different or absent nav; the nav itself is four fragment anchors, and /search is reachable from no link anywhere in the application. REQUIRED: one persistent chrome in the root layout, with every route's copy deleted rather than left beside it. REQUIRED: a navigation model that reaches every screen that exists, including search; a dead link is worse than an absent one because it teaches a viewer the feature is broken. REQUIRED: a token layer -- spacing, type, radius, elevation, colour -- replacing eight dark-only variables and the px literals around them, and a light theme or an explicit recorded decision that this product is dark-only. REQUIRED: a skip link, a focus indicator defined globally rather than only inside two CSS modules, and Windows display scaling at 100/125/150/200 percent without clipping. NOT IN SCOPE: replacing any working server-side logic; this task is the shell the existing screens hang in. |
| PW-0302 | P0 | Frontend | READY | - | - | Artwork, end to end, because a media application without images is a list | Titles have artwork, and getting it does not become a new way to fetch arbitrary URLs. ACCEPTANCE RECONCILED IN ROUND 83 ON gpt-architect's RULING. The previous wording asked for a rights basis and dimensions 'beside the URL'. The repository already has a reviewed artwork vocabulary that deliberately carries NO URL: artworkRefSchema in packages/catalog-ingestion/src/record.ts holds a role, an assetRef constrained to an opaque lower-case token, and a REQUIRED rights basis -- required there and nowhere else in that package, on the stated argument that a work with an undeclared basis is merely refused from browse while an IMAGE with an undeclared basis that reached a page would be somebody else's file served from our origin. project.ts then drops artwork on the way to CatalogItem, and domains/catalog.ts opens by promising the browse shape carries no stream, URL or provider field. The ruling is explicit: do not weaken a previously rights- and security-reviewed opaque-reference boundary to satisfy stale task prose. REQUIRED: the catalog and title contracts carry an OPAQUE ARTWORK REFERENCE plus required display metadata and a rights basis. Not a URL. The vocabulary ADOPTS artworkRefSchema's shape rather than inventing a second one; where the two must differ, say why in the module. REQUIRED: the client never receives an arbitrary upstream image URL merely to render artwork. REQUIRED: an authorized artwork-resolution boundary turns the opaque reference into a fetchable or served asset under allowlisted transport policy. That boundary is where an origin is decided, and it is the only place one is. REQUIRED: no arbitrary URL proxy. SECURITY.md's 'never proxy arbitrary client-provided URLs' is not weakened for pictures, which is why this task carries a security review as well as a rights review. REQUIRED: absent artwork keeps the DESIGNED gradient fallback. An absent image is a designed state, not a broken one, and a broken-image icon is not the answer. REQUIRED: next/image -- or whatever equivalent configuration the implementation uses -- permits only the controlled Liberty artwork origin or boundary, never arbitrary remote hosts and never a wildcard. Because no other origin is expressible through the resolution boundary, this should be a structural fact rather than a check somebody has to remember to maintain; state which of the two the implementation achieves. MEASURED STARTING POINT, unchanged and still true: there are zero images in this product at any layer -- no next/image, no img tag, and no poster, artwork, backdrop or thumbnail key anywhere in packages/contracts/src. Posters are CSS gradients. |
| PW-0303 | P0 | Frontend | DONE | - | APPROVED by gpt-architect | Profiles become usable: a picker, a switch, and a create | A person can see who they are watching as, switch, and create a profile. THE BACKEND IS COMPLETE AND REVIEWED AND NOTHING RENDERS IT: /api/v1/profiles and /api/v1/profiles/selection exist with handlers, contracts and tests, and there is no picker, no switch, no create and no avatar anywhere in the application. REQUIRED: the active profile is visible in the shell at all times, because every progress and watchlist row in this product is profile-scoped and a viewer who cannot see which profile is active cannot understand their own continue-watching list. REQUIRED: profile selection goes through the existing selection route and the existing ProfileScope, never through a client-held id -- PL-0405 recorded a forgeable-scope defect and this UI must not reintroduce it, which is why a security review is required. REQUIRED: switching profiles invalidates any cached profile-scoped view rather than leaving another profile's rows on screen. |
| PW-0305 | P0 | Frontend | READY | - | - | Continue watching: resume where the viewer left off | A viewer sees what they were watching and resumes it at the right position. The progress API, its lease route and its writer-epoch conflict rules are complete, tested and reviewed, and no rail, no card progress bar and no resume prompt exists. REQUIRED: a continue-watching rail on home, profile-scoped, ordered by recency, excluding finished items by a stated rule rather than a magic threshold buried in a component. REQUIRED: a progress indicator on cards for partially watched titles. REQUIRED: resume uses the stored position through the session's startAtSeconds, which the playback path already carries, rather than seeking after load -- the test matrix already lists 'resumed playback uses stored progress safely' as a regression scenario. REQUIRED: a viewer can start over; a resume that cannot be declined is a trap. |
| PW-0306 | P0 | Player | BACKLOG | - | - | A player that can actually be operated | The player has controls. TODAY IT HAS NONE: player-surface.tsx sets the native controls attribute on a video element and renders a debug panel, and its own header says 'NO CONTROLS ARE BUILT HERE'. There is no play, pause, seek, volume, fullscreen, subtitle, audio, quality or source control anywhere in this product. REQUIRED: play/pause, a seek bar with buffered ranges, volume and mute, fullscreen, a subtitle menu and an audio-track menu driven by PW-0206, and a source/quality state the viewer can read. REQUIRED: keyboard operation -- space, arrows, F, M, and Escape -- because there is not one keyboard handler in the entire application today and a Windows media application that cannot be driven from the keyboard is not finished. REQUIRED: an idle-hide overlay that never hides a control from a keyboard or screen-reader user. REQUIRED: the controls layer sends no side effect the state machine does not already own; playback-effects.ts deliberately exposes four effects and its header says FOUR, AND NO MORE, so a control that needs a fifth is a machine change with its own justification. REQUIRED: the controls read the adapter, not Shaka, so the same UI operates the native engine. |
| PW-0402 | P0 | Security | DONE | - | APPROVED by gpt-architect | Routes authorize the caller, and a launch token is not a login | Sensitive application routes authorize WHO is calling them, and possession of the desktop launch token is never mistaken for that. THE R4 FINDING, QUOTED: docs/SECURITY.md records that 'no authentication or authorization exists on any API route yet' and that 'nothing in the current code should be read as a decision that these routes are safe to leave anonymous'. Acceptable for a web demo; not for a shipped desktop application with a loopback listener. THIS TASK IS DELIBERATELY NOT PW-0101 AND MUST NOT BE FOLDED INTO IT. PW-0101 owns the LISTENER boundary -- loopback-only bind, per-launch bearer, Host validation, CSP, the port handshake -- which answers 'may this process talk to the sidecar at all'. This task answers 'may this caller perform this operation', and the two are different questions with different failure modes. A single machine-wide token that satisfied both would mean every process on the user's PC, and every page the webview ever loads, holds full authority over every profile. REQUIRED, each proven mechanically rather than asserted: (1) a protected route requires an authenticated account/session context and refuses without one; (2) a profile-scoped route authorizes the ACTIVE OR OWNED profile and never trusts a profile id supplied by the caller -- PL-0405 recorded a forgeable ProfileScope as a cross-profile data-access bypass and this is the route-level half of that same defect; (3) a playback or session operation does not become authorized merely because the caller holds the desktop launch token, asserted by a test that presents a valid token and no account context and requires a refusal; (4) a refusal does not leak resource existence where an existing contract requires that property, so 'not yours' and 'not there' are indistinguishable on the wire; (5) loopback origin is NEVER treated as authentication -- a test must present a request from 127.0.0.1 with no credential and require a refusal, because 'it came from this machine' is the exact assumption that makes a local listener a privilege-escalation surface. REQUIRED: the authorization decision is made in ONE place and the routes call it, rather than each handler deciding -- a second opinion about who may read a profile is how the first one stops being enforced. USES THE EXISTING SEAM: packages/auth and the ProfileScope minted by PL-0405 are adopted and wrapped, not replaced. |
| PW-0403 | P0 | Backend | READY | - | - | Production authentication and session resolution | A request in a DEPLOYMENT can be identified, so that an account identity is never a typed header. This task owns the residual gpt-architect named when approving PW-0402, verbatim: PW-0402 does NOT establish production user authentication, and today a desktop build classifying as non-deployment would make the header the identity layer, with nothing currently preventing that classification. THE BOUNDARIES, WHICH THIS TASK DOES NOT MOVE. PW-0101 owns local listener authentication and hardening. PW-0402 owns authorization once an identity is known. PW-0401 owns the authenticated backend service for provider resolution. PW-0403 owns production account and session authentication and nothing else. REQUIRED: ADOPT the reviewed @liberty/auth/server Better Auth seam rather than replacing it. packages/auth/src/better-auth.ts is already the only module in the repository that imports better-auth, it already refuses a surface beyond ENABLED_AUTH_CAPABILITIES, and config.ts already validates secure-cookie, trusted-origin and baseURL behaviour. This task constructs the reviewed instance from that validated config; it does not stand up a second identity system beside it. REQUIRED: expose the Next App Router auth handler and endpoints, importing better-auth/next-js in apps/web rather than in the package, which must stay buildable and testable without Next -- the boundary better-auth.ts states in its own header. REQUIRED: a deployment request resolves AccountIdentity ONLY from a verified database-backed session. resolveRequestAccount gains a real deployment-authenticated branch instead of answering authentication_not_configured. REQUIRED: development headers stay strictly confined to a minted non-deployment runtime capability. The witness discipline in apps/web/src/lib/session/account.ts is preserved exactly -- a deployment cannot obtain an identity there, cannot be issued the witness, and a manufactured one is refused rather than accepted on the strength of its type. REQUIRED: a missing, malformed, expired or revoked session CANNOT produce an AccountIdentity. Each of the four is its own test, because 'expired' and 'revoked' fail through different code paths and a suite that covers only the missing case proves the least interesting one. REQUIRED: the desktop launch token cannot substitute for a user session. This is the exact confusion PW-0402's authorizeRoute was built to prevent by never being given a Request, and authentication must not reintroduce it from the other side. REQUIRED: a loopback origin cannot substitute for a user session. Being on 127.0.0.1 is not an identity. REQUIRED: profile selection stays ABOVE authentication and is not moved into Better Auth user or session product data. active_profile_selection is keyed by session in Liberty's own store, which is what lets the television and the phone act as different profiles; folding it into the identity library's session record would couple the two and lose that. REQUIRED: PW-0402's indistinguishable authorization refusal behaviour is preserved. An unauthenticated request and a request for another household's resource must not become distinguishable because one of them now fails earlier. REQUIRED: secure cookie, trusted-origin and baseURL behaviour comes from the existing validated auth configuration rather than from new literals at the handler. REQUIRED: no credential or secret is placed in the desktop artifact or in any log. REQUIRED: docs/SECURITY.md R4 is updated to state what is then true. Its current text was corrected in round 82 to say authorization is enforced and authentication is not; leaving that in place after this task ships would be a second competing truth. |
| PW-0501 | P0 | Infra | BACKLOG | - | - | A Windows build that CI actually produces | An installable Windows artifact is produced by CI on a windows-latest runner. MEASURED STARTING POINT: every job in .github/workflows/ci.yml is ubuntu-latest, no Windows runner exists, and nothing in this repository builds, packages, signs, installs or updates a Windows application. THIS IS ALSO THE ONLY WAY THIS WORK CAN BE BUILT AT ALL FROM THE ENGINEERING SESSION: the cloud container has a Linux-only Rust toolchain and the linked machine exposes a Linux VM, so no Windows binary can be produced or run here, and a windows-latest job is the substitute for a developer machine rather than a nicety. REQUIRED: the job builds the sidecar, the shell and the installer, and uploads the artifact. REQUIRED: application identity -- product name, publisher, icons, a version scheme derived from one source rather than restated in three manifests. REQUIRED: application data, log and cache locations under the correct Windows known folders, stated and tested, not scattered beside the executable. REQUIRED: the job fails on a real failure; no continue-on-error and no \|\| true, on the rule the e2e job already states. NOT IN SCOPE: code signing, which needs an owner-held certificate and belongs in LAST_MILE. |
| PW-0601 | P0 | Test | DONE | - | APPROVED by gpt-architect | A real-device certification matrix that says who runs each test and why | Every scenario the Windows product must survive is written down, with an explicit owner: automated, or the commander's machine. MEASURED STARTING POINT: docs/TEST_MATRIX.md has eight rows, every cell an automated tier, and no manual, real-device, HDR, hardware-decode or Windows row at all -- while two other documents already require real hardware, for the compositing experiment and for the flash-and-blip A/V rig. REQUIRED: the matrix covers cold start, restart, crash recovery, movie and episodic playback, seek, pause/resume, source switching, resume position, next episode, subtitles, multiple audio tracks, stereo and multichannel, lip-sync and A/V drift, long sessions, 1080p, 4K, HDR where the display supports it, hardware decoding, fullscreen and window transitions, sleep/wake, network interruption and recovery, backend unavailable, malformed or unavailable source, Live TV channel change, EPG behaviour, memory/CPU/GPU behaviour, clean install, upgrade, and uninstall/reinstall. REQUIRED: each row states its owner and, for a commander-machine row, exactly what to do and what a pass looks like -- a matrix that says 'manual' without a procedure is a wish. REQUIRED: rows that cannot be judged without hardware the commander may not have (an HDR display, a multichannel receiver) say so rather than being marked failed. NO ROW MAY BE MARKED PASSED FROM THIS SESSION; nothing here can observe a Windows machine. |
| PL-0303 | P1 | Provider | DONE | - | APPROVED by gpt-architect | Provider health contract | A provider with zero observations reports unknown with a null observed success rate and a zero sample count, never a mid-range score and never a pass; any prior used to rank an unobserved provider is labelled as a prior rather than presented as measured availability; degraded-but-usable is a state distinct from up and down; health output is pure over the observations plus an explicit policy version, with no hidden wall-clock decay; and health never affects entitlement, so a provider at perfect health still cannot make an unauthorized candidate eligible |
| PL-0304 | P1 | Provider | DONE | - | APPROVED by gpt-architect | Media inspection service | Media inspection sits behind its own package boundary rather than inside the provider SDK, because probing is cross-provider I/O plus an external binary with a different security profile; the manifest-parsing path carries no FFmpeg dependency and ships first, reading the declared ladder out of an HLS or DASH manifest without opening a media segment; a fact obtained by probing carries provenance that distinguishes it from a fact merely stated by a provider, and a divergence between the two is preserved rather than collapsed into a fact of unknowable origin; and the service never becomes a general-purpose fetcher, so protocols are allowlisted, egress is confined to an allowlist, hostnames are resolved and private ranges rejected before inspection is invoked, and redirects are revalidated rather than trusted because the first host was allowed |
| PL-0305 | P1 | Provider | DONE | - | APPROVED by gpt-architect | A real catalog metadata source | A metadata source stands behind resolveCatalogMetadataSource and answers what the port deliberately does not. Ingestion: something fetches, schedules and backfills, rather than an array being maintained by hand. Refresh: an answer carries its age and a stated staleness policy, so nobody has to guess how old a rail is. Identity and dedupe: how a provider's native id becomes a normalizedContentId, and what happens when two sources describe the same work, is settled as a design decision before it is coded - this is the largest missing piece and it is not a coding question. Deletion: a work that disappears from a source produces a tombstone, so it stays distinguishable from a fetch that failed. Paging and provider-side search: listRecords returning everything is a shape only six fixtures can use, and who owns ranking and paging is part of the contract rather than an accident of in-memory filtering. Artwork: images arrive with their own licensing, tracked separately from the work's, or they do not arrive. Localization and availability: title, genre and synopsis are locale-tagged and a work can be offered in more than one language, and availability windows and territory exist, so the empty state's claim about a region is backed by something. Every record declares a rights basis whose category agrees with its item and whose reference is an opaque internal identifier or null, and the ingestion path is treated as untrusted third-party I/O - allowlisted egress, bounded bodies, redirects revalidated, and no media address anywhere in a catalog payload. |
| PL-0306 | P1 | Provider | DONE | - | APPROVED by gpt-architect | Fixture provider states the protection fact it actually knows | The fixture provider adapter emits contentProtection { state: "clear" } for its own candidates, because for a fixture the absence of DRM is a fact the adapter genuinely knows rather than something it is guessing. Created from gpt-architect's round-44 non-blocking finding on PL-0501, in the reviewer's own words: "Create a provider-owned task for the fixture provider protection fact. The fixture adapter, not PL-0501, should emit { state: \"clear\" } when that fact is genuinely known. PL-0501 may keep PROTECTION_NOT_STATED as the conservative fallback until that provider task lands." So the deliverable is a narrowing of where the fact is asserted, not a behaviour change in routing: PROTECTION_NOT_STATED stays in PL-0501 as the fallback for every adapter that has not stated a protection fact, and this task removes the fixture from the set that needs it. Required, and each is a way this task could be done wrongly: the fixture must set the field itself rather than PL-0501 special-casing the fixture provider id, because a consumer branching on a provider identity is exactly the provider-specific logic outside @liberty/provider-sdk that invariant 3 forbids; requiresContentDecryptionModule must be observed returning false for a fixture candidate and the observation recorded, since state !== "clear" is the whole point of the encoding and an untested clear is indistinguishable from an untested unknown; the fallback path in PL-0501 must be left in place and a test must still cover an adapter that states nothing, because deleting the fallback once its only current user stops needing it would fail open for the next adapter; and no other adapter may be given a protection fact here, because for a real provider the honest value is unknown and asserting clear for one would be a rights misstatement rather than a defaulting decision. Out of scope: any change to contract shape (PL-0902 owns it), to ranking (@liberty/media-engine), or to capability routing. |
| PL-0307 | P1 | Player | DONE | - | APPROVED by gpt-architect | Forward provider-stated protection into the authorized playback candidate | toCandidateSource forwards a provider's stated protection UNCHANGED, so fixture candidates reach the session as { state: "clear" } while adapters that genuinely state nothing still receive PROTECTION_NOT_STATED. TODAY THE FACT IS STATED AND THEN DISCARDED: PL-0306 made the fixture adapter emit clear at the correct ownership boundary, and apps/web/src/app/api/v1/playback/session/authorized-candidates.ts:473 hardcodes PROTECTION_NOT_STATED instead of reading entry.protection, so the session still publishes unknown. That is the conservative direction and breaks nothing, which is exactly why it could sit unnoticed. Required, and each clause is a way this can be done wrongly: there must be NO provider-id special case, because a consumer branching on provider identity is the provider-specific logic outside @liberty/provider-sdk that invariant 3 forbids -- the forwarding must be uniform; a regression must prove a fixture candidate becomes clear THROUGH THE REAL SESSION BOUNDARY rather than through a unit of the mapper alone, since the mapper is not what the client sees; a second regression must prove an unstated provider still arrives as unknown, because a forwarder that defaults to clear when the field is absent would fail open for every adapter that has not been updated; RANKING MUST REMAIN UNABLE TO READ PROTECTION, which is the PL-0902 separation -- streamCandidateSchema is the ranker's input and protection lives on resolvedStreamCandidateSchema; and NO decryption or key behaviour is introduced by this task. |
| PL-0308 | P1 | Backend | DONE | - | APPROVED by gpt-architect | Production composition registers the catalog ingestion runtime | A hosted deployment that is configured for a real catalog source actually gets one: something in the production composition root calls registerCatalogIngestionRuntime with a runtime built next to the Node pinned-fetch construction. PL-0305 made the application CAPABLE of hosting the real source and the reviewer accepted that as complete; what remains is that NOTHING CALLS THE REGISTRATION, so a hosted deployment still answers no_metadata_source_configured -- a true statement about the deployment rather than about the registry, and the exact gap this task owns. Required: the runtime is built from the existing pinned-fetch transport rather than a second fetch path; NO CREDENTIAL OR API KEY IS INTRODUCED, since the initial source needs none and an ambient key would be the hidden credentialed source PL-0305's review forbade; an unconfigured deployment must STILL refuse by name rather than silently acquiring a default; and a regression must drive the composition root and observe a configured resolution, because a registration nobody has watched fire is not known to work. OUT OF SCOPE: persistent scheduling and storage, which is PL-0309, and the rights register, which is not a coding task. NOTE BEFORE CLAIMING: the exact composition-root file may not exist yet; the declared surface names the likely homes and must be corrected before the claim rather than widened after, on the PL-0710 precedent. |
| PL-0309 | P1 | Backend | DONE | - | APPROVED by gpt-architect | Catalog ingestion is scheduled and stored, not re-run per query | Catalog answers are served from stored ingested state refreshed on a schedule, rather than by running a full ingestion pass per query. TODAY THE ADAPTER RUNS ONE FULL runIngestionPass PER QUERY, which is correct for a seam and wrong for a deployment: it makes every page view an outbound crawl, ties response time to an upstream service, and scales the load on that service with this product's traffic. Required: the freshness and next-pass planning PL-0305 already built is what DRIVES the schedule rather than being recomputed alongside it; an answer still carries its age and stated staleness policy, so nothing becomes fresher by being cached; tombstones survive a refresh, because a work withdrawn upstream must not reappear from stored state; a refresh that fails leaves the previous state served AND SAYS SO, rather than emptying the catalog or silently serving stale data as current; and rights and availability are re-evaluated against stored records rather than trusted from the pass that stored them, since a rights basis can lapse. The reviewer noted that PL-0305 is NOT to be described as having delivered a deployed ingestion worker; this task is where that claim would become true. |
| PL-0311 | P1 | Backend | DONE | - | APPROVED by gpt-architect | apps/web declares the package it imports for the pinned transport | apps/web/package.json declares @liberty/media-inspection AND package-lock.json carries the matching apps/web dependency edge, landing together in one change. PL-0308's server-bootstrap.ts imports nodePinnedFetch from @liberty/media-inspection and apps/web does not declare it; the import resolves through the root workspace symlink, so tsc and vitest are green and NO GATE CATCHES IT, which is the reason to declare it rather than a reason to leave it. Verified 2026-09-20: package-lock.json's packages['apps/web'] dependency map lists seven @liberty packages and not this one, while the manifest -- before this was reverted -- listed eight. npm ci --dry-run returned 0 in that state, so nothing is broken today; the two files simply disagree, and npm's tolerance of a link node that already exists is not a contract. WHY THIS IS A TASK AND NOT A LINE IN PL-0308: package-lock.json is reserved by PL-0710, which is in REVIEW. A task in REVIEW keeps reserving its surface so nothing mutates it mid-review, and PL-0710's approval binds to a tree hash the lockfile is part of. PL-0308 therefore reverted its own manifest line rather than ship a manifest that disagrees with a lockfile it may not touch -- the undeclared import is the pre-existing state, and a NEW inconsistency introduced into a tree under review would have been worse than the old one. Required: both files change in the SAME commit, because either alone is the disagreement this task exists to remove; the lockfile edge is produced by npm rather than hand-written; and npm ci is run from a tree with no node_modules and observed to exit 0, since a dry run against a populated tree is what made the disagreement survivable in the first place. |
| PL-0312 | P1 | Backend | DONE | - | APPROVED by gpt-architect | One authoritative provider health floor, consumed by both packages | ONE authoritative provider-health-floor vocabulary exists and BOTH @liberty/provider-sdk and @liberty/media-engine consume it, with no second literal of the value anywhere in production code. THE DUPLICATION, stated exactly. packages/media-engine/src/ranking.ts:83 declares `export const PROVIDER_HEALTH_FLOOR = 0.5` and line 128 compares `candidate.healthScore < PROVIDER_HEALTH_FLOOR` to produce the `provider_health_below_floor` reason. packages/provider-sdk/src/health.ts:216 separately sets `failBelow: 0.5` in its default policy and line 604 compares `score < policy.failBelow` to produce the `fail` verdict. Three comments in health.ts (lines 177, 391 and 405) and one runtime message (line 625) already ASSERT that the two agree. An assertion in prose is exactly what does not survive an edit to one of the two numbers, which is the whole finding. REQUIRED: the shared declaration lands in a package both consumers ALREADY depend on, or in a new dependency-leaf package, and the implementer states which and why. Measured 2026-09-21: @liberty/media-engine depends on @liberty/contracts only; @liberty/provider-sdk depends on @liberty/contracts, @liberty/media-inspection, @liberty/net-policy and zod; @liberty/contracts depends on zod only. So @liberty/contracts is today the only existing package that both consume and that cannot import either of them, and putting the floor there adds no edge at all. A new leaf on the @liberty/net-policy pattern is also acceptable if the implementer can show why contracts is the wrong home. Adding an edge from media-engine to provider-sdk is NOT acceptable: provider-sdk is the provider-adapter isolation boundary (invariant 3) and media-engine is downstream of it. REQUIRED: a check that FAILS if the two ever disagree again, and it must not be satisfiable by a comment. Either the two consumers read the same exported symbol -- so disagreement is unrepresentable rather than merely tested -- or, where a policy field must stay independently configurable, a test asserts the default policy's `failBelow` equals the authoritative floor and would go red on either number alone being changed. If both mechanisms are used, say which one carries the guarantee. REQUIRED: the two comparisons keep their CURRENT and DIFFERENT boundary semantics, or the change of semantics is deliberate and stated. media-engine excludes a candidate strictly BELOW the floor; provider-sdk returns `fail` strictly BELOW `failBelow`. These agree today. A refactor that quietly moves either to `<=` changes which providers serve, and sharing one constant must not be allowed to smuggle that in. PRESERVED INVARIANT, non-negotiable: health may only SUBTRACT eligibility, never grant it. PL-0303's approval rests on health.ts importing nothing and taking no rights, candidate or source, and on media-engine checking the rights allowlist FIRST and unconditionally. A shared floor module must therefore carry the threshold vocabulary ONLY -- no rights, entitlement, allowlist or candidate types, and no import of provider-sdk or media-engine -- so that the new module cannot become a route by which a health verdict reaches a rights decision. A test asserts the module's import graph is empty of those. NOT IN SCOPE: changing the value 0.5, changing the health algorithm, the prior/measured score split, the policy version enum, or anything PL-0303 delivered. PL-0303 is DONE and APPROVED at ad1e1ea4f3c9b95e10a6140aab7e8ecfdb597e77 and must not be reopened for this. If the correct home for the floor turns out to require a change inside PL-0303's delivered files, that is fine -- those files are on this task's surface -- but the health BEHAVIOUR proven by PL-0303's 31-mutant campaign must be unchanged, and the campaign's tests must stay green without being edited to accommodate the move. |
| PL-0313 | P1 | Backend | DONE | - | APPROVED by gpt-architect | A resumed pass may not infer absence: fix what `complete` means | `complete` means THE WHOLE SOURCE WAS OBSERVED, sufficient to infer absence -- not merely that this invocation reached end-of-enumeration. THE HAZARD, stated exactly. `IngestionPassResult.complete` is set when the provider's cursor runs out. A pass started from a non-null `resumeCursor` can reach that end having DELIBERATELY SKIPPED every page before the cursor, and it reports `complete: true`. `reconcileTombstones` takes `complete` as licence to infer absence, so every known content id living on a skipped page is unseen, is inferred withdrawn, and is mass-tombstoned. Under PL-0309's tombstone rule a tombstone is released only by a later complete pass that accepts the work again, so the damage is not self-healing on the next pass. WHY THIS IS NOT LIVE TODAY, and why that is not a reason to defer it: PL-0309's `schedule.ts` always passes `resumeCursor: null`, which keeps the hazard unreachable at the cost of re-reading the prefix every pass. The reviewer ruled it non-blocking for PL-0309 on exactly that ground and required this task BEFORE resume support is enabled. So the ordering constraint is the deliverable as much as the fix is: whoever adds resume must find this already closed. REQUIRED: absence inference is gated on a fact that a resumed pass cannot satisfy by accident. The implementer chooses the mechanism and states why -- a distinct field separating 'enumeration ended' from 'whole source observed', a fourth `TombstoneWithholdReason` for a resumed pass, or a `seenContentIds` set that makes the reconciliation exact rather than inferential. What is NOT acceptable is a comment telling a future caller not to resume, which is the state this task exists to replace. REQUIRED: a regression that fails against today's code. Start a pass from a `resumeCursor` positioned past a known id, let it run to the provider's end, and assert that id is NOT tombstoned. That test goes red on the current tree -- verify it does and say so -- which makes this a genuine red-to-green rather than a restatement. CONSIDER AND RULE ON, rather than silently skipping: `IngestionPassResult` publishes no seen set, and refusals carry native ids rather than derived content ids, so PL-0309 could not distinguish 'the complete pass saw this work and refused it' from 'never saw it' and took the conservative reading. If `seenContentIds` is the mechanism chosen here, say what it does to that narrowing. NOT IN SCOPE: enabling resume, backfill, or paging beyond `maxPages`; changing the tombstone-RELEASE rule PL-0309 delivered and the reviewer accepted; and redesigning `CatalogStore`, `applyPassToSnapshot` or `refreshCatalogIfDue`. |
| PL-0404 | P1 | Backend | DONE | - | APPROVED by gpt-architect | Watchlist persistence | Typed add, remove, and list APIs with authorization |
| PL-0503 | P1 | Player | DONE | - | APPROVED by gpt-architect | Playback telemetry | Startup, rebuffer, quality switch, and failure events are observable with privacy redaction |
| PL-0504 | P1 | Player | DONE | - | APPROVED by gpt-architect | A/V continuity diagnostics | Browser playback diagnostics detect and report deterministic A/V continuity proxies with their evidence source and reason trail, never label a proxy as measured A/V skew, never expose a millisecond sync-offset claim without an external measurement source, and document the external flash-and-blip procedure for true A/V offset measurement |
| PL-0601 | P1 | Live | SUPERSEDED | - | - | Channel and EPG contracts | Licensed channel, program, schedule, and EPG contracts are typed and documented |
| PL-0602 | P1 | Live | BLOCKED | - | - | Live provider integration | Licensed live provider integrated with normalized channel/EPG contracts |
| PL-0603 | P1 | Live | DONE | - | APPROVED by gpt-architect | Corrective, re-run: channel and EPG contracts under an honest review range | Licensed channel, program, schedule and EPG contracts are typed and documented. A channel carries a REQUIRED, non-nullable rights basis drawn from the one shared vocabulary rather than a restated copy. No media address may appear in a channel or a listing, and that is structural rather than conventional: a playability-bearing-key list, a compile-time witness that the listing shape carries none of them, and strict parsing so a feed sending streamUrl is REFUSED rather than silently stripped. A listing cannot yield a rights basis on its own -- resolving one requires its channel, and a listing naming another channel produces a named refusal rather than null. Ordering over listings is deterministic and total, and an offset-less timestamp is refused rather than guessed. The documentation states what the contract does not cover, including that the opaque rights reference belongs to the provider layer rather than here. |
| PL-0707 | P1 | Security | DONE | - | APPROVED by gpt-architect | Playback session route refuses an oversized request body | The production playback-session route refuses a request body above a declared bound before parsing it, and the refusal carries a reason code that exists in the contract. TODAY IT READS AN UNBOUNDED BODY: apps/web/src/app/api/v1/playback/session/playback-session-implementation.ts reads the body with no cap while the development-only resolve scaffold beside it caps one, so the production path is the weaker of the two. This is a task rather than a line in PL-0702 because the fix cannot be made honestly inside that task's surface: there is no reason code for an oversized body, adding one changes API behaviour, and invariant 5 requires docs/API_CONTRACTS.md to change first or deliberately alongside. Required: the bound is a named constant with the reason for its value, not a magic number; the reason code is added to the contract and to docs/API_CONTRACTS.md in the same change; the refusal happens BEFORE the body is buffered, because a cap enforced after reading the whole body does not bound memory; a regression drives a body over the bound through the real route and observes the refusal, and a second drives one just under it and observes normal handling, since a cap that refuses everything is indistinguishable from an outage. Out of scope: the candidate-id bound, which is PL-0708, and any change to what a well-formed request means. |
| PL-0708 | P1 | Media | DONE | - | APPROVED by gpt-architect | Stream candidate fields carry length bounds | streamCandidateSchema bounds the length of its string fields, so a candidate cannot amplify a response. MEASURED, not hypothesised: a 1,000,000-character candidate id produced a 2,002,555-byte response during the PL-0702 review. The bound belongs in the contract rather than at the route, because every consumer of a candidate inherits it there and a per-route cap is one route away from being forgotten. Required: each bounded field's limit is justified against a real identifier rather than picked round -- state what the longest legitimate value looks like; a property test asserts no candidate accepted by the schema can exceed the response budget; and the change is checked against the fixture and Wikidata sources, because a bound that rejects a legitimate upstream id is an outage, not a fix. THE RISK THIS TASK CARRIES: streamCandidateSchema is the ranker's input and PL-0902 extends it; a bound set too low silently drops candidates and the reason trail must say so rather than showing an empty list. Out of scope: the request body bound (PL-0707). |
| PL-0711 | P1 | Security | DONE | - | APPROVED by gpt-architect | Playback-session candidate wire bounds match StreamCandidate bounds | playbackSessionCandidateSchema uses THE SAME AUTHORITATIVE id and providerId limits as the StreamCandidate contract -- the exported MAX_STREAM_CANDIDATE_ID_CHARS and MAX_STREAM_CANDIDATE_PROVIDER_ID_CHARS -- and NOT copied numeric literals. PL-0708 exported those constants for exactly this reason; a copied literal would recreate the defect one layer out, which is the defect this task exists to close. Required: an oversized resolved candidate CANNOT RE-EXPAND ON THE SESSION WIRE after passing through another producer seam, which is the actual risk -- the contract bound governs what a provider may produce, and a second unbounded restatement downstream means the bound stops at the seam; fixture and Wikidata legitimate identifiers remain accepted, because a bound that rejects a real upstream id is an outage rather than a fix; tests cover the EXACT MAXIMUM, MAXIMUM PLUS ONE, and the serialized response, since a bound tested only from above does not prove the boundary is where it is claimed to be; reason and error output does not echo the oversized value; and NO SECOND INDEPENDENT BOUND VOCABULARY IS INTRODUCED. Note while working: playbackSessionCandidateSchema also carries unbounded uri and mimeType. They are not named in the reviewer's acceptance and are not in scope by default -- if they warrant bounds, say so and let the reviewer rule rather than adding them quietly. |
| PL-0801 | P1 | Recommendations | DONE | - | APPROVED by gpt-architect | Recommendation boundary | The deliverable is a package and information boundary, not a recommender: eligibility is resolved upstream so recommendation can never make content playable, candidate generation may see only the active profile's explicit watchlist, that profile's progress and completion state, catalog metadata and already-resolved eligibility, and it returns content ids plus generator reasons so the reason trail survives the layer most likely to lose it |
| PL-0903 | P1 | Player | DONE | - | APPROVED by gpt-architect | An engine-unavailable reason that can say libmpv did not load | The engine-unavailable vocabulary can represent a native engine failing to load without naming Shaka, a browser or libmpv in the member itself, and the existing web reasons keep their present meaning and their present tests. An engine's own detail is carried beside the reason rather than encoded in it. No unavailability reason reaches the failover scheduler as retryable. The rename or widening is proven by a regression that fails if browser_unsupported is reintroduced as the only way to say an engine is missing. |
| PL-0904 | P1 | Player | DONE | - | APPROVED by gpt-architect | Playback errors carry an origin, so a native failure is not a Shaka number | A playback error names the origin that produced it, and an engine-specific code is interpretable only against that origin rather than being read on one engine's numbering. A native error can be represented without borrowing a Shaka code and without discarding the detail the engine did supply. The classification packages/media-engine consumes stays engine-neutral, derived per origin, and unchanged in meaning for every existing Shaka case, proven by the existing shaka-error regressions continuing to pass unmodified. An error that cannot be classified stays unclassified rather than being assigned a plausible class. |
| PL-AI-0002 | P1 | Coordination | DONE | - | APPROVED by gpt-architect | GitHub bridge for cross-agent review | GitHub becomes the shared state bridge for Claude Desktop and OpenAI review without manual file copying. SCOPE ADDITION 2026-09-21, on gpt-architect's ARCHITECTURE / CI FINDING at f6dba611346ac53d72901f661a2b21cc12e80eac, which ruled the check belongs here rather than in a competing task: CI mechanically detects an UNDECLARED WORKSPACE IMPORT -- a source file in one workspace importing a local @liberty package that its own package.json does not declare in dependencies. THE EXACT DEFECT THIS MUST CATCH, preserved as the regression case: apps/web/src/lib/server-bootstrap.ts imported @liberty/media-inspection/node/pinned-fetch while apps/web/package.json declared seven @liberty packages and not that one. It reached a reviewer because NO EXISTING GATE CAN SEE IT: the import resolves through the root workspace symlink, so typecheck, lint, build and the unit suites were green, and even npm ci --dry-run returned 0 because npm tolerates a link node that already exists. It was found by a human reader, which is not a detection mechanism. Required of the check: it reads each workspace's own manifest rather than the root's; it resolves subpath imports (@liberty/x/node/y declares @liberty/x); it covers test files as well as source, since server-bootstrap.test.ts carried the same undeclared import; and it fails the build rather than warning, because a warning in a green pipeline is how this one survived. A dry-run install against a populated node_modules does NOT satisfy this and must not be offered as the implementation. NOT IN SCOPE: the reverse direction (a declared dependency nothing imports) is a different and much weaker signal, and adding it would make the check noisy enough to be turned off. SECOND SCOPE ADDITION 2026-09-22, on gpt-architect's ruling at cd6f3efd46a75a150206617a2fa6bf747a1c3a5b: 'CI must not rely on typecheck and build racing successfully when typecheck consumes files build writes.' THE DEFECT, verified structurally rather than inferred from a flake: apps/web/tsconfig.json line 15 includes .next/types/**/*.ts; turbo.json's build task declares .next/** as an output; and turbo.json's typecheck task declares dependsOn ['^typecheck'] with NO edge to build. So tsc and next build race on that directory whenever both run in one graph. HOW IT SURFACED, because the shape of the evidence matters: a single @liberty/web#typecheck exit 2 with NO 'error TS' line in the output, which passed on two re-runs of the identical tree. The command that produced it -- npx turbo run typecheck lint build --force -- had been this project's recorded gate invocation for several rounds and had passed every time, which is what a race looks like until it does not. The lead changed its own procedure to three separate invocations, which removes the race from the evidence but NOT from CI. Required: CI must not be able to run the two concurrently in a way that lets a typecheck read a partially written .next/types. The mechanism is the implementer's -- a dependsOn edge from typecheck to build, a separate tsconfig for the check that excludes generated types, or sequencing in the workflow -- but a green run is not evidence the race is gone, so whatever is chosen must be demonstrated by argument from the task graph rather than by re-running until it passes. NOT IN SCOPE: fixing the unrelated pre-existing env:validate warnings. SMALLER AND RELATED, folded in here rather than filed: turbo.json's globalEnv lists none of the LIBERTY_CATALOG_* variables, including the eight predating round 63. Harmless today because they are read at runtime by instrumentation.ts rather than at build time, so it affects only build-cache hashing -- but a variable that is read at build time later and still unlisted would let one cache entry serve builds that meant different values, which is the defect env:validate already warns about for LIBERTY_FIXTURE_MEDIA_ORIGIN. |
| PL-AI-0003 | P1 | Coordination | DONE | - | APPROVED by gpt-architect | Optional API-driven autonomous dispatcher | Optional external agent runner can dispatch queued tasks to configured providers with budgets, audit logs, retries, and human approval gates |
| PL-AI-0008 | P1 | Coordination | DONE | - | APPROVED by gpt-architect | An agent instruction file has provenance, or it is not an instruction | Repository validation detects any AGENTS.md or CLAUDE.md under a working directory that is not on an explicit, reviewed allowlist, and the authoritative instruction hierarchy is stated somewhere an agent reads before it starts. THE CONCRETE CASE: apps/web/AGENTS.md is GENERATED BY next dev and instructs agents to read node_modules/next/dist/docs/ before writing code. Nothing acted on it, and it is almost certainly benign. The finding is not that file -- it is that framework tooling can write an instruction file into a directory agents treat as authoritative, which makes a dependency's postinstall a path into the control plane's instruction surface. Required: the allowlist names each legitimate instruction file and who owns it, so an unowned one is a validation failure rather than a judgement call; generated instruction files are either gitignored and removed or allowlisted after being read, and the choice is recorded per file; validation fails on an unexpected file rather than warning, because a warning in a passing run is not a control; a regression plants an unexpected AGENTS.md and observes the failure, since a detector nobody has seen fire is not known to work; and the instruction hierarchy is written down -- which file wins, and that nothing under node_modules is ever authoritative. WHAT THIS TASK MUST NOT DO: silently delete a file a framework needs, or treat allowlisting as review. An allowlisted generated file must have been READ by a human or an agent that reports what it says. |
| PL-AI-0009 | P1 | Coordination | DONE | - | APPROVED by gpt-architect | release clears the implementation base, or a re-claim inherits a stale one | ai:release clears implementationBaseSha, so a task returned to an unowned queue and later re-claimed records the base of the round that actually implements it. OBSERVED, not hypothesised: PL-0710 was claimed and started at 33195d593695618b779ab6c97a50817a2694e8df, released with nothing written when its implementing agent was terminated, and re-claimed at HEAD 20edec3aa3f33dbd58aa405d1210f56ce54c207f -- and start reported 'started from 33195d593695', preserving the stale base rather than capturing HEAD. CLAUDE.md says implementationBaseSha is the exact lower bound of the first review range and that the range validator refuses a base that is either wider or narrower, so a preserved base makes the published window wider than the implementation. The rule release already follows is the argument: it discards gate results because 'evidence belongs to one implementation round under one owner', and a base is evidence of where that round began. Required: release clears the base, and ai:unblock is checked for the same defect and fixed if it has it; a scenario in scripts/test-ai-control-plane.mjs claims, releases, re-claims at a different HEAD and asserts the second base is the second HEAD, because this defect is invisible until exactly that sequence occurs; and the fix must NOT let start overwrite a base on an ordinary re-start of a task that was never released, since that is the falsification --reconcile-existing exists to prevent. Do NOT hand-edit any existing task's recorded base as part of this work; PL-0703 was blocked for that move and the historical records stay as they are. |
| PL-AI-0010 | P1 | Coordination | DONE | - | APPROVED by gpt-architect | A property suite gets a timeout of its own, not vitest's default | packages/contracts declares its own vitest testTimeout, with the reason for the value written down, so its fast-check property suite is not running against a default it can exhaust under load. THIS CLOSES AN OPEN ITEM RATHER THAN OPENING ONE. Round 52 recorded a single observed-but-unreproduced failure in stream-candidate.property.test.ts and the lead could not reproduce it across ten seeds; the PL-0710 part-A work reproduced it deterministically and it was never seed-dependent. It is CONCURRENCY-dependent: the box has 2 cores, turbo's default concurrency is 10, the new @liberty/net-policy package takes the graph from 18 test tasks to 20, and 'refuses a licence endpoint that is not https, whatever else it is' -- which runs in 1.73s standalone -- measured 5.4s and 7.4s under that graph against vitest's 5000ms default. Characterised rather than re-rolled: the same tree at --concurrency=4 passes, and the same tree filtered to 19 tasks at default concurrency passes. Required: the timeout is justified against the suite's measured standalone cost and the headroom a loaded machine needs, not picked round; the config explains that a property suite's cost is a function of run count and machine load rather than of the code under test, so a default sized for unit tests is the wrong instrument; and NOTHING ABOUT THE PROPERTY ITSELF IS WEAKENED -- not the run count, not the generators, not an assertion. Raising a timeout to hide a genuinely slow property would be the same mistake one level out, so if the suite is slow for a reason the code should answer for, say so instead. Out of scope: a turbo concurrency cap, which is a CLI flag rather than a turbo.json key and is a CI decision. |
| PL-AI-0011 | P1 | Coordination | DONE | - | APPROVED by gpt-architect | A superseded task needs a terminal state that is true | A superseded task has a TRUTHFUL terminal representation, so that a provenance-invalid original whose work shipped under a named successor is neither left BLOCKED forever nor mislabelled as work nobody did. THE GAP, measured rather than asserted. control/policies.json lists eight statuses and exactly two are terminal: DONE and CANCELED. BLOCKED transitions only to BACKLOG, READY or CANCELED. There is NO cancel command on the CLI -- the reachable verbs are validate, sync, status, dispatch, claim, start, gate, review, approve, request-changes, done, block, unblock, release and event -- so CANCELED is currently reachable only by hand-editing control/tasks.json, which is the move the operating contract forbids. And scripts/ai-control-plane.mjs states at its gate-recording guard that CANCELED means 'there is no work to evidence', which is FALSE for a superseded original: the work exists, was reviewed and shipped under the successor's id. THE CONTROL PLANE ALREADY KNOWS ABOUT SUPERSESSION AND DELIBERATELY DOES NOT ACT ON IT. The comment at the supersession detector says a corrective re-run supersedes its predecessor and 'the predecessor is left BLOCKED deliberately -- its provenance record is preserved as audit history rather than repaired, on the rule PL-0703 established', and that BLOCKED can never reach DONE so requireAllDependenciesDone gates every dependent forever. It reports and never repairs, by design. This task does not change that: it adds a way to SAY the thing the detector already notices. REQUIRED: a terminal status that means superseded and says so -- the implementer chooses the spelling and justifies it -- carrying a REQUIRED reference to the successor task id, validated to exist and to be DONE. A status that permits a null successor is not acceptable: the whole content of the claim is which task carries the work now. REQUIRED: a control-plane command to reach it, because a terminal state reachable only by hand-editing the source of truth is worse than no terminal state. The transition must be refused when the named successor is not DONE, and the audit event must carry both ids. REQUIRED: a superseded task must NOT count as DONE in progress figures and must NOT satisfy requireAllDependenciesDone by pretending to be complete. Whether it should satisfy a dependency AT ALL is the design question and the implementer must answer it explicitly: a dependent of a superseded task is almost certainly meant to depend on the successor, and the existing detector already reports exactly that -- so the safe answer may be that supersession does NOT satisfy the dependency and the detector's advice to repoint stands. REQUIRED: the four current instances are the acceptance test, and their subsumption was audited in round 73 before this task was filed. PL-0205 superseded by PL-0207 (DONE), PL-0401 by PL-0405 (DONE), PL-0601 by PL-0603 (DONE), PL-0703 by PL-0706 (DONE). In every case the successor's acceptance was read in full against the original's and found to be a strict superset, not merely a DONE task with a similar name. NOT IN SCOPE: repairing the four originals' provenance, which the reviewer has ruled cannot be done in place; automatically repointing dependencies, which the existing comment refuses on the ground that a self-modifying graph is a larger failure than the one it fixes; and any change that lets a superseded task reach DONE. |
| PW-0104 | P1 | Infra | READY | - | - | A killed desktop dev server cannot leave a tracked file rewritten | A desktop build or dev server cannot leave apps/web/next-env.d.ts rewritten in the working tree, under ANY termination. THE DEFECT, OBSERVED RATHER THAN PREDICTED, in round 81: the file was committed pointing at ./dist/desktop/dev/types/routes.d.ts instead of ./.next/types/routes.d.ts. Next regenerates that TRACKED file to name whichever distDir last built; the three *:desktop scripts snapshot and restore it around the child; and build-target.ts:300-342 already records that the restore happens ONLY ON NORMAL EXIT. Playwright signal-kills `next dev` when the harness finishes, so the rewrite survived, was staged by a routine `git add -A`, and was caught only by reading the diff before delivery. It typechecked locally because dist/desktop/dev happened to exist; on a fresh clone it would not have. REQUIRED: a forced termination -- SIGTERM, SIGINT, SIGKILL of the child, and the parent being killed -- cannot leave the tracked file dirty. REQUIRED: THE SOLUTION MUST NOT DEPEND ON A NORMAL-EXIT CLEANUP HOOK ALONE. A second `process.on('exit')` handler is the same mechanism that already failed and would fail the same way under SIGKILL; the implementer must choose a design that is correct when no cleanup runs at all, and justify it. Two candidates, neither mandated: make the file untracked and generated (which changes what a fresh clone must do, and docs/DEVELOPMENT.md must then say so), or make the desktop distDir's type path stable so no rewrite occurs. REQUIRED: the regression is MECHANICALLY TESTED -- a test that starts a desktop invocation, kills it without letting it clean up, and fails if the tracked file differs from HEAD. A test that only exercises normal exit reproduces nothing, because normal exit already works. REQUIRED: the fix does not weaken the build-target split; build-target.test.ts must keep passing, including its assertion that no shipped module under apps/web/src names LIBERTY_BUILD_TARGET. NOT IN SCOPE: anything about product behaviour. This is a tooling defect and gpt-architect's ruling is explicit that it must not be folded into unrelated product work. |
| PW-0206 | P1 | Player | BACKLOG | - | - | Subtitle and audio track selection, on both engines, for the first time | A viewer can choose an audio track and a subtitle track, and the choice survives a candidate switch. THIS CAPABILITY DOES NOT EXIST AT ALL TODAY: there is no getTextTracks, no selectAudioLanguage, no track state and no UI anywhere in the repository, while PRODUCT_SPEC.md names 'Accessibility, subtitles, and audio correctness are core features' as a product principle and RELEASE_CRITERIA.md lists 'Audio/subtitle preferences implemented' as an MVP bar. REQUIRED: one normalised track vocabulary that both adapters answer in, so the UI never branches on engine. REQUIRED: the audio-language and subtitle-preference policy already contracted in packages/contracts/src/domains/audio.ts and subtitles.ts is the source of the DEFAULT selection, rather than a second policy being invented here. REQUIRED: forced and default subtitle flags are honoured, which the existing test matrix already lists as a playback regression scenario. REQUIRED: a selection made by the viewer is re-applied after a failover, because a candidate switch is a full teardown and losing the viewer's language on a network blip is the defect this clause exists to prevent. |
| PW-0208 | P1 | Infra | BACKLOG | - | - | An LGPL-compatible libmpv and FFmpeg the product may actually ship | The libmpv and FFmpeg binaries the installer ships are built under terms this product may distribute, and the evidence is recorded rather than assumed. docs/DESKTOP_PLAYBACK.md section 9 names two traps and RESEARCH_PLAYBACK.md already found the equivalent defect elsewhere -- 'every prebuilt ffprobe on npm is a GPL-3.0 binary, and several declare otherwise' -- so a downloaded build is not evidence of its own licence. REQUIRED: the configure flags that keep the build LGPL are stated and reproducible, and any GPL-only component is named and excluded. REQUIRED: the written-offer and attribution obligations LGPL imposes on a shipped binary are satisfied by something in the installer, not by a sentence in a README nobody ships. REQUIRED: the exact upstream versions are pinned and recorded, because PW-0207's error mapping is built against a specific header. NOT IN SCOPE: any decoder whose distribution needs a patent licence this project does not hold -- if one is required, stop and report it as a commander decision rather than shipping it. |
| PW-0304 | P1 | Frontend | READY | - | - | Watchlist becomes usable: an add control and a list | A person can add a title to their watchlist and see the list. The API and its mutation rules are complete, tested and reviewed -- including the conflict semantics in packages/persistence/src/watchlist-mutation.ts -- and nothing renders them. REQUIRED: the add control appears on the title screen and on a catalog card, and reflects state optimistically while remaining correct when the write is refused. REQUIRED: the resolution vocabulary the API already returns (accepted plus a reason, not a boolean ok) is surfaced honestly; a refused write must not render as a success, which is the shape of defect the harnesses in this repository have caught four times. |
| PW-0307 | P1 | Frontend | BACKLOG | - | - | Series navigation and the next episode | A viewer can move through a series without going back to the catalog. Today every season of a series renders as one flat stack on the title page, with no season selector, no per-episode progress and no way to reach the next episode. REQUIRED: season navigation that does not lose the viewer's place. REQUIRED: per-episode watched and in-progress state from the progress API. REQUIRED: a next-episode affordance at the end of playback, honouring the SAME per-episode rights gate the episode list already applies -- an autoplay that skips the rights check would be the rights breach PL-0703 was opened for, in a new place. REQUIRED: the next-episode selection rule is a pure function with its own tests, not logic inside a component, because 'what plays next' is exactly the kind of decision this project requires to be reconstructible. |
| PW-0309 | P1 | Frontend | READY | - | - | Offline, degraded and error states a desktop application needs | The application behaves honestly when the network, the backend or the sidecar is unavailable. There is no offline handling of any kind today -- no navigator.onLine, no service worker, no offline banner, no retry-on-reconnect. THIS MATTERS MORE ON THE DESKTOP THAN IT DID ON THE WEB: docs/DESKTOP_PLAYBACK.md section 8 accepts that an offline desktop cannot resolve playback at all, and requires that failure be surfaced as an honest unavailable outcome rather than an empty candidate list -- so the UI half of that ruling is this task. REQUIRED: a network-state signal and a visible degraded mode that names what is unavailable. REQUIRED: recovery on reconnect without a manual reload. REQUIRED: the sidecar being down is a DISTINCT state from the backend being down and from the network being down, because the three have different remedies and collapsing them produces a support burden. REQUIRED: no state invents content; an offline home shows what it has and says so. |
| PW-0310 | P1 | Frontend | BACKLOG | - | - | Keyboard, focus and accessibility across the whole application | The application is operable from the keyboard, end to end, and PRODUCT_SPEC.md's principle that 'accessibility, subtitles, and audio correctness are core features' is true of the shipped product rather than of three components. MEASURED STARTING POINT: there are zero keyboard handlers in the non-player UI, no skip link, no focus trap and no focus restoration; the rails are not arrow-key navigable, which is table stakes for a desktop media application. REQUIRED: roving tabindex on rails and grids. REQUIRED: focus restoration on route change and on dialog close. REQUIRED: a visible focus indicator on every interactive element, including those styled by globals.css, which defines none today. REQUIRED: an automated check in the e2e suite that fails when a new interactive element is unreachable by keyboard, because a one-off audit decays and a gate does not. NOT IN SCOPE: a full WCAG certification; the bar here is operability plus a mechanical guard against regression. |
| PW-0401 | P1 | Backend | READY | - | - | The authenticated backend the desktop build talks to | The counterparty the desktop forwarder already speaks to exists as a service instead of only as an e2e stub. docs/DESKTOP_PLAYBACK.md section 8 was ruled by the commander: provider resolution must not rely on the user-administered sidecar as its trust boundary, and the desktop build proxies to an authenticated backend. The FORWARDER is implemented and tested; the thing it forwards TO is e2e/src/backend-stub.mjs. REQUIRED: the same route contract, byte for byte -- the cross-target e2e suite already compares whole bodies between targets and must keep passing. REQUIRED: caller authentication, and a refusal that does not leak whether a content id exists. REQUIRED: provider credentials live only here, never in a desktop artifact, env var, config file or cached response; the build-target import-graph guard already proves the desktop bundle cannot reach @liberty/provider-sdk and this service is where that capability actually lives. REQUIRED: it is deployable and testable WITHOUT a licensed provider -- the resolution path must return an honest unavailable with provider_not_configured when none is wired, which is exactly what the web target already does. THIS TASK DOES NOT NEED CREDENTIALS and must not wait for them; PL-0302 remains the separate task that wires a real provider into it. |
| PW-0502 | P1 | Infra | BACKLOG | - | - | Updates, and a version the application can prove | The application can tell a user what version it is and can update itself safely, or states plainly that it cannot yet and why. REQUIRED: an update mechanism whose payloads are signature-verified -- an updater that fetches and executes an unverified binary is a remote code execution feature, and SECURITY.md's threat list already ranks unauthorized access above convenience. REQUIRED: the update endpoint is pinned and allowlisted like every other egress in this repository. REQUIRED: a rollback story, or a stated decision that there is none and what a user does instead. REQUIRED: the version is derived from one source shared with PW-0501. IF SIGNING IS UNAVAILABLE the update path must be disabled by default and say so, rather than shipping an unverified one -- an unsigned auto-updater is worse than no updater, and that dependency goes to LAST_MILE rather than being worked around. |
| PW-0503 | P1 | Test | BACKLOG | - | - | Install, upgrade, uninstall, reinstall — tested, not assumed | The installer's lifecycle is exercised automatically wherever that is possible on a runner, and everything that is not is named for the commander's machine. REQUIRED: clean install on a machine that has never had the application, then launch. REQUIRED: upgrade over a previous version with user data preserved, which is the one a release breaks most often and nobody tests. REQUIRED: uninstall leaves no service, no scheduled task and no orphaned node.exe, and states exactly what user data it keeps or removes. REQUIRED: reinstall after uninstall. REQUIRED: each case is marked automated or commander-machine, with the reason, so the split is visible rather than implied. |
| PW-0602 | P1 | Test | BACKLOG | - | - | The automated half of the Windows matrix, running on Windows | Every row PW-0601 marks automated runs on a windows-latest runner and fails the build when it fails. REQUIRED: the existing e2e suite runs against the DESKTOP target on Windows, not only the web target on Ubuntu -- the desktop axis is currently built on Ubuntu only, and only incidentally, because the axis defaults on and openssl happens to be installed. REQUIRED: the shell is driven end to end where the harness can reach it, and where it cannot, the row stays in the commander-machine half rather than being replaced by a unit test that proves something easier. REQUIRED: retries stay at zero, on the rule playwright.config.ts already states at length. REQUIRED: a row that is skipped says why, by name, the way the existing harness already does for the desktop and media-rig axes. |
| PW-0603 | P1 | Test | BACKLOG | - | - | The commander's run sheet, and the evidence it produces | The commander can run the real-device half in one sitting and hand back evidence that is worth recording. REQUIRED: an ordered run sheet with a stated environment -- Windows build, GPU, driver, display, audio device -- because a pass on unrecorded hardware is not reproducible evidence. REQUIRED: for each row, the exact action, the expected result, and what to capture when it fails. REQUIRED: a defect report format that carries enough to reproduce: the reason trail the player already publishes, the log location PW-0501 establishes, and the version PW-0502 exposes. REQUIRED: the sheet is honest about how long it takes. THE RUN ITSELF IS A LAST_MILE ITEM and this task delivers the sheet, not the results; recording a pass nobody observed would be fabricating a gate result. |
| PL-0310 | P2 | Frontend | DONE | - | APPROVED by gpt-architect | A configured catalog with nothing usable does not look empty | The user-facing catalog path preserves the distinction the port now makes between no_records_usable and catalog_empty. apps/web/src/lib/catalog.ts maps an empty array to 'empty' and therefore COLLAPSES THE TWO, which is the one place PL-0305's four-state work stops short of the user. The two states have different causes and different remedies: catalog_empty means the source has nothing, while no_records_usable means records exist and every one of them was withheld -- almost always for want of an operator rights basis, which is an operator action rather than a user problem. Required: catalog.ts consumes describeCatalog through requireCatalogDescription rather than inferring state from an empty array; a source that does not implement describeCatalog still works and is not reported as a failure, because the method is optional on the port by design; the user-facing copy for the withheld case does NOT promise the content is coming, since nobody has committed to obtaining those rights; and the withheld reasons are not leaked verbatim to the user, because they name internal policy vocabulary. A regression must drive both states through the real loader and observe different answers. |
| PL-0709 | P2 | Media | DONE | - | APPROVED by gpt-architect | Egress host classification folds the DNS root label | packages/media-inspection/src/egress.ts classifies a fully qualified name the same as its dotless spelling, matching the fix PL-0702 made in packages/provider-sdk/src/stremio/url-policy.ts. THIS IS LOWER PRIORITY THAN IT SOUNDS AND THE REASON MUST NOT BE LOST: this path fails CLOSED -- an unrecognised spelling is refused rather than permitted -- so it is an inconsistency between two classifiers, not a bypass. It is filed anyway because two classifiers disagreeing about what a host is will eventually be reconciled by someone copying the wrong one. Required: the fix is the same narrowing (fold the root label before comparison, refuse an empty label rather than repairing it); a test asserts the two classifiers agree on a shared table of hostile spellings, so the next divergence fails a test rather than waiting for a review; and the fail-closed behaviour is preserved and asserted, because making this path agree with the other one must not make it permissive. Out of scope: merging the two classifiers into one, which is the resolve-and-pin question and belongs to PL-0710. |
| PL-0712 | P2 | Test | DONE | - | APPROVED by gpt-architect | A repo guard that times out under load is a guard nobody can read | packages/media-inspection/src/net-policy-boundary.test.ts:396, the test named “is not restated by any file outside this package”, stops being a test whose result depends on machine load and on what is on disk, WITHOUT weakening the property it asserts. THE DEFECT, DIAGNOSED RATHER THAN NAMED. Its failure mode is 'Error: Test timed out in 5000ms', not an assertion failure -- the assertion has never reported a false offender; the test simply does not finish. Its listSourceFiles recurses into every directory under packages/ and apps/ with NO exclusions and reads every .ts file it finds: 3,680 files totalling 24.3 MB on the tree measured, of which 3,337 are under node_modules and the rest include build output. The set of files the property is actually about is 327, reachable in 1-2 ms once node_modules, .next, .turbo, dist and coverage are skipped. It is the only test in this repository whose runtime is a function of installed dependencies and build artifacts rather than of source, which is why it presents as intermittent: every other repo-walking guard here -- packages/net-policy/src/module-boundary.test.ts, packages/contracts/src/module-boundary.test.ts, packages/provider-sdk/src/stremio/net-policy-boundary.test.ts and apps/web/src/app/watch/route-loading-boundaries.test.ts -- is scoped to its own SRC_DIR or APP_DIRECTORY. THE REPRODUCTION GRADIENT, measured on 2026-09-22 at e24d51f on a 2-core container: the test body alone, idle with a warm page cache, 109 ms; the whole file in an isolated vitest run, 687 ms; the file under turbo run test --force with a warm cache, 849-995 ms; the same under turbo run test --force immediately after dropping the page cache, 3,048 ms. The observed failure happened on a run started immediately after the e2e suite had rewritten apps/web/.next and apps/web/dist/desktop -- thousands of freshly written files, a cold cache, background flushing, and up to nine other workspace vitest processes competing for two cores -- which is where that gradient crosses the 5,000 ms default testTimeout. REQUIRED: the walk must not descend into node_modules, .next, .turbo, dist or coverage, and the exclusion list must be stated with its reason rather than left as a magic array. REQUIRED: the property must not be narrowed to buy the speed -- it must still see every source file under packages/ and apps/ outside this package, and the implementer must show that a planted offender is still caught. A RAISED testTimeout IS NOT AN ACCEPTABLE FIX ON ITS OWN: it moves the ceiling without removing the dependence on disk state, and the next person to add a dependency moves the runtime again. SECOND DEFECT, FOUND WHILE DIAGNOSING AND IN SCOPE: build output ALREADY contains the string this test looks for. Seven files under apps/web/.next and apps/web/dist/desktop carry a reference-path fragment naming m3u8-parser.d.ts inside bundled chunks. They are .js today so the .ts filter misses them, which means the test is one bundler-output change away from naming a build artifact as an offender and failing for a reason that has nothing to do with any source file. The exclusion above fixes the correctness hazard and the performance one with the same edit, which is the argument for doing it that way rather than by raising a timeout. |
| PW-0207 | P2 | Player | BACKLOG | - | - | A native failure says what went wrong instead of returning null | classifyNativeFailure maps mpv's error enum to PlaybackFailureKind instead of returning null on every path. TODAY BOTH ARMS OF THAT FUNCTION RETURN null (playback-failure.ts:262) above a 44-line comment that names exactly what would change it: reading the mpv_error enum off include/mpv/client.h. This is a documented placeholder with a stated unblock condition, and PW-0204 is that condition. REQUIRED: the mapping is built against the header of the libmpv version actually shipped, and the version is recorded in the gate evidence, because the enum is not stable across major versions. REQUIRED: an unmapped error stays null rather than being assigned a plausible kind -- null is a contractually valid 'cannot tell' and guessing a retryable kind for an unknown fault is how a permanent failure becomes an infinite retry. REQUIRED: a DRM-adjacent native fault can never map to a retryable kind, mirroring the web path where the whole Shaka DRM category maps to rights_unverifiable and is never retried. |
| PW-0308 | P2 | Frontend | BACKLOG | - | - | Settings a viewer can actually change | There is a settings screen, and every switch on it changes something real. Neither a screen nor an API exists today. REQUIRED: preferred audio and subtitle languages, persisted per profile and consumed as PW-0206's defaults rather than as a second policy. REQUIRED: a diagnostics and telemetry control that is honest about what it does -- telemetry-decision.ts already reports WHY telemetry is off, and that reasoning belongs in front of the viewer instead of in a details element in the player. REQUIRED: an about section naming the application version and the licences the installer must carry, which is where PW-0208's LGPL attribution obligation is discharged in the product. REQUIRED: no setting weakens a security or rights control; there is no 'allow insecure sources' switch and no way to disable the transport checks. |
| PW-0311 | P2 | Live | BACKLOG | - | - | Live TV and the guide | A channel list and a guide exist and they obey the rule LIVE_TV.md exists to state: A LISTING IS NOT AN ENTITLEMENT. REQUIRED: playability is read from the CHANNEL's rights basis and never from a listing, which the contract already makes structurally impossible to get wrong -- EpgListing has no field capable of expressing playability and a compile-time guard keeps it that way -- so the UI must not reintroduce the idea by inferring it. REQUIRED: channel change and guide navigation from the keyboard. REQUIRED: a listing for a channel the viewer has no basis for renders as information, not as a play affordance. DEPENDS ON PL-0602, WHICH IS BLOCKED ON LICENSED LIVE FEED ACCESS, and that dependency is real: the contract is implemented and tested, the acquisition is not, and building a guide over fabricated channels would be fabricating provider success. This task stays BACKLOG until PL-0602 can move. |

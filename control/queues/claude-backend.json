{
  "generatedAt": "2026-09-18T23:16:09.995Z",
  "agent": "claude-backend",
  "executionAvailable": true,
  "assigned": [
    {
      "id": "PL-0305",
      "priority": "P1",
      "lane": "Provider",
      "status": "REVIEW",
      "title": "A real catalog metadata source",
      "notes": "The discovery layer has no metadata source. PL-0105 landed a seam, and docs/CATALOG_SOURCE.md says so in its first line; this task is the thing that belongs behind it, tracked so the gap stays visible instead of being hidden by a port that looks finished. docs/PRODUCT_SPEC.md step 2 of the initial user journey is browse/search normalized metadata, and nothing in this repository ingests any: every surface reads six fictional works. PL-0301 and PL-0302 do not close it - they supply stream candidates, which is what a title plays from, not what a catalog is made of. PARTLY BLOCKED, and the split is deliberate rather than a hedge. The design half - identity and dedupe, refresh and staleness, tombstones, paging, provider-side search, the locale-tagged contract, and the undeclared-rights state that catalogItemSchema cannot currently express - needs no external access and can begin now. Connecting an actual metadata provider, and carrying artwork, cannot: both need an agreement and credentials, which control/policies.json escalates to human-only under Licensing and Credentials - the same categories that block PL-0302. It is a DIFFERENT agreement from PL-0302's, covering catalog metadata and image rights rather than stream rights, so this is not merely waiting on that task; the honest status is BACKLOG with the ceiling named, not BLOCKED on someone else's licence.",
      "dependencies": [
        "PL-0105"
      ],
      "allowedPaths": [
        "packages/catalog-ingestion/**",
        "apps/web/src/lib/catalog-source.ts",
        "apps/web/src/lib/catalog-source-registry.ts",
        "docs/CATALOG_SOURCE.md",
        "docs/ARCHITECTURE.md",
        "package-lock.json"
      ],
      "preferredAgent": "claude-backend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "architecture-review",
        "security-review",
        "rights-review",
        "typecheck",
        "unit"
      ],
      "acceptance": "A metadata source stands behind resolveCatalogMetadataSource and answers what the port deliberately does not. Ingestion: something fetches, schedules and backfills, rather than an array being maintained by hand. Refresh: an answer carries its age and a stated staleness policy, so nobody has to guess how old a rail is. Identity and dedupe: how a provider's native id becomes a normalizedContentId, and what happens when two sources describe the same work, is settled as a design decision before it is coded - this is the largest missing piece and it is not a coding question. Deletion: a work that disappears from a source produces a tombstone, so it stays distinguishable from a fetch that failed. Paging and provider-side search: listRecords returning everything is a shape only six fixtures can use, and who owns ranking and paging is part of the contract rather than an accident of in-memory filtering. Artwork: images arrive with their own licensing, tracked separately from the work's, or they do not arrive. Localization and availability: title, genre and synopsis are locale-tagged and a work can be offered in more than one language, and availability windows and territory exist, so the empty state's claim about a region is backed by something. Every record declares a rights basis whose category agrees with its item and whose reference is an opaque internal identifier or null, and the ingestion path is treated as untrusted third-party I/O - allowlisted egress, bounded bodies, redirects revalidated, and no media address anywhere in a catalog payload.",
      "owner": "claude-backend",
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-09-17T04:17:53.810Z",
          "by": "claude-backend",
          "commitSha": "645e58562dc0e2376f7395da19d0991a9899e972",
          "evidence": "npx turbo run typecheck --force at integrated head 645e58562dc0e2376f7395da19d0991a9899e972, exit 0, 10 of 10 tasks and 0 cached. The lockfile did NOT change this round -- no dependency was added -- and npm install --package-lock-only left package-lock.json byte-identical; npm ci exits 0 in a clean checkout of the tracked tree rather than in the worktree, whose node_modules is shared."
        },
        "unit": {
          "status": "pass",
          "at": "2026-09-17T04:17:54.014Z",
          "by": "claude-backend",
          "commitSha": "645e58562dc0e2376f7395da19d0991a9899e972",
          "evidence": "npx turbo run test --force at integrated head 645e58562dc0e2376f7395da19d0991a9899e972, exit 0, 17 of 17 and 0 cached, 2318 passed; catalog-ingestion now 117 cases across 8 files. THE COMMANDER'S LICENSING DECISION IS NOW IMPLEMENTED: Wikidata stands behind resolveCatalogMetadataProvider, which closes the first acceptance clause that round 44 correctly refused to claim. The port did not move -- wikidata.ts imports FROM provider.ts and nothing in the port imports back, and ingest, project, identity, freshness and safety never mention Wikidata -- which is what keeps the decision an INITIAL source choice rather than a shape baked into the seam. CAPABILITIES ARE DECLARED HONESTLY RATHER THAN OPTIMISTICALLY, each measured: providerSideSearch true; incrementalSince FALSE because WDQS's default graph binds nothing for schema:version or dateModified and the Action API that has them costs a measured 206 KB per entity, so changedSince is ignored entirely rather than silently approximated; reportsDeletions FALSE because deletions need a different API; maxPageSize 50, the number the source itself uses; and sourceRevision ALWAYS NULL, which is a real gap stated as one -- a consumer cannot tell whether a record changed between passes. Enumeration is keyset-paged on the numeric QID rather than OFFSET, because CirrusSearch hard-fails at offset 10,000, measured. THE CC0 / CC BY-SA DISTINCTION IS ENFORCED BY FOUR MECHANICAL CONTROLS, NOT DOCUMENTED: a frozen two-host set with construction refused if the operator's egress allowlist names anything else, so en.wikipedia.org in the policy yields NO provider; an entity-id pattern admitting only Q, P and L, with Commons M-ids refused by name; no SERVICE clause in any built query, asserted; and no request for P18, P154, P3383, sitelinks or extracts, with artwork always empty and unable to be otherwise. THE USER-AGENT OBLIGATION IS STRUCTURAL: the option stays required with no default, createWikidataProvider additionally refuses an agent string with no contact token or with a browser token by two separate named refusals, and a ts-expect-error test fails if a default is ever added -- a bad agent gets no provider rather than an IP block. RIGHTS MACHINERY ON REAL RECORDS: the rights register is required with no default and noRightsBasisEstablished must be passed BY NAME, so a default composition against real Wikidata publishes NOTHING, asserted over three recorded real films -- that is the correct outcome, not a bug. P6216 is read and not interpreted. The pre-parse media-address scan now has an untrusted writer behind it, since a Wikidata label is editable by anyone logged in and a vandalised title reading like a manifest URL is an ordinary string to the schema; the test is built from a real response with one label replaced. A FINDING THE IMPLEMENTER GOT BY WRITING THE TEST AND GETTING THE OTHER ANSWER: a rights reference carrying a contract URL is refused as media_address_in_catalog_payload rather than as a non-opaque reference, because the address scan runs first. ROUND 44'S TRIPWIRE FIRED AND WAS INVERTED RATHER THAN DELETED: the licensed-source set is frozen, holds only wikidata, throws on runtime mutation and licenses no keyed source, and a tmdb request is refused by name. LIVE TESTS ARE OUT OF THE GATE: wikidata.live.test.ts hits the real service, is excluded by the package vitest config, reachable only through a separate script, and was run deliberately -- 3 passed in 20.4s -- but is not what this gate rests on."
        }
      },
      "implementationAgent": "claude-backend",
      "updatedAt": "2026-09-17T04:17:54.401Z",
      "implementationBaseSha": "a3e7d13323cda0479aa5352efb7bdb5dea973125",
      "surfaceWidenedAtClaim": "WIDENED 2026-09-16 by claude-lead to include package-lock.json, and FLAGGED TO THE REVIEWER AS OURS RATHER THAN PRESCRIBED, on exactly the precedent PL-0405 set and had approved this round. This task creates a new workspace package, packages/catalog-ingestion, and the root lockfile does not know it: npm ci fails with 'Missing: @liberty/catalog-ingestion@0.1.0 from lock file', and .github/workflows/ci.yml runs npm ci, so CI is red until the lockfile lands. A package that cannot be installed reproducibly is not a completed package, so the lockfile is part of adding one rather than a separate concern. The delta was computed with npm install --package-lock-only and is ADDITIONS ONLY, 28 lines, three entries: the workspace link, the package manifest, and its nested zod 3.25.76 -- which exists because root node_modules carries zod 4.4.3 as a transitive dependency of something else and every workspace declaring zod ^3.0.0 gets a nested 3.25.76. Nothing else in the file changed. The implementer stopped and reported rather than editing it, which was correct; this is the lead recording the widening instead of it being discovered in a red CI run. If the reviewer would rather see the lockfile as its own change, it is one entry to remove.",
      "licensingDecision": {
        "decidedBy": "human-commander",
        "decidedOn": "2026-09-17",
        "decision": "Wikidata is the initial catalog metadata source.",
        "rationale": "It requires no credential and keeps the provider seam replaceable.",
        "scope": "INITIAL SOURCE CHOICE, NOT AN EXCLUSIVE OR PERMANENT SOURCE MANDATE. Recorded in those words at the reviewer's insistence, so a later reader cannot mistake a first pick for a standard.",
        "transport": "Relayed by the commander in chat, inside gpt-architect's round-45 review text, and transcribed by claude-lead. Not machine-attested. Recorded as a human-commander decision rather than self-certified by the implementing agent, which is what control/policies.json requires: escalation.humanOnly lists Licensing, and selecting a catalog source is exactly that.",
        "notCoveredByThisDecision": [
          "Any credentialed source. TMDB and anything else requiring an API key remains a separate Credentials escalation and is untouched by this.",
          "Whether bulk ingestion engages the EU/UK sui generis database right. That question applies to Wikidata as much as to any other candidate and is for counsel, not for this record.",
          "Commercial use terms for any future source. Only Wikidata's CC0 position on structured data was evidenced."
        ],
        "obligationsThatFollow": "Wikidata's structured data is CC0, but access is not unconditional: the Wikimedia User-Agent policy requires an informative User-Agent with contact information or the client may be IP-blocked without notice, and browser-impersonating agents are forbidden. That is why CatalogDocumentOptions.userAgent is required with no default, and it must stay that way. Text outside the main, property and lexeme namespaces is CC BY-SA rather than CC0, so the distinction has to survive into whatever is ingested."
      }
    },
    {
      "id": "PL-0306",
      "priority": "P1",
      "lane": "Provider",
      "status": "REVIEW",
      "title": "Fixture provider states the protection fact it actually knows",
      "dependencies": [
        "PL-0902",
        "PL-0301"
      ],
      "allowedPaths": [
        "packages/provider-sdk/src/fixture/**"
      ],
      "reviewDependencies": [
        "packages/contracts/src/shared/drm.ts",
        "packages/contracts/src/domains/playback.ts"
      ],
      "preferredAgent": "claude-backend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit",
        "rights-review"
      ],
      "acceptance": "The fixture provider adapter emits contentProtection { state: \"clear\" } for its own candidates, because for a fixture the absence of DRM is a fact the adapter genuinely knows rather than something it is guessing. Created from gpt-architect's round-44 non-blocking finding on PL-0501, in the reviewer's own words: \"Create a provider-owned task for the fixture provider protection fact. The fixture adapter, not PL-0501, should emit { state: \\\"clear\\\" } when that fact is genuinely known. PL-0501 may keep PROTECTION_NOT_STATED as the conservative fallback until that provider task lands.\" So the deliverable is a narrowing of where the fact is asserted, not a behaviour change in routing: PROTECTION_NOT_STATED stays in PL-0501 as the fallback for every adapter that has not stated a protection fact, and this task removes the fixture from the set that needs it. Required, and each is a way this task could be done wrongly: the fixture must set the field itself rather than PL-0501 special-casing the fixture provider id, because a consumer branching on a provider identity is exactly the provider-specific logic outside @liberty/provider-sdk that invariant 3 forbids; requiresContentDecryptionModule must be observed returning false for a fixture candidate and the observation recorded, since state !== \"clear\" is the whole point of the encoding and an untested clear is indistinguishable from an untested unknown; the fallback path in PL-0501 must be left in place and a test must still cover an adapter that states nothing, because deleting the fallback once its only current user stops needing it would fail open for the next adapter; and no other adapter may be given a protection fact here, because for a real provider the honest value is unknown and asserting clear for one would be a rights misstatement rather than a defaulting decision. Out of scope: any change to contract shape (PL-0902 owns it), to ranking (@liberty/media-engine), or to capability routing.",
      "notes": "Provenance: gpt-architect CHANGES_REQUESTED on PL-0501, reviewed at 27d3a03389781256451c5e52d72520132cfa76c1, non-blocking/follow-up section. Filed as a task rather than actioned inside PL-0501 on the reviewer's explicit instruction that the fact belongs to the provider, not to the session API. Depends on PL-0902 because the field this task populates is defined there and PL-0902 is still in REVIEW; a fixture that sets a field whose contract is not yet approved would have to be rewritten if the contract moves.",
      "owner": "claude-backend",
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-09-18T23:16:08.970Z",
          "by": "claude-backend",
          "commitSha": "fd859bb05dbdef886bbc9bcc5aee35a7a148d390",
          "evidence": "npx turbo run typecheck --force at the integrated four-task head, exit 0, 10 of 10, 0 cached. The compile-time guard is part of the deliverable rather than incidental: type StremioStatesNoProtection = \"protection\" extends keyof MappedStream ? never : true stops compiling the moment anyone gives the Stremio adapter a protection fact, which is acceptance clause 4 -- no other adapter may assert one, because for a real provider the honest value is unknown."
        },
        "unit": {
          "status": "pass",
          "at": "2026-09-18T23:16:09.062Z",
          "by": "claude-backend",
          "commitSha": "fd859bb05dbdef886bbc9bcc5aee35a7a148d390",
          "evidence": "npx turbo run test --force at the integrated head, exit 0, 17 of 17, 0 cached, 2409 passed across the wave (base 2332). Package-level: 8 files, 223 passed in provider-sdk. RED-THEN-GREEN, test written before the implementation: npx vitest run src/fixture/provider.test.ts --root packages/provider-sdk exit 1, 1 failed 24 passed, 'AssertionError: expected undefined to deeply equal { state: \"clear\" }' at provider.test.ts:401; after the change, 25 passed. CLAUSE 2 OBSERVED RATHER THAN ASSUMED: over each of the three real resolve().mapped entries the test asserts requiresContentDecryptionModule(entry.protection) === false, protectionKeySystem null, describeContentProtection 'clear', and contentProtectionSchema.safeParse success -- an untested clear is indistinguishable from an untested unknown. CLAUSE 3, the fallback is intact and still covered: apps/web was not written to, and its two existing tests pinning PROTECTION_NOT_STATED still pass unmodified (2 files, 87 passed), plus new in-package coverage of the stating-nothing case. CLAUSE 1: no consumer special-cases the provider id; authorized-candidates.ts still contains no comparison against 'fixture'. Note the contract field is spelled 'protection' on resolvedStreamCandidateSchema, not 'contentProtection'; the adapter uses the contract's spelling with the specified { state: 'clear' } value."
        }
      },
      "implementationAgent": "claude-backend",
      "updatedAt": "2026-09-18T23:16:09.715Z",
      "implementationBaseSha": "fd859bb05dbdef886bbc9bcc5aee35a7a148d390"
    }
  ],
  "review": [],
  "reservedExternal": [],
  "recommended": [
    {
      "id": "PL-0402",
      "priority": "P0",
      "lane": "Backend",
      "status": "READY",
      "title": "Profile model",
      "dependencies": [
        "PL-0405"
      ],
      "allowedPaths": [
        "apps/web/src/**",
        "packages/contracts/**",
        "packages/**"
      ],
      "preferredAgent": "claude-backend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit",
        "integration",
        "security-review"
      ],
      "acceptance": "Profiles are modelled above authentication rather than inside the auth library, with the active profile carried alongside the session rather than inside the identity record; profile scoping is present in the first migration rather than retrofitted, because that is the part that is expensive to reverse; profiles can be created and selected with authorization tests",
      "owner": null,
      "gateResults": {}
    },
    {
      "id": "PL-0303",
      "priority": "P1",
      "lane": "Provider",
      "status": "READY",
      "title": "Provider health contract",
      "dependencies": [
        "PL-0301"
      ],
      "allowedPaths": [
        "packages/provider-sdk/**",
        "packages/contracts/**",
        "packages/observability/**"
      ],
      "preferredAgent": "claude-backend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit"
      ],
      "acceptance": "A provider with zero observations reports unknown with a null observed success rate and a zero sample count, never a mid-range score and never a pass; any prior used to rank an unobserved provider is labelled as a prior rather than presented as measured availability; degraded-but-usable is a state distinct from up and down; health output is pure over the observations plus an explicit policy version, with no hidden wall-clock decay; and health never affects entitlement, so a provider at perfect health still cannot make an unauthorized candidate eligible",
      "owner": null,
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-08-20T16:02:03.512Z",
          "evidence": "turbo run typecheck --filter @liberty/provider-sdk exit 0 under pinned Node 22 resolved via npx node@22."
        },
        "unit": {
          "status": "pass",
          "at": "2026-08-20T16:02:03.607Z",
          "evidence": "turbo run test --filter @liberty/provider-sdk exit 0 under pinned Node 22: zero observations yielding unknown with a null observed rate and a zero sample count, a prior labelled rather than presented as measurement at the same ranking number as a measured value, the degraded band held distinct from up and down, purity over observations plus an explicit policy version with Date.now replaced by a thrower to prove no wall clock is read, whole-report permutation invariance under generated policies, monotonicity in each count, and a compile-time guard that no health type can carry entitlement."
        }
      }
    }
  ]
}

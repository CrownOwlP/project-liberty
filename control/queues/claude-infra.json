{
  "generatedAt": "2026-09-23T04:33:52.261Z",
  "agent": "claude-infra",
  "executionAvailable": true,
  "assigned": [],
  "review": [],
  "reservedExternal": [],
  "recommended": [
    {
      "id": "PW-0102",
      "priority": "P0",
      "lane": "Infra",
      "status": "READY",
      "title": "A Tauri v2 shell that owns the sidecar's lifetime",
      "dependencies": [
        "PW-0101"
      ],
      "allowedPaths": [
        "apps/desktop/**"
      ],
      "reviewDependencies": [
        "apps/web/next.config.ts",
        "apps/web/package.json",
        "turbo.json",
        "package.json",
        "docs/DESKTOP_PLAYBACK.md",
        "docs/ARCHITECTURE.md"
      ],
      "preferredAgent": "claude-infra",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit",
        "build",
        "architecture-review",
        "security-review"
      ],
      "acceptance": "A Tauri v2 application starts, spawns the PW-0101 sidecar, reads its bound port from stdout, points the webview at it, and cannot leave the sidecar running. REQUIRED: the Tauri version is pinned exactly, on the rule this repository already applies to Shaka and Playwright -- a caret range means the shell a gate was recorded against can change under a lockfile refresh. REQUIRED: the sidecar is packaged as node.exe plus the standalone tree under bundle.resources and spawned as a child process; pkg, Node SEA and Bun --compile are documented as unable to swallow a Next standalone server and must not be attempted as a shortcut. REQUIRED: a Win32 Job Object with JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE, because Tauri provides no supervision and an orphaned node.exe holding a loopback port is the failure a user sees as 'it will not start the second time'. REQUIRED: supervision -- health check, restart with a bounded attempt budget, and an honest surfaced failure when the budget is spent, never a blank window. REQUIRED: the shell passes the bearer token to the sidecar through the environment and never on a command line, where it would be readable from the process list. STATED LIMITATION TO CARRY IN THE GATE EVIDENCE: no Windows binary can be produced or run from the cloud session, so this task's build gate is cargo check plus a windows-latest CI job, and 'it launches' is real-device evidence owed to PW-0601.",
      "notes": "PROPOSED FOR gpt-architect RULING, round 80. Created unowned as part of the WINDOWS PRODUCT COMPLETION phase board so the proposal is machine-readable rather than prose, and NOT claimed before the verdict. SURFACE NOTE: this task's allowedPaths name directories that do not exist yet. The evidence-based narrowing procedure applies to reconciling work already in history; for genuinely new work the surface is the smallest set of paths the acceptance can be satisfied within, and it is narrowed again from the diff before review if it turns out to be wider than what was written. ",
      "owner": null,
      "gateResults": {}
    },
    {
      "id": "PW-0104",
      "priority": "P1",
      "lane": "Infra",
      "status": "READY",
      "title": "A killed desktop dev server cannot leave a tracked file rewritten",
      "dependencies": [],
      "allowedPaths": [
        "apps/web/package.json",
        "apps/web/next-env.d.ts",
        ".gitignore",
        "scripts/desktop-target.mjs",
        "scripts/test-desktop-target.mjs"
      ],
      "reviewDependencies": [
        "apps/web/src/app/api/v1/playback/build-target.ts",
        "apps/web/next.config.ts",
        "e2e/playwright.config.ts",
        "docs/DEVELOPMENT.md"
      ],
      "preferredAgent": "claude-infra",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit",
        "build"
      ],
      "acceptance": "A desktop build or dev server cannot leave apps/web/next-env.d.ts rewritten in the working tree, under ANY termination. THE DEFECT, OBSERVED RATHER THAN PREDICTED, in round 81: the file was committed pointing at ./dist/desktop/dev/types/routes.d.ts instead of ./.next/types/routes.d.ts. Next regenerates that TRACKED file to name whichever distDir last built; the three *:desktop scripts snapshot and restore it around the child; and build-target.ts:300-342 already records that the restore happens ONLY ON NORMAL EXIT. Playwright signal-kills `next dev` when the harness finishes, so the rewrite survived, was staged by a routine `git add -A`, and was caught only by reading the diff before delivery. It typechecked locally because dist/desktop/dev happened to exist; on a fresh clone it would not have. REQUIRED: a forced termination -- SIGTERM, SIGINT, SIGKILL of the child, and the parent being killed -- cannot leave the tracked file dirty. REQUIRED: THE SOLUTION MUST NOT DEPEND ON A NORMAL-EXIT CLEANUP HOOK ALONE. A second `process.on('exit')` handler is the same mechanism that already failed and would fail the same way under SIGKILL; the implementer must choose a design that is correct when no cleanup runs at all, and justify it. Two candidates, neither mandated: make the file untracked and generated (which changes what a fresh clone must do, and docs/DEVELOPMENT.md must then say so), or make the desktop distDir's type path stable so no rewrite occurs. REQUIRED: the regression is MECHANICALLY TESTED -- a test that starts a desktop invocation, kills it without letting it clean up, and fails if the tracked file differs from HEAD. A test that only exercises normal exit reproduces nothing, because normal exit already works. REQUIRED: the fix does not weaken the build-target split; build-target.test.ts must keep passing, including its assertion that no shipped module under apps/web/src names LIBERTY_BUILD_TARGET. NOT IN SCOPE: anything about product behaviour. This is a tooling defect and gpt-architect's ruling is explicit that it must not be folded into unrelated product work.",
      "notes": "Filed round 82 on gpt-architect's ruling item 4, which refused to let this stay a warning in a handoff. The scripts directory is named in allowedPaths because the three `node -e` wrappers inside apps/web/package.json are the thing that would move: JSON cannot carry a comment, which is why their reasoning currently lives in build-target.ts, and a real script file is the likeliest shape of the fix. It is not mandated -- the implementer chooses and justifies.",
      "owner": null,
      "gateResults": {}
    }
  ]
}

{
  "generatedAt": "2026-09-15T17:52:51.067Z",
  "agent": "gpt-architect",
  "executionAvailable": false,
  "assigned": [],
  "review": [
    {
      "id": "PL-0301",
      "priority": "P0",
      "lane": "Provider",
      "status": "REVIEW",
      "title": "Authorized fixture provider",
      "dependencies": [
        "PL-0207"
      ],
      "notes": "DEPENDENCY REPOINTED 2026-09-15 from PL-0205 to PL-0207 by claude-lead. This is a task-definition change, not a state edit, and it is recorded as a control-plane event rather than performed silently. PL-0205 is BLOCKED and can never reach DONE: its own record states that it is preserved as audit history and superseded, because its implementationBaseSha is false and the rule PL-0703 established refuses in-place repair -- reconciliation will not revise an existing base, release is unreachable from REVIEW and preserves the base regardless, and hand-editing a published provenance figure is the move PL-0703 was blocked for. PL-0207 is that successor, it is DONE, and it carries the same unknown-media-metadata semantics this task actually depends on: the required-and-nullable candidate fields, unknownMediaFacts and compatibilityConfidence that a fixture provider must normalize against. So the dependency was pointing at the audit record of the work rather than at the work. With requireAllDependenciesDone true and no CANCELED transition available from the CLI, leaving the pointer on PL-0205 gated PL-0301, and through it PL-0302, PL-0501, PL-0502, PL-0701 and PL-0702 -- the entire M4 playback vertical slice -- behind a task whose only possible terminal state is BLOCKED. The three other superseded tasks, PL-0401, PL-0601 and PL-0703, have no dependents and are deliberately left BLOCKED as the audit history their notes say they are.",
      "allowedPaths": [
        "packages/provider-sdk/src/fixture/**",
        "packages/provider-sdk/src/provider.ts",
        "packages/provider-sdk/src/registry.ts",
        "packages/provider-sdk/src/index.ts",
        "packages/provider-sdk/src/stremio/**",
        "packages/provider-sdk/package.json"
      ],
      "preferredAgent": "claude-backend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit",
        "security-review",
        "rights-review"
      ],
      "acceptance": "Server-side fixture adapter returns normalized authorized candidates only",
      "owner": "claude-backend",
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-09-15T17:35:04.063Z",
          "by": "claude-backend",
          "commitSha": "80e3d3e3e0130571a1a0791b496a9e2cce0c6c7a",
          "evidence": "npx turbo run typecheck --force at 80e3d3e3e013a8e8a1d32bbb8f8a5ba5c0d0a4f2, exit 0, 9 of 9 tasks and 0 cached; and again scoped with --filter=@liberty/provider-sdk --force, exit 0, 2 of 2 and 0 cached, so the reviewed package is proven rather than inferred from a whole-repo pass. Forced in both runs: a cached typecheck proves a previous tree."
        },
        "unit": {
          "status": "pass",
          "at": "2026-09-15T17:35:04.273Z",
          "by": "claude-backend",
          "commitSha": "80e3d3e3e0130571a1a0791b496a9e2cce0c6c7a",
          "evidence": "npx turbo run test --force --filter=@liberty/provider-sdk, exit 0, 8 test files and 213 tests passed, 0 cached. Whole-repo npx turbo run test --force at the same tree, exit 0, 15 of 15 tasks, 2096 passed 1 skipped 0 failed. The 213 are this task's own regressions: fixture/provider.test.ts carries 24 cases across five describe blocks -- three witness forgeries (cast, spread copy, and a structural literal as @ts-expect-error), the opaque-rights-reference rule, five construction refusals, nine resolution properties, and the registry port -- and the Stremio half adds client.test.ts, mapping.test.ts, mapping.property.test.ts, source.test.ts and url-policy.test.ts, which is where the rights-basis declaration and the URL policy the fixture gate depends on are exercised. NOT EXECUTED AND THEREFORE NOT CLAIMED: no database, no network and no real provider were involved; these are the package's own suites against fixtures, which is what this task's acceptance asks for and no more."
        }
      },
      "reviewDependencies": [
        "packages/contracts/src/domains/playback.ts",
        "packages/contracts/src/shared/media-facts.ts",
        "packages/contracts/src/shared/rights.ts",
        "packages/contracts/src/shared/runtime.ts",
        "packages/contracts/src/shared/ids.ts",
        "packages/contracts/src/shared/codecs.ts",
        "apps/web/src/app/api/v1/playback/session/authorized-candidates.ts",
        "e2e/tests/playback-session.api.spec.ts"
      ],
      "surfaceNarrowedAtClaim": "NARROWED 2026-09-15 by claude-lead in the same edit that claimed the task, on the precedent PL-0205 recorded and PL-0204 reasoned: a package-wide reservation for work confined to part of a package holds unrelated lanes, because pathsOverlap tests prefix containment in BOTH directions. WHAT WAS REMOVED AND WHY. packages/contracts/** was a READ DECLARED AS A WRITE. Every contracts symbol the provider SDK touches is imported and already exists -- StreamCandidate and unknownMediaFacts from domains/playback.ts, MediaFact and MEDIA_FACTS from shared/media-facts.ts, ContentRights and PLAYABLE_CONTENT_RIGHTS from shared/rights.ts, ClassifiedRuntime from shared/runtime.ts, normalizedContentIdSchema from shared/ids.ts, the codec unions from shared/codecs.ts -- and none of it has to change for this acceptance. The rights-basis vocabulary this task's gate rests on is not in contracts at all: RightsBasis, RIGHTS_BASES_FOR_RIGHTS and describeRightsBasis are declared in packages/provider-sdk/src/stremio/source.ts. Of the five commits naming PL-0301, none touched packages/contracts except 4091a2b, a six-lane preflight whose contracts changes belong to the PL-0204, PL-0205, PL-0102 and PL-0103 lanes named in its own subject. The one contract change somebody might reach for, putting uri and mimeType on StreamCandidate, is argued AGAINST at fixture/provider.ts line 138. So the six leaves are now reviewDependencies: read, fingerprinted, not reserved. apps/web/src/app/api/** was a reservation of the surface this task exists to unblock. Its only consumer, apps/web/src/app/api/v1/playback/session/authorized-candidates.ts, says in its own header that it belongs to PL-0501 and that it no longer implements the fixtures but configures the SDK's; it is declared read-only here instead. The entry normalizes to the prefix apps/web/src/app/api, which is declared identically by PL-0702 and PL-0302 and contained by PL-0701's apps/web/** and containing PL-0501's apps/web/src/app/api/v1/playback/** -- so keeping it would serialize PL-0301 against four downstream tasks, including two it gates. apps/web/src/app/api/deployment-environment.ts records the distortion in the code: it sits under app/api 'because that was the only directory every consumer could reach inside PL-0301's allowedPaths when it was written.' WHAT WAS KEPT, AND WHY NOT packages/provider-sdk/**. src/health.ts and its two suites are PL-0303's, created by 67c5fc9, and PL-0303 already has typecheck and unit recorded; the fixture provider CONSUMES evaluateProviderHealth, and consuming is not owning. tsconfig.json is bootstrap and untouched by any PL-0301 commit -- declaring an untouched file pads a review range exactly as omitting a touched one narrows it. src/index.ts is kept and is NOT the PL-0205 barrel case that was excluded: that barrel was re-export-only with an export * already covering the change, whereas this one's CONTENT is load-bearing -- fixture/environment.ts names 'NonProductionRuntime is not re-exported by ../index.ts' as one of the three mechanisms its rights witness rests on, and a rights argument that turns on an omission from a file cannot leave that file out of the reviewed surface. TWO ENTRIES FLAGGED FOR THE REVIEWER RATHER THAN PRESENTED AS SETTLED. First, packages/provider-sdk/src/stremio/**. The acceptance sentence names only the fixture adapter, but all thirteen files carry PL-0301 headers, all were created by commits naming PL-0301, coordination/IMPLEMENTATION_RECOVERY.md lists 'the fixture provider and the Stremio adapter' under this task, stremio/client.test.ts line 104 says 'PL-0301's acceptance criterion, end to end', and the coupling is structural rather than historical: fixture/rights.ts imports the rights-basis table from stremio/source.ts and fixture/provider.ts imports checkUrl from stremio/url-policy.ts. If the reviewer rules fixture-only, two things follow and neither is hidden: the base moves to f6c4b942ebbd02fd3fa9ed8f74fde4fc603affc2, the parent of 719d2d7, and those two stremio files must become reviewDependencies -- and the Stremio adapter is then owned by no task at all, which is the worse end state but is the reviewer's call and not this lead's. Second, packages/provider-sdk/package.json: genuinely edited by c0ebf6b, which added zod and the test script, and by 79a0e65. It is the entry to drop first under challenge, flagged in the same spirit PL-0405 flagged package-lock.json. NOT DECLARED, DELIBERATELY: packages/contracts/src/testing/arbitraries.ts. stremio/mapping.property.test.ts does import from it, so there is an argument for listing it read-only. Against: PL-0206 is editing that file right now, and the arbitraries this task consumes -- audioCodecArb, contentRightsArb, defined, healthScoreArb, permutationKeysArb, permute, unvettedRightsArb, videoCodecArb -- are disjoint from the languageTagArb region PL-0206 is changing. Declaring it would bind this approval's freshness to churn in a region the task does not read. It costs nothing in dispatch either way, since reviewDependencies are not in pathsOverlap; it is a fingerprint-hygiene call and it is flagged rather than buried. DISJOINTNESS, CHECKED IN BOTH DIRECTIONS against every currently active task: PL-0206 (packages/media-engine, three packages/contracts leaves), PL-0405 (packages/auth, packages/persistence/src, the identity migration, package-lock.json, two docs), PL-0704 (apps/web app routes, one e2e spec, docs/E2E.md, .github/workflows/ci.yml), PL-AI-0007 (two scripts, control/README.md) and PL-0901 (two docs). No proposed prefix equals, prefixes, or is prefixed by any of theirs. Note package-lock.json versus packages/provider-sdk/package.json: neither startsWith the other plus a separator, so they do not collide.",
      "implementationAgent": "claude-backend",
      "updatedAt": "2026-09-15T17:35:04.476Z",
      "implementationBaseSha": "fb217b96add37ed41e5c7bf1a35e693c39b5138b",
      "implementationBaseProvenance": {
        "kind": "reconciled-existing-implementation",
        "baseSha": "fb217b96add37ed41e5c7bf1a35e693c39b5138b",
        "reconciledAt": "2026-09-15T17:34:32.292Z",
        "reconciledBy": "claude-backend",
        "implementationAgent": "claude-backend",
        "headAtReconciliation": "80e3d3e3e0130571a1a0791b496a9e2cce0c6c7a",
        "reviewSurface": "allowedPaths + reviewDependencies",
        "surfaceCommitCount": 17,
        "oldestSurfaceCommit": "c0ebf6b0f9ef237dcfc9eebda31befc135bbf037",
        "newestSurfaceCommit": "a4209a4fcf0b232e5de92f2af09dd8fb3d266909",
        "surfaceCommits": [
          "a4209a4fcf0b232e5de92f2af09dd8fb3d266909",
          "b74b0d3c4befa226109fc1ce623b5addd46a9490",
          "eecca6b621a2fccaab09ab3ec15d3bc93936df3b",
          "ba9521b2c3998ae05210240827c0a773596819c9",
          "719d2d7c31465901f3dbee1e936a180e87c3d44f",
          "754a786625499ae7e436f93758edd11ef7487ff4",
          "9933a55dd177ccc08b20b1fe3e49f479c8a7de1f",
          "6c31298ba1eece125b2733780dc6b107aa5e5200",
          "bbe68ed8d16f864c87309ceb1c089495deb89766",
          "ba2bf4756175e84aab07ec36f9e7c3d6e324d350",
          "4ca4313f2642f4666ab6dda0084c25fcb2fbf3d5",
          "67c5fc9270621f461cbf671fe870da7394d20065",
          "79a0e651bd06068d785b54dfd9080245d7b9280b",
          "f06dec1b21925bf1af8c676bc7e24bc4ecf3d002",
          "4091a2b65b8f187ccb87a04790272007dabd39ea",
          "f711522205cc9a4b03ecb52bc55224d6466338a9",
          "c0ebf6b0f9ef237dcfc9eebda31befc135bbf037"
        ],
        "surfaceCommitsTruncated": false,
        "changedFileCount": 30,
        "baseCommitSurfaceTouchCount": 0,
        "baseCommitSurfaceTouches": [],
        "baseCommitSurfaceTouchesTruncated": false,
        "reason": "The base is fb217b96add37ed41e5c7bf1a35e693c39b5138b, the first parent of c0ebf6b, the earliest commit that names PL-0301. It is proven against the TREE rather than against filenames or commit messages, because this project has lost four tasks to bases that passed every mechanical check and were still false. At fb217b9, git ls-tree -r packages/provider-sdk returns exactly three paths -- package.json, src/index.ts and tsconfig.json -- so no adapter of any kind exists in the parent tree, fixture or Stremio, and the BEHAVIOUR is absent rather than merely the files. That is the PL-0205 standard: that failure came from a witness file created by a later refactor that only MOVED declarations, so the parent tree already held the semantics. Nothing can have been moved into a package whose entire content is a bootstrap barrel and a tsconfig. Probes used were stremio/source.ts and stremio/mapping.ts, which are genuinely new at c0ebf6b; src/provider.ts was deliberately NOT used as a probe, because git show fb217b9:packages/provider-sdk/src/index.ts shows the AuthorizedMediaProvider interface and the allowlist assertion already present in the barrel and merely relocated into that file, which is the same trap in a second place. src/index.ts and package.json were likewise not used, since both exist at the bootstrap commit b484735. The base commit itself changes nothing under the reviewed surface. THE IMPLEMENTATION PREDATES THE CLAIM BY DESIGN, NOT BY OVERSIGHT: coordination/IMPLEMENTATION_RECOVERY.md lists this task in the table headed Implemented, unreviewed, the record carried owner null with no gate results and no base, and the five fixture files were created at 719d2d7 while the task sat BACKLOG. ONE CLASSIFICATION IS LOAD-BEARING AND IS PUBLISHED RATHER THAN BURIED: this base covers the Stremio adapter as well as the fixture adapter, because every file under packages/provider-sdk/src/stremio carries a PL-0301 header, all were created by commits naming PL-0301, IMPLEMENTATION_RECOVERY names both, client.test.ts line 104 says PL-0301s acceptance criterion end to end, and fixture/rights.ts and fixture/provider.ts import the rights-basis table and checkUrl from that directory, so the fixture gate cannot be reviewed without it. If the reviewer rules the surface is fixture-only, this base is wrong and the correct one is f6c4b942ebbd02fd3fa9ed8f74fde4fc603affc2, the parent of 719d2d7 -- and the Stremio adapter is then owned by no task at all."
      }
    },
    {
      "id": "PL-0704",
      "priority": "P0",
      "lane": "Frontend",
      "status": "REVIEW",
      "title": "notFound() never reaches the wire: dead addresses answer 200",
      "notes": "IMPLEMENTED IN THE TREE AND NEVER CLAIMED, like the rest of recovery packet 1 -- but PARTIALLY, and the two gaps are different in kind. This is the fifth task found in that state, and its code landed while it sat BACKLOG and unowned: control/events.jsonl records apps/web/src/app/loading.tsx and page.tsx being edited as belonging to PL-0704 while it was blocked on a lifecycle dependency. So it must be started with --reconcile-existing, never against HEAD. IT IS A MODIFICATION TASK IN THE REVIEWER'S TAXONOMY, MOSTLY, and that changes the probe. Its central act was a DELETION -- the root loading.tsx removed -- and marker absence cannot witness a deletion. What it also did, and what CAN be witnessed, is introduce two named components where anonymous route-level skeletons used to be: CatalogSkeleton in app/page.tsx and PlaybackLoading in app/watch/[contentId]/page.tsx, each now a Suspense fallback sitting BELOW the existence decision rather than a loading file wrapping the whole segment. Those two names are new even though their markup is not, so a pickaxe on the names finds the relocation, which is precisely this task's work. Use them as the witnesses; do not use the deleted file. TWO GAPS AGAINST THE ACCEPTANCE, AND ONLY ONE IS A CODE GAP. FIRST, THE TITLE SKELETON, WHICH I BELIEVE IS A CONTRACT GAP RATHER THAN A DEFECT AND AM FLAGGING FOR A RULING RATHER THAN FIXING. The acceptance says any fix must KEEP the loading skeletons rather than deleting them to move a status code. Home and watch comply: both relocated their skeleton into a Suspense boundary below a cheap existence check that consults nobody. The title route did not -- it deleted its skeleton outright -- and its own header argues why: on that route a status line precedes the first body byte, so no byte may be sent before the catalog answers whether the title exists, which is the definition of having nothing to stream. A full-page skeleton there IS the defect the task exists to remove. The only honest way to restore one would be a section whose data does not depend on the title existing, and none exists today. So the code looks right and the clause looks over-general. I am not rewriting the acceptance to match the code unasked -- that is the move the reviewer has twice had to authorise explicitly, on PL-0205's wording and on PL-0706's -- so it is proposed here and left for the verdict. SECOND, THE PRODUCTION-MODE 404, WHICH IS NOT THIS TASK'S TO CLOSE. An unknown title answers 404 under development and 200 with a refusal panel under production, and that is correct rather than broken: on a hosted build no catalog metadata source is constructible, so loadTitleDetail reports error rather than not-found, notFound() is unreachable, and a build that answered 404 there would be claiming an absence it never looked for. The precondition is a catalog source that exists in a deployment, which is PL-0305's, now READY since PL-0105 completed. ALLOWEDPATHS CORRECTED IN BOTH DIRECTIONS. apps/web/src/app/(home)/** is DROPPED: it is a dead declaration for a route-group approach that was considered and rejected, it reserves a directory that does not exist, and it starves nothing only by accident. apps/web/src/app/loading.tsx is KEPT even though the file is deleted, because the deletion is the change and a surface that omitted it would not fingerprint the task's central act. TWO PATHS ADDED. docs/E2E.md, because it and .github/workflows/ci.yml currently CONTRADICT EACH OTHER about whether the two 404 assertions have ever been observed passing -- E2E.md claims an executed 2026-09-05 pass on all four browser projects, ci.yml says UNKNOWN, expected to pass, never observed passing -- and settling that is exactly what this task's EXECUTED clause is about. It is declared by PL-0706, which is DONE, and by PL-0703, which is BLOCKED, so no active conflict. And .github/workflows/ci.yml, which UNTIL NOW WAS DECLARED BY NO TASK IN THE REPOSITORY AT ALL: every CI change this project has made has landed outside every declared surface. That is a real declaration hole rather than a convenience, and this task adopting it is a partial fix at best -- the file is repository governance rather than frontend, and it should eventually belong to whatever task adopts CLAUDE.md and control/README.md, which are undeclared for the same reason.",
      "dependencies": [
        "PL-0103"
      ],
      "allowedPaths": [
        "apps/web/src/app/loading.tsx",
        "apps/web/src/app/page.tsx",
        "apps/web/src/app/title/**",
        "apps/web/src/app/watch/**",
        "e2e/tests/critical-journey.spec.ts",
        "docs/E2E.md",
        ".github/workflows/ci.yml"
      ],
      "preferredAgent": "claude-frontend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit",
        "e2e"
      ],
      "acceptance": "An unknown title id and a malformed watch id each answer HTTP 404 rather than a skeleton served at 200, proven by an EXECUTED Playwright run rather than by a route calling notFound(). The cause is that a segment's loading file wraps that segment in Suspense, so React flushes the shell at 200 before the access-fallback error escapes the render and Next can set the status; the root loading file wraps every route in the application, so per-route fixes alone are insufficient. Any fix must keep the loading skeletons rather than deleting them to move a status code. DEPENDS ON PL-0103 because the title route is that task's declared write surface and it is currently in REVIEW; this is a lifecycle dependency, not a technical one.",
      "owner": "claude-frontend",
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-09-15T05:35:49.522Z",
          "by": "claude-frontend",
          "commitSha": "0337663aa9cdf1572298492b5f49e37e48f6e0a2",
          "evidence": "npx turbo run typecheck --force at 0337663aa9cdf1572298492b5f49e37e48f6e0a2, exit 0, 9 of 9 tasks successful and 0 cached, 28.8s, run by the lead in the integrated checkout after PL-0704 and PL-0405 were merged. Forced rather than cached deliberately: a cached typecheck proves a previous tree. The same command was run independently in the PL-0704 worktree at 82a50dc, also exit 0. One earlier run in that worktree exited 2 on packages/auth/src/better-auth.ts with TS2307 Cannot find module better-auth; that was an incomplete container install rather than a tree defect -- packages/auth/node_modules was absent -- and a plain npm ci fixed it. It is reported rather than dropped because an exit code that was observed is evidence even when it is superseded."
        },
        "unit": {
          "status": "pass",
          "at": "2026-09-15T05:35:49.733Z",
          "by": "claude-frontend",
          "commitSha": "0337663aa9cdf1572298492b5f49e37e48f6e0a2",
          "evidence": "npx turbo run test --force at 0337663aa9cdf1572298492b5f49e37e48f6e0a2, exit 0, 15 of 15 tasks successful and 0 cached: web 745, media-engine 369 passed with 1 skipped, persistence 260, provider-sdk 213, media-inspection 191, contracts 138, observability 68, auth 65, recommendations 47 -- 2096 passed, 1 skipped, 0 failed. Run against a tree installed by npm ci from the committed lockfile, exit 0. PL-0704's own additions in this count are a fourth case in apps/web/src/app/watch/route-loading-boundaries.test.ts asserting that the two relocated skeletons still exist -- that CatalogSkeleton is still defined in app/page.tsx and still rendered as a Suspense fallback, and the same for PlaybackLoading in app/watch/[contentId]/page.tsx. It was confirmed non-vacuous by temporarily swapping the home fallback to null, which produced the intended named failure, and the pre-existing guard in the same file was confirmed non-vacuous by recreating a trivial root loading.tsx, which produced two named violations."
        },
        "e2e": {
          "status": "pass",
          "at": "2026-09-15T05:36:06.291Z",
          "by": "claude-frontend",
          "commitSha": "0337663aa9cdf1572298492b5f49e37e48f6e0a2",
          "evidence": "EXECUTED Playwright run, not a prediction, which is the clause this task exists to satisfy. From e2e/ after npm ci in that directory, exit 0. Development: LIBERTY_E2E_WEB_MODE=development npx playwright test --project=api --project=chromium at 82a50dc, exit 0, 43 passed 3 skipped 0 failed. Production, the default: npx playwright test --project=api --project=chromium at 82a50dc, exit 0, 34 passed 12 skipped 0 failed. Specs: catalog.api, playback-session.api, progress.api, rights-boundary.api, critical-journey, search, media-rig skipped for no rig. The asserted statuses: critical-journey.spec.ts:448 for /title/no-such-title-pl0701 expects 404 under development and passed, and expects 200 with catalog_source_not_configured under production and passed; critical-journey.spec.ts:750 for /watch/Not%20A%20Valid%20Id expects 404 and passed in BOTH modes; critical-journey.spec.ts:814, the new skeleton-retention case, passed in both modes. Served bytes carried no Loading title and no Loading player on the 404 cases. Second independent witness, not the spec reading itself: the dev server request log recorded GET /title/no-such-title-pl0701 404 in 140ms and GET /watch/Not%20A%20Valid%20Id 404 in 71ms, and a separate curl probe of an independently started next dev returned 404, 404, 200, 200, 200 for the unknown title, malformed watch id, real title, real watch id and root. ONE NARROWING OF THIS EVIDENCE, STATED PLAINLY RATHER THAN OMITTED: the browser under test was NOT the pinned revision. The container ships Chromium revision 1194, Chromium 141.0.7390.37, while @playwright/test 1.62.1 requires 1234, and the first attempt failed verbatim with browserType.launch: Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell. playwright install was unavailable. Rather than edit e2e/playwright.config.ts, which is outside this task's allowedPaths, a symlink registry presenting the 1194 build under the 1234 names was built in a scratch directory and selected with PLAYWRIGHT_BROWSERS_PATH; nothing in the repository changed for it. That is exactly the drift the pinning comment in playwright.config.ts exists to prevent, and it is recorded in docs/E2E.md as well as here. Only the api and chromium projects ran -- the pair CI runs -- so this says nothing about WebKit, mobile-safari or Firefox, and PL-0705's WebKit clause is untouched. No claim is made that the CI job will be green: a container run is not that job, and asserting otherwise would be the same class of claim as the expected to pass comments this task deleted."
        }
      },
      "implementationAgent": "claude-frontend",
      "updatedAt": "2026-09-15T05:36:47.246Z",
      "implementationBaseSha": "f6c4b942ebbd02fd3fa9ed8f74fde4fc603affc2",
      "implementationBaseProvenance": {
        "kind": "reconciled-existing-implementation",
        "baseSha": "f6c4b942ebbd02fd3fa9ed8f74fde4fc603affc2",
        "reconciledAt": "2026-09-15T05:35:31.110Z",
        "reconciledBy": "claude-frontend",
        "implementationAgent": "claude-frontend",
        "headAtReconciliation": "0337663aa9cdf1572298492b5f49e37e48f6e0a2",
        "reviewSurface": "allowedPaths",
        "surfaceCommitCount": 7,
        "oldestSurfaceCommit": "719d2d7c31465901f3dbee1e936a180e87c3d44f",
        "newestSurfaceCommit": "0337663aa9cdf1572298492b5f49e37e48f6e0a2",
        "surfaceCommits": [
          "0337663aa9cdf1572298492b5f49e37e48f6e0a2",
          "82a50dcdbe90d8b1e596d71886dc75fbf1458431",
          "b74b0d3c4befa226109fc1ce623b5addd46a9490",
          "eecca6b621a2fccaab09ab3ec15d3bc93936df3b",
          "ba9521b2c3998ae05210240827c0a773596819c9",
          "c47e1bba1890e8cdc911b8b1db19abfeff81d7be",
          "719d2d7c31465901f3dbee1e936a180e87c3d44f"
        ],
        "surfaceCommitsTruncated": false,
        "changedFileCount": 18,
        "baseCommitSurfaceTouchCount": 0,
        "baseCommitSurfaceTouches": [],
        "baseCommitSurfaceTouchesTruncated": false,
        "reason": "The base is f6c4b942ebbd02fd3fa9ed8f74fde4fc603affc2, the first parent of 719d2d7c31465901f3dbee1e936a180e87c3d44f, derived from history rather than named: 719d2d7 is the one commit that deletes all three loading files, retires HomeLoading and WatchLoading, and introduces CatalogSkeleton and PlaybackLoading, and 'git grep <Suspense> f6c4b94 -- apps/web/src/app' returns only a prose comment, so not one Suspense boundary of this task's arrangement existed in the base tree under any name or in any file. Because the two named witnesses are relocations and could have carried the PL-0205 risk, they are backed by that structural absence, by the paired disappearance of HomeLoading and WatchLoading in the same commit, and by route-loading-boundaries.test.ts being a genuine add there -- the only renames in this repository are PL-AI-0006's four packages/contracts moves, outside this surface. The whole declared surface was swept rename-aware with --full-history plus --follow on the five leaf paths and a conservative 13-file enumeration of title/** and watch/**, reconstructing the deleted apps/web/src/app/loading.tsx deliberately; that yields exactly 23 pre-base commits, every one pre-existing work: c9efc49, 5399c92 and 4091a2b build the three loading files that are the defect, ba2bf47 writes the two 404 assertions as PL-0701's acceptance harness twelve days before this task existed, 8b1172b is the first executed run, which created PL-0704 in control/tasks.json and states in the code that the fix is not done here, and c469e92 adds the comment DO NOT DELETE THIS TO RECOVER THE 404 ... The status is PL-0704's to fix, in the root file. The two document paths were proven separately because that is what sank PL-0601 and PL-0401: .github/workflows/ci.yml contains no 404, loading, skeleton or PL-0704 text whatsoever at the base and gains the entire 404 CI record at 719d2d7, while docs/E2E.md carries only 8b1172b's failing-run report whose prescribed route-group remedy this task rejected rather than implemented. The probes were proven before use, reproducing gpt-architect's own PL-0603 re-derivation exactly and reproducing the three-deletions-one-survivor loading-file set recorded in control/events.jsonl. ONE CLASSIFICATION IS LOAD-BEARING AND IS FLAGGED RATHER THAN BURIED: ba2bf47 and 8b1172b put this task's exact subject matter on its declared e2e and docs paths before the base. They are classified as the task's input rather than its implementation, because ba2bf47 predates PL-0704's existence in control/tasks.json by twelve days and was written by PL-0701's harness lane as an expected-to-pass assertion, and 8b1172b is the commit that created PL-0704, says in the code being changed that the fix is not done here, and proposes a route-group remedy PL-0704 recorded as considered and rejected. If the reviewer rules that authoring a deliberately-red acceptance assertion is part of delivering that acceptance, this base moves and this derivation is wrong."
      }
    },
    {
      "id": "PL-0206",
      "priority": "P0",
      "lane": "Media",
      "status": "REVIEW",
      "title": "Macrolanguage and extlang equivalence in the shared language matcher",
      "notes": "THE REVIEWER'S OWN CARVE-OUT FROM THE PL-0203 APPROVAL, not a finding of this lane's. gpt-architect approved PL-0203's script/trim corrective and, in the same verdict, identified this as a genuinely separate defect and agreed it should be its own cross-lane media task covering both audio and subtitles. In substance: the zh-yue / zh-cmn instinct points at it, but it is not a script issue -- those are Cantonese and Mandarin extended-language/macrolanguage distinctions; RFC 5646 specifically notes that the Chinese languages encompassed by zh are generally not mutually intelligible when spoken, so a matcher that collapses both to primary zh can call the wrong SPOKEN language a preferred-language fallback. The reviewer proposed P0 and stated explicitly that this does not invalidate the script/trim corrective PL-0203 landed. IT IS DISTINCT FROM THAT SCRIPT RULE, and the distinction is the whole task: a script subtag identifies a WRITING SYSTEM, which is why require_compatible_script is right for automatic subtitle selection and deliberately looser for audio; this is about MUTUAL INTELLIGIBILITY OF SPEECH, an audio-first fact that reaches subtitles only because both surfaces share one matcher. It depends on PL-0203 rather than racing it because it corrects the same function -- languageMatch in packages/media-engine/src/audio.ts, which subtitles.ts imports -- and two concurrent owners of that function would collide. Provenance of the verdict is the same as PL-0203's review record: transcribed by Claude from the ChatGPT review session rather than received over the agent bus. IMPLEMENTED IN ROUND 45, AND IT IS THE ONLY TASK IN THE RECOVERY CAMPAIGN THAT NEEDED NO PROVENANCE WORK AT ALL -- genuinely unstarted, so an ordinary start captures a true base. THE RULE IS STRUCTURAL, WITH NO REGISTRY VENDORED, exactly as the script rule already is. A new spokenLanguage helper answers which language will come out of the speakers rather than which subtag a string starts with, and it differs from primarySubtag in precisely one place: where a macrolanguage has been narrowed by an extended-language subtag. BCP-47 spells a language as 2*3ALPHA optionally followed by an extlang of exactly 3ALPHA, while a region is two letters or three DIGITS and a script is four letters, so three ASCII letters immediately after a two- or three-letter primary is unambiguously an extlang and nothing else can take that shape in that position. RFC 5646 permanently invalidates the second and third extlang positions, so there is at most one to find. THE COLLAPSE RUNS TOWARD THE SPECIFIC RATHER THAN THE GENERAL, which is what makes the equivalence clause work in both directions: zh-cmn becomes cmn rather than zh, so the prefixed and bare spellings of one language agree, and the equivalence survives any region or script suffix because those subtags are never consulted. A bare zh stays zh -- it names the macrolanguage and neither variety, so treating it as either would invent a preference the viewer did not express, which is the same asymmetry the script rule uses when it says a tag naming no script conflicts with nothing. THE NEW OUTCOME IS ITS OWN REASON, as the acceptance requires: preferred_language_other_variety_only, distinct from preferred_language_other_script_only and from the regional fallback, because the three have different remedies -- a script conflict is about READING and the dialogue is still the viewer's, a regional difference is cosmetic and is still selected, and this one is the wrong language. Reporting it as a regional fallback would move the defect out of the matcher and into the vocabulary. ONE THING WORTH KNOWING THAT THE DESIGN MADE TRUE BY ACCIDENT AND THE TESTS NOW PIN: the script and variety findings are MUTUALLY EXCLUSIVE rather than merely ordered. matchesOnlyAcrossScripts requires a match under ignore_script, setting the script aside leaves the spoken-language comparison, and a different variety fails it -- so zh-yue-Hant against a zh-cmn-Hans preference is not across both boundaries, it is simply the wrong language and the script question is moot. Only a track in the viewer's own spoken language can be across the script boundary, which is the only case where a font is the remedy. EXERCISED THROUGH BOTH CONSUMERS, which the acceptance demands because the two share one comparator precisely so they cannot drift, and the way that guarantee fails is one being corrected while the other keeps the old behaviour. selectAudioTrack is asserted not to hand a Mandarin listener a six-channel Cantonese track that would have won on quality had the language matched, and selectSubtitleTrack is asserted to refuse a Cantonese subtitle under the new reason while still returning it in ordered for a player to offer. TWO PROPERTIES HAD TO BE TIGHTENED RATHER THAN MERELY KEPT PASSING, and that is the most useful thing this task turned up. Both audio.property.test.ts and subtitles.property.test.ts asserted that a matched pair shares a PRIMARY SUBTAG, which was a restatement of the rule being removed; once zh-cmn and cmn are both in languageTagArb it is simply false, since that is one spoken language with two primary subtags. Both now ask spokenLanguage, which is the stronger claim and the one the matcher actually makes. The arbitrary gained zh, zh-cmn, zh-yue, cmn and yue-hk so the property suites reach the new path at all -- without that they would have kept passing while testing nothing about it. ALLOWEDPATHS NARROWED BEFORE THIS TASK IS EVER CLAIMED, for the reason recorded on PL-0204: packages/contracts/** prefix-contains PL-0706's leaf packages/contracts/src/shared/runtime.ts, so the wildcard would hold this P0 behind an unrelated security corrective for no benefit. The three contracts entries are where the language vocabulary this task corrects actually lives -- domains/subtitles.ts carries the subtitle policy and its outcome vocabulary, domains/audio.ts the track contract, and testing/arbitraries.ts the language-tag generators the property suites draw from. Narrowed on a PREDICTION rather than on a diff, unlike PL-0204's and PL-0205's, because this task is not yet written: if the correction turns out to want a new shared leaf -- a language-tag vocabulary of its own, say -- that is a deliberate widening at claim time with a line in this field, which is the ordinary path and is cheaper than a wildcard that reserves a whole package. packages/media-engine/** stays wide: this task owns the matcher and both its consumers, and it shares an owner with PL-0204 and PL-0205.",
      "dependencies": [
        "PL-0203"
      ],
      "allowedPaths": [
        "packages/media-engine/**",
        "packages/contracts/src/domains/audio.ts",
        "packages/contracts/src/domains/subtitles.ts",
        "packages/contracts/src/testing/arbitraries.ts"
      ],
      "preferredAgent": "claude-media",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit"
      ],
      "acceptance": "The shared languageMatch in packages/media-engine/src/audio.ts stops reducing a tag to its primary subtag where that reduction crosses a MACROLANGUAGE boundary. A spoken-language distinction encompassed by a macrolanguage is not reported as a preferred-language fallback: zh-yue against zh-cmn, and either against a bare zh preference, must not answer with a fallback strength, because a fallback there tells the caller it may play Cantonese for a Mandarin preference. Canonical primary and extlang spellings of the SAME language are treated as equivalent in both directions -- cmn and zh-cmn are one language, yue and zh-yue are one language -- and the equivalence survives a region or script suffix on either side. The rule is exercised through BOTH consumers of the shared matcher, the audio track selection in audio.ts and the subtitle selection policy in subtitles.ts, rather than through the helper alone, so neither consumer can be corrected while the other keeps the old behaviour. Any new outcome carries its own named reason: a distinction that is not regional must not be reported with a regional-fallback reason.",
      "owner": "claude-media",
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-09-14T16:23:20.603Z",
          "by": "claude-media",
          "commitSha": "c746305784226104b5304c653217dd373f52958c",
          "evidence": "turbo run typecheck via npm run check, exit 0 under pinned Node 22 on the tree committed this round. spokenLanguage is derived from subtag SHAPE with no registry vendored, the same way scriptSubtag already is, so the new rule adds no data dependency to the package."
        },
        "unit": {
          "status": "pass",
          "at": "2026-09-14T16:23:21.296Z",
          "by": "claude-media",
          "commitSha": "c746305784226104b5304c653217dd373f52958c",
          "evidence": "vitest via npm run check, exit 0 on the tree committed this round. The rule is exercised through BOTH consumers rather than through the helper, which the acceptance requires because the two share one comparator precisely so they cannot drift: selectAudioTrack is asserted not to hand a Mandarin listener a six-channel Cantonese track that would have won on quality had the language matched, and selectSubtitleTrack is asserted to refuse a Cantonese subtitle under its own new reason while still returning it in ordered for a player to offer. Equivalence is pinned in both directions -- cmn and zh-cmn are one language, yue and zh-yue are one, and the equivalence survives a region or script suffix -- and a bare zh is refused against either variety, because it names the macrolanguage and neither. Shape confusion is pinned negatively: a region, a script, a variant and a private-use singleton are none of them read as an extlang. ONE EXISTING ASSERTION WAS REVERSED RATHER THAN ADDED TO, and it is recorded as such in the test: zh-cmn-hans-cn against zh-hant under ignore_script used to expect a match and now expects none, because that is exactly the conflation this task removes. TWO PROPERTIES WERE TIGHTENED. Both suites asserted a matched pair shares a PRIMARY SUBTAG, which restated the rule being removed and is false once cmn and zh-cmn are both generated; both now assert spokenLanguage equality, which is the stronger claim and the one the matcher makes. languageTagArb gained zh, zh-cmn, zh-yue, cmn and yue-hk so the property suites reach the new path at all -- without that they would have kept passing while testing nothing about it."
        }
      },
      "implementationAgent": "claude-media",
      "updatedAt": "2026-09-14T16:23:21.922Z",
      "implementationBaseSha": "7abddb1ff77db029b44b7e79ebb215a104b3d239"
    },
    {
      "id": "PL-0405",
      "priority": "P0",
      "lane": "Backend",
      "status": "REVIEW",
      "title": "Corrective, re-run: auth integration decision, with a forgeable profile scope and an unsupported pin",
      "description": "This task SUPERSEDES PL-0401. It carries three merits blockers as well as a provenance correction, and the merits are the larger half. PROVENANCE: PL-0401 recorded implementationBaseSha fc1ea4d54c9e7d2b2f6d9aa71a977abdc3bcb400, proven with ENABLED_AUTH_CAPABILITIES and WITHHELD_AUTH_PLUGIN_FAMILIES. Those witnesses correctly located the code introduction and missed earlier task implementation on another declared write path: docs/DATA_MODEL.md did not exist at 56b343 and was created in bbe68ed8, before the recorded base, already identifying itself as covering this task's auth boundary and already selecting Better Auth 1.7.1. The true lower bound is 56b3435418f222f557ce957e7d5de3827da107b7 while docs/DATA_MODEL.md remains a write surface. PL-0401 is BLOCKED with the false range preserved as audit history. DO NOT CLAIM THIS TASK UNTIL THE THREE BLOCKERS IN THE NOTES ARE IMPLEMENTED; it is created now so the findings are recorded against a task rather than living only in a review transcript.",
      "notes": "BLOCKER 1, AND IT IS THE PL-0706 DEFECT IN A SECOND PLACE. ProfileScope carries a type-only unique symbol property while mintProfileScope returns an ordinary object via cast, so a caller holding a genuine scope can write a spread with a replacement profileId and TypeScript carries the branded structural type straight through -- without the explicit cast ADR-007 claims is the only forgery route. Downstream persistence then trusts scope.profileId directly, which makes this a cross-profile data-access bypass rather than a typing nicety. The prescription: make the scope genuinely nominal so a spread copy cannot remain assignable, and if it is meant to be a runtime capability rather than a compile-time proof, use issuance identity as well. The pattern already exists in this repository twice -- ClassifiedRuntime in packages/contracts/src/shared/runtime.ts and PinnedTarget in media-inspection -- module-private brand, freeze, WeakSet registry, identity check as the consumer's first action. THE SURFACE THIS IMPLIES IS WIDER THAN THIS TASK CURRENTLY DECLARES, AND THAT IS SETTLED BEFORE THE CLAIM RATHER THAN DISCOVERED DURING IT. The reviewer offered two levels: at minimum make the scope genuinely nominal so a spread copy cannot remain assignable, and if it is meant to be a runtime capability, use issuance identity as well. The minimum is NOT ACHIEVABLE IN THE TYPE SYSTEM, and PL-0706 already established why: a spread copies a branded property along with everything else and type-checks with no cast at all, so only object identity rejects it. TypeScript cannot make a spread unassignable. Therefore the runtime registry is not the optional upper level here, it is the only level that works -- and a registry closes nothing unless the consumers ASK. The consumers are in another package: packages/persistence/src/progress-repository.ts and watchlist-repository.ts read scope.profileId straight into their predicates, and profile-repository.ts and repository-scoping.test.ts also handle the type. So this task must declare packages/persistence/src/** as a write path when it is claimed, not merely the migration, and it will then overlap PL-0402, PL-0403 and PL-0404, which are BACKLOG today and must be sequenced after it. The alternative -- brand and registry in packages/auth with no consumer asking -- would produce a capability that looks unforgeable and is not, which is worse than the present state because it would read as fixed. Whoever claims this should widen the declaration in the same edit that claims it, and say so in the reason. BLOCKER 2, AND THE EXACT PIN IS WORKING RATHER THAN FAILING. better-auth and its Drizzle adapter are pinned at exactly 1.7.1. Upstream's current release is 1.7.4, from 2026-09-10, and its security policy supports only the latest version. The reviewer was explicit that this is not an argument against exact pins: it proves the policy works by making staleness visible, which is what ADR-007 said it was for. Move both packages together and update REVIEWED_BETTER_AUTH_VERSION. DO NOT BUMP AND LEAVE THE MIGRATION: 1.7.3 restored the 1.6 account core schema after the issuer-schema change in 1.7.0 through 1.7.2, while this repository's hand-written migration still has an issuer column and an (issuer, account_id) uniqueness rule. The migration must be regenerated or reconciled against the reviewed version BEFORE security approval. That is why packages/persistence/migrations/0000_profile_scoped_identity.sql moves from reviewDependency to WRITE PATH here: on PL-0401 it was only evidence, and read-only was correct; here it has to change, and reviewDependencies are read-only by contract. PL-0402 also owns packages/persistence and is BACKLOG, so no active conflict exists today, but the two must be sequenced and the reviewer should say which owns that file afterwards. BLOCKER 3. ADR-007 still states obsolete control-plane routing, naming gpt-architect as preferred implementer and claude-lead as reviewer, while the record now correctly has claude-backend implementing and gpt-architect reviewing independently. Rewrite that paragraph as history rather than as current routing, and prefer wording that points at the task gate as authoritative -- otherwise completing the review needs a post-approval edit that immediately stales the fingerprint it just bound. TWO HONEST CAVEATS CARRIED FORWARD FROM PL-0401, neither an unmet clause: the migration has never been executed, because no PostgreSQL exists in this environment, and better-auth.ts is deliberately untested. SEQUENCING: this task is READY rather than BLOCKED because nothing external gates it, but it should not be claimed until the three blockers are implemented, and claiming it early would reserve packages/auth and two documents while no work was happening.",
      "dependencies": [],
      "allowedPaths": [
        "packages/auth/**",
        "docs/DECISIONS.md",
        "docs/DATA_MODEL.md",
        "packages/persistence/migrations/0000_profile_scoped_identity.sql",
        "packages/persistence/src/**",
        "package-lock.json"
      ],
      "surfaceWidenedAtClaim": "WIDENED 2026-09-15 by claude-lead in the same edit that claimed the task, which is what the notes instruct: 'this task must declare packages/persistence/src/** as a write path when it is claimed, not merely the migration' and 'whoever claims this should widen the declaration in the same edit that claims it, and say so in the reason'. TWO ENTRIES WERE ADDED, AND THEY ARE NOT THE SAME KIND OF ADDITION. First, packages/persistence/src/**, exactly as prescribed: the registry is the only mechanism that rejects a spread forgery, TypeScript cannot make a spread unassignable, and a registry closes nothing unless the consumers ask, so the consumers must change. This was demonstrated rather than argued: with the issuance check in place and persistence untouched, packages/persistence/src/repository-scoping.test.ts fails six tests, because its fixtures build scopes with 'as unknown as ProfileScope' and refuseForeignScope now refuses them. progress-repository.ts moves from reviewDependency to write path for the same reason and is removed from reviewDependencies rather than listed twice. Second, package-lock.json, WHICH THE REVIEWER DID NOT NAME AND WHICH IS THEREFORE FLAGGED FOR THE REVIEWER RATHER THAN PRESENTED AS PRESCRIBED. The notes require better-auth and its Drizzle adapter to move together; the root lockfile still resolves 1.7.1, so after the pin moves, npm ci refuses the mismatch and every gate that starts from a clean install fails before it runs. Leaving it stale would ship a dependency bump that cannot be installed reproducibly, so the lockfile is part of moving the packages rather than a separate concern. It is a generated file owned by no active task; PL-0001, which generated it, is DONE. If the reviewer would rather see it as its own task, it is one entry to remove. NO OTHER WRITE SURFACE WAS ADDED, and packages/persistence/src/schema/auth.ts is inside the prescribed entry rather than an extra: it still declares the issuer column and the (issuer, account_id) unique that the migration reconciliation removes, so leaving it would make drizzle-kit propose reinstating both on the next generate. REVIEWDEPENDENCIES ALSO CHANGED, AND IT IS A REPLACEMENT RATHER THAN A WIDENING OF WRITE ACCESS. packages/persistence/src/progress-repository.ts left the list because it became a write path and listing it in both would say two different things about the same file. In its place the two files the reviewer named as the pattern this implementation must copy are declared read-only: packages/contracts/src/shared/runtime.ts (ClassifiedRuntime) and packages/media-inspection/src/pin.ts (PinnedTarget). Neither is written; they are declared so the approval fingerprints the originals against which the copy is judged, which is exactly what reviewDependencies is for. If the reviewer considers that padding, they are two entries to remove and nothing else changes.",
      "reviewDependencies": [
        "packages/contracts/src/shared/runtime.ts",
        "packages/media-inspection/src/pin.ts"
      ],
      "preferredAgent": "claude-backend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "architecture-review",
        "security-review",
        "typecheck",
        "unit"
      ],
      "acceptance": "A recorded decision selects the auth approach and states the boundary it implies: a packages/auth seam the rest of the application depends on instead of the vendor library, database-backed sessions, a deliberately small enabled surface with the advanced plugin stacks left off, and an exact-pinned dependency whose upgrades are treated as security-sensitive work rather than floated on a caret range. The pinned version must be one upstream currently supports, and the hand-written migration must agree with that version's account schema rather than an abandoned intermediate one. ProfileScope must be UNFORGEABLE by a holder of a genuine scope: a spread copy carrying a replaced profileId must not remain assignable, and any consumer that grants data access on the strength of a scope must establish that the scope was ISSUED rather than merely that it type-checks. The decision record must describe the routing that actually applies, or point at the task record as authoritative rather than restating it.",
      "owner": "claude-backend",
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-09-15T05:36:23.753Z",
          "by": "claude-backend",
          "commitSha": "0337663aa9cdf1572298492b5f49e37e48f6e0a2",
          "evidence": "npx turbo run typecheck --force at 0337663aa9cdf1572298492b5f49e37e48f6e0a2, exit 0, 9 of 9 tasks successful and 0 cached, 28.8s, run by the lead in the integrated checkout. Forced rather than cached deliberately. Run against a tree installed by npm ci from the regenerated lockfile -- npm ci exit 0 with better-auth resolving to 1.7.5 -- so the typecheck covers the bumped dependency rather than the old resolution. The same command was run in the PL-0405 worktree at 3e93d0a, also exit 0, likewise forced."
        },
        "unit": {
          "status": "pass",
          "at": "2026-09-15T05:36:23.972Z",
          "by": "claude-backend",
          "commitSha": "0337663aa9cdf1572298492b5f49e37e48f6e0a2",
          "evidence": "npx turbo run test --force at 0337663aa9cdf1572298492b5f49e37e48f6e0a2, exit 0, 15 of 15 tasks and 0 cached, 2096 passed 1 skipped 0 failed. Two new suites carry this task's security claim. packages/auth/src/profile-scope.test.ts, 14 cases, attempts three distinct forgeries and asserts rejection at the consumer boundary for each: a spread copy with a replaced profileId built WITH NO CAST ANYWHERE, so the file stops compiling the day TypeScript can reject it; a hand-built object via as unknown as ProfileScope; and a structurally-correct never-issued object, frozen, same keys, and a symbol with the same description, which one case first asserts is indistinguishable under Object.isFrozen, Object.keys and symbol description before the next asserts it is still refused. It also asserts that the spread copy carries the SAME brand symbol object as the genuine scope, which is the load-bearing fact making the brand insufficient on its own. packages/persistence/src/scope-forgery.test.ts, 14 cases, proves the other half -- that the repositories ASK -- because the auth suite proves only that the mechanism exists. It is exhaustive over all ten exported repository functions rather than representative, since the defect it guards is an eleventh function reading the field directly, and its db is a Proxy that throws on any property access so that refused and refused before doing any I/O are distinguished rather than conflated. INTERMEDIATE FAILURE REPORTED RATHER THAN DROPPED: an earlier run of this gate at 4b3ae21 exited 1 with 6 failures in packages/persistence/src/repository-scoping.test.ts. That was the fix working -- those fixtures minted scopes with as unknown as ProfileScope and the issuance check refused them -- and it was left red rather than neutralised until the task's write surface was widened to packages/persistence/src/** as the task record prescribes. The fixtures now obtain genuine issued scopes; the foreign-scope fixture became strictly sharper in the process, because as a cast it failed on two counts at once and would have passed with the account comparison deleted, while as a real grant to another household it fails on exactly one thing and goes red if that comparison is removed."
        }
      },
      "implementationAgent": "claude-backend",
      "updatedAt": "2026-09-15T05:36:47.042Z",
      "implementationBaseSha": "68c432645131336393f3d5e08503951193ca5350",
      "supersedes": "PL-0401"
    },
    {
      "id": "PL-AI-0007",
      "priority": "P0",
      "lane": "Coordination",
      "status": "REVIEW",
      "title": "Supersession is a field, and a task cannot stay gated behind one",
      "description": "Four tasks -- PL-0205, PL-0401, PL-0601, PL-0703 -- are BLOCKED, terminal, and superseded by a corrective re-run. Each says so only in prose. BLOCKED transitions only to BACKLOG, READY or CANCELED, so a superseded task can never reach DONE, and with completion.requireAllDependenciesDone true, any task depending on one is gated forever. That is not a hypothetical: PL-0301 was gated behind PL-0205 while PL-0207 sat DONE, and with it PL-0302, PL-0501, PL-0502, PL-0701 and PL-0702. A human noticed. Nothing in ai:validate could have.",
      "notes": "SCOPE IS THE VALIDATOR, NOT A DEPENDENCY REWRITER. The rule reports; it never edits a dependency, because repointing a dependency is a task-definition change and those are deliberate, reasoned and recorded -- see PL-0301's own notes for what that looks like. An automatic repointer would make the graph self-modifying on the strength of a field any writer can set. THE RULE, PRECISELY. For each task T declaring supersededBy S: S must exist, must not be T, and T must not itself be DONE, or that is an ERROR, because a DONE task that claims to be superseded is two contradictory records of the same work. If S declares supersedes, it must name T -- a one-way pointer is a typo waiting to be trusted. Then, for each task D whose dependencies contain T: if S is DONE, ERROR, because D is gated behind a task that can never complete while its replacement already has; if S is not DONE, WARN, because the edge is not yet fatal but will become so the moment S completes, and a warning that arrives before the deadlock is the entire point. WHY ERROR AND NOT WARN FOR THE FIRST CASE. ai:validate already treats provenance drift as a warning and structural impossibility as an error. A dependency that can never be satisfied is structural: no sequence of legal transitions resolves it. It belongs with 'missing dependency' and 'invalid status', not with drift. WHAT THIS RULE CANNOT DO, STATED SO NOBODY OVERTRUSTS IT. supersededBy is self-asserted, exactly like fromAgent on the agent bus. It proves that somebody wrote a pointer, not that the successor genuinely carries the predecessor's work. A wrong pointer produces a wrong error or hides a real deadlock. It is a cheap alarm on a failure mode that has already cost this project one stalled milestone, not a proof of anything.",
      "dependencies": [],
      "allowedPaths": [
        "scripts/ai-control-plane.mjs",
        "scripts/test-ai-control-plane.mjs",
        "control/README.md"
      ],
      "reviewDependencies": [
        "control/policies.json"
      ],
      "preferredAgent": "claude-lead",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "repo-validate",
        "typecheck",
        "unit"
      ],
      "acceptance": "ai:validate refuses a task graph in which a task depends on a task that declares supersededBy and whose named successor is DONE, and warns when the successor is not yet DONE. It also refuses a supersededBy naming a missing task, naming itself, declared on a DONE task, or contradicted by the successor's supersedes field. The rule reports and never edits a dependency. The four existing supersession pairs validate clean, and a regression in scripts/test-ai-control-plane.mjs proves the rule fires -- both that it errors on a reconstructed PL-0301-behind-PL-0205 graph and that it stays silent on the repaired one, so it cannot pass by being inert.",
      "owner": "claude-lead",
      "gateResults": {
        "repo-validate": {
          "status": "pass",
          "at": "2026-09-15T17:32:54.251Z",
          "by": "claude-lead",
          "commitSha": "80e3d3e3e0130571a1a0791b496a9e2cce0c6c7a",
          "evidence": "npm run repo:validate at the PL-AI-0007 working tree, exit 0, 'Project Liberty repository validation passed.' Re-run after the control/README.md section documenting supersession was written, because that file is a declared write path and validate-repo.mjs reads the repository layout this task's documentation describes."
        },
        "typecheck": {
          "status": "pass",
          "at": "2026-09-15T17:32:54.458Z",
          "by": "claude-lead",
          "commitSha": "80e3d3e3e0130571a1a0791b496a9e2cce0c6c7a",
          "evidence": "npx turbo run typecheck --force, exit 0, 9 of 9 tasks and 0 cached. STATED PLAINLY BECAUSE IT WOULD OTHERWISE OVERCLAIM: this task's code is scripts/ai-control-plane.mjs and scripts/test-ai-control-plane.mjs, which are plain ESM and are not in any workspace, so no tsc pass covers them and this gate proves only that the change broke nothing in the nine TypeScript projects. The regression evidence for the rule itself is under the unit gate, where it belongs."
        },
        "unit": {
          "status": "pass",
          "at": "2026-09-15T17:32:54.650Z",
          "by": "claude-lead",
          "commitSha": "80e3d3e3e0130571a1a0791b496a9e2cce0c6c7a",
          "evidence": "Two executions, and the second is the one that matters. npx turbo run test --force, exit 0, 15 of 15 tasks and 0 cached, 2096 passed 1 skipped 0 failed. Then node scripts/test-ai-control-plane.mjs, exit 0, 'AI control plane tests passed (67 scenarios)' -- the suite that actually exercises validate, up from 66. Scenario 10s is new and asserts the rule in BOTH directions on one frozen fixture set, because a validator rule fails characteristically by staying silent forever while everyone assumes it works: the reconstructed PL-0301-behind-PL-0205 shape errors naming both ends, the repair clears it BY REPOINTING THE DEPENDENCY AT THE SUCCESSOR rather than by deleting the pointer, a successor that is not yet DONE warns instead of failing the build, and four malformed pointers are each refused -- unknown successor, self-reference, a successor whose supersedes disagrees, and supersededBy on a DONE task. Fixtures rather than live data on this suite's standing rule: the live graph now has zero such edges, so a scenario reading it would pass by being inert. ALSO REPAIRED, AND DISCLOSED RATHER THAN QUIETLY FIXED: scenario 9w went red because authoring PL-0901 gave claude-lead an autonomously workable task, which falsified an assertion that was a screenshot of that day's backlog rather than an invariant. On this file's own standing rule the scenario was moved onto a frozen fixture set instead of having its expectation rewritten -- rewriting it would be indistinguishable from rewriting it to match a regression -- and it now proves both halves: an orchestration-owning candidate is skipped by name and left READY, and an identical candidate with a non-privileged surface is still selected, so the scenario cannot pass by selection being broken outright. The other two script suites were run to prove the change is contained: node scripts/test-validate-env.mjs exit 0, 38 scenarios; node scripts/cloud/test-dispatcher.mjs exit 0, 35 scenarios."
        }
      },
      "implementationAgent": "claude-lead",
      "updatedAt": "2026-09-15T17:32:54.857Z",
      "implementationBaseSha": "80e3d3e3e0130571a1a0791b496a9e2cce0c6c7a"
    },
    {
      "id": "PL-0901",
      "priority": "P0",
      "lane": "Architecture",
      "status": "REVIEW",
      "title": "Windows desktop shell and the PlayerAdapter boundary",
      "description": "Project Liberty's first production target is Windows desktop on the commander's own machine. The commander has settled the direction: a Tauri shell, the existing Next.js application preserved rather than rewritten and served by a Next standalone sidecar, a PlayerAdapter boundary the application and domain layers depend on instead of any concrete player, libmpv for compatible non-DRM playback, Shaka/EME retained for DRM-capable playback, capability routing made explicit before playback rather than discovered during it, and an LGPL-compatible libmpv/FFmpeg build pipeline of our own rather than a GPL prebuilt. This task records that decision and specifies the boundary. It writes documentation only -- no shell, no adapter, no dependency is added here, because the instruction is that architecture is recorded before a large irreversible implementation, not after it.",
      "notes": "THE AUDIT CAME FIRST AND IT FOUND A CLEAN SLATE. A repository-wide search for electron, tauri, mpv, libmpv, webview2, neutralino and nw.js across every .ts, .tsx, .json, .md, .mjs and .yml outside node_modules returned nothing. So neither shell was favoured by anything already in the tree, and the decision was made on evidence rather than on inertia. Today's player is shaka-player ~5.2.6 with xstate ^5.32.5 and custom-media-element in apps/web. THE DECIDING FACT IS COMPOSITING, NOT BUNDLE SIZE. Putting HTML chrome over a hardware-accelerated native video surface in one window on Windows is a documented, shipping arrangement on WebView2 -- ICoreWebView2Controller2::put_DefaultBackgroundColor accepts alpha 0 or 255 only, and at 0 the host's content renders beneath -- and has no supported path on Chromium, where PPAPI is gone and child HWNDs are no longer in the window hierarchy. Stremio ships the WebView2 plus libmpv --wid arrangement in production on Windows and measured its previous render-API-into-a-texture approach as 2-5x worse. Any size-based argument for Tauri should be dropped: with a Node sidecar the installer lands near Electron's anyway. THE SIDECAR IS WHAT PRESERVES THE APPLICATION. Tauri's default is a static export, which would force every /api/v1 route handler off Next -- a rewrite of the application's architecture, which the instruction forbids. output: standalone in a sidecar keeps Server Components, route handlers, proxy.ts, cookies(), headers(), Server Actions and ISR working. It costs a Node runtime in the bundle, a loopback listener, supervision code and Tauri's CSP injection, which does not reach an external http origin. ONE QUESTION THIS TASK DOES NOT DECIDE AND MUST NOT DECIDE SILENTLY. A sidecar runs the application's server on a machine the user administers. If provider resolution runs there, then the boundary that enforces invariants 1 and 2 executes from files the user can read and replace, with whatever credentials it needs in its environment. The alternative is that the desktop build implements those specific routes as an authenticated proxy to the backend -- same route, same contract, same URL, a different implementation behind it, selected by build target, which is ordinary adapter work rather than a rewrite. This document states the question, states the recommendation, and leaves the ruling to the reviewer and the commander. It is a design input to PL-0501 and is cheaper to supply now than to retrofit. DRM IS A ROUTING CONSTRAINT, NOT A GAP TO CLOSE. mpv has no CDM and cannot be given one. That is not something to work around: the mpv adapter must REFUSE a DRM-protected candidate with a named reason and must never attempt it, never fall back and never degrade. Which means capability routing has to be an explicit, reasoned decision before playback -- canPlay returning a reason on both branches -- rather than a guess discovered at load time. That is what makes invariant 2 structural here rather than a promise, and it is why the boundary carries a capability descriptor at all. THE LICENSING WORK IS THE LONG POLE AND IT GATES SHIPPING RATHER THAN PROTOTYPING. A stock prebuilt libmpv is a GPL build and must not ship in a closed-source product; the one LGPL variant on offer statically links an LGPLv3 FFmpeg and carries its builder's own no-warranty disclaimer. Building our own with -Dgpl=false and an LGPL FFmpeg, with an SBOM, converts an open-ended exposure into a reviewable artifact. Nothing in this document is legal advice and counsel review is named as a precondition of shipping rather than assumed.",
      "dependencies": [],
      "allowedPaths": [
        "docs/DESKTOP_PLAYBACK.md",
        "docs/ARCHITECTURE.md"
      ],
      "reviewDependencies": [
        "apps/web/src/components/player/playback-machine.ts",
        "apps/web/src/components/player/engine.ts",
        "apps/web/src/components/player/shaka-engine.ts",
        "apps/web/src/components/player/playback-session.ts",
        "packages/contracts/src/domains/playback.ts",
        "docs/RESEARCH_PLAYBACK.md",
        "docs/AV_SYNC_MEASUREMENT.md",
        "docs/CONTENT_RIGHTS.md"
      ],
      "preferredAgent": "claude-lead",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "architecture-review",
        "rights-review"
      ],
      "acceptance": "A recorded architecture decision states the Windows desktop target, the Tauri shell, the preserved Next.js application served by a standalone sidecar, and the LGPL-compatible libmpv/FFmpeg build pipeline, each with the evidence it rests on and the strongest argument against it. It specifies PlayerAdapter as an interface the application and domain layers depend on -- load an authorized playback session, play, pause, seek, stop, select audio track, select subtitle track, set volume, expose position and duration, expose buffering and stall events, expose recoverable and fatal errors, expose track metadata, expose A/V sync telemetry where available, dispose -- with no mpv, Electron, Tauri or Shaka type reachable from it, and names the two implementations behind it and the explicit capability routing that chooses between them, including the requirement that the mpv adapter refuse DRM-protected candidates with a named reason rather than attempt them. It states that media addresses continue to come only from authorized provider resolution and the playback-session boundary and that the client can never submit one, that stream candidate ranking stays in @liberty/media-engine, and that provider-specific logic stays in @liberty/provider-sdk. It states where provider resolution runs in the desktop build as an open question with a recommendation rather than deciding it silently, because that answer is a design input to PL-0501. It records what was verified, what was not, and which specific facts would reverse each decision. No shell, adapter or dependency is implemented here.",
      "owner": "claude-lead",
      "gateResults": {},
      "implementationAgent": "claude-lead",
      "updatedAt": "2026-09-15T17:50:43.130Z",
      "implementationBaseSha": "80e3d3e3e0130571a1a0791b496a9e2cce0c6c7a"
    }
  ],
  "reservedExternal": [],
  "recommended": [
    {
      "id": "PL-AI-0006",
      "priority": "P0",
      "lane": "Architecture",
      "status": "READY",
      "title": "Contract module boundaries",
      "dependencies": [
        "PL-AI-0004"
      ],
      "allowedPaths": [
        "packages/contracts/**",
        "packages/media-engine/**",
        "packages/provider-sdk/**",
        "apps/web/**"
      ],
      "preferredAgent": "claude-lead",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "repo-validate",
        "unit"
      ],
      "acceptance": "Shared vocabularies live in their own leaf modules, domain contracts live in one module each, index.ts is a re-export-only compatibility barrel rather than the authoritative public surface, and a wildcard exports map lets a new domain contract be added without touching package metadata or the barrel; no cycle-driven z.lazy remains and structural regressions prove the boundary rather than merely reflecting it; no schema behaviour changes",
      "owner": null,
      "gateResults": {}
    },
    {
      "id": "PL-AI-0002",
      "priority": "P1",
      "lane": "Coordination",
      "status": "READY",
      "title": "GitHub bridge for cross-agent review",
      "dependencies": [
        "PL-AI-0001"
      ],
      "allowedPaths": [
        ".github/**",
        "control/**",
        "scripts/**",
        "docs/**"
      ],
      "preferredAgent": "claude-lead",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "architecture-review",
        "security-review"
      ],
      "acceptance": "GitHub becomes the shared state bridge for Claude Desktop and OpenAI review without manual file copying",
      "owner": null,
      "gateResults": {},
      "implementationAgent": "claude-lead",
      "updatedAt": "2026-08-17T01:53:44.285Z",
      "implementationBaseSha": "b157a5846d45568f516240f581d6b317a544aa12"
    },
    {
      "id": "PL-0305",
      "priority": "P1",
      "lane": "Provider",
      "status": "READY",
      "title": "A real catalog metadata source",
      "notes": "The discovery layer has no metadata source. PL-0105 landed a seam, and docs/CATALOG_SOURCE.md says so in its first line; this task is the thing that belongs behind it, tracked so the gap stays visible instead of being hidden by a port that looks finished. docs/PRODUCT_SPEC.md step 2 of the initial user journey is browse/search normalized metadata, and nothing in this repository ingests any: every surface reads six fictional works. PL-0301 and PL-0302 do not close it - they supply stream candidates, which is what a title plays from, not what a catalog is made of. PARTLY BLOCKED, and the split is deliberate rather than a hedge. The design half - identity and dedupe, refresh and staleness, tombstones, paging, provider-side search, the locale-tagged contract, and the undeclared-rights state that catalogItemSchema cannot currently express - needs no external access and can begin now. Connecting an actual metadata provider, and carrying artwork, cannot: both need an agreement and credentials, which control/policies.json escalates to human-only under Licensing and Credentials - the same categories that block PL-0302. It is a DIFFERENT agreement from PL-0302's, covering catalog metadata and image rights rather than stream rights, so this is not merely waiting on that task; the honest status is BACKLOG with the ceiling named, not BLOCKED on someone else's licence.",
      "dependencies": [
        "PL-0105"
      ],
      "allowedPaths": [
        "packages/catalog-ingestion/**",
        "apps/web/src/lib/catalog-source.ts",
        "apps/web/src/lib/catalog-source-registry.ts",
        "docs/CATALOG_SOURCE.md",
        "docs/ARCHITECTURE.md"
      ],
      "preferredAgent": "claude-backend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "architecture-review",
        "security-review",
        "rights-review",
        "typecheck",
        "unit"
      ],
      "acceptance": "A metadata source stands behind resolveCatalogMetadataSource and answers what the port deliberately does not. Ingestion: something fetches, schedules and backfills, rather than an array being maintained by hand. Refresh: an answer carries its age and a stated staleness policy, so nobody has to guess how old a rail is. Identity and dedupe: how a provider's native id becomes a normalizedContentId, and what happens when two sources describe the same work, is settled as a design decision before it is coded - this is the largest missing piece and it is not a coding question. Deletion: a work that disappears from a source produces a tombstone, so it stays distinguishable from a fetch that failed. Paging and provider-side search: listRecords returning everything is a shape only six fixtures can use, and who owns ranking and paging is part of the contract rather than an accident of in-memory filtering. Artwork: images arrive with their own licensing, tracked separately from the work's, or they do not arrive. Localization and availability: title, genre and synopsis are locale-tagged and a work can be offered in more than one language, and availability windows and territory exist, so the empty state's claim about a region is backed by something. Every record declares a rights basis whose category agrees with its item and whose reference is an opaque internal identifier or null, and the ingestion path is treated as untrusted third-party I/O - allowlisted egress, bounded bodies, redirects revalidated, and no media address anywhere in a catalog payload.",
      "owner": null,
      "gateResults": {}
    }
  ]
}

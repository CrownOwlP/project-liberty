{
  "generatedAt": "2026-09-20T15:23:15.338Z",
  "agent": "gpt-architect",
  "executionAvailable": false,
  "assigned": [
    {
      "id": "PL-0711",
      "priority": "P1",
      "lane": "Security",
      "status": "IN_PROGRESS",
      "title": "Playback-session candidate wire bounds match StreamCandidate bounds",
      "dependencies": [
        "PL-0708",
        "PL-0501"
      ],
      "allowedPaths": [
        "apps/web/src/app/api/v1/playback/session/**",
        "docs/API_CONTRACTS.md"
      ],
      "reviewDependencies": [
        "packages/contracts/src/domains/playback.ts"
      ],
      "preferredAgent": "claude-security",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit",
        "security-review"
      ],
      "acceptance": "playbackSessionCandidateSchema uses THE SAME AUTHORITATIVE id and providerId limits as the StreamCandidate contract -- the exported MAX_STREAM_CANDIDATE_ID_CHARS and MAX_STREAM_CANDIDATE_PROVIDER_ID_CHARS -- and NOT copied numeric literals. PL-0708 exported those constants for exactly this reason; a copied literal would recreate the defect one layer out, which is the defect this task exists to close. Required: an oversized resolved candidate CANNOT RE-EXPAND ON THE SESSION WIRE after passing through another producer seam, which is the actual risk -- the contract bound governs what a provider may produce, and a second unbounded restatement downstream means the bound stops at the seam; fixture and Wikidata legitimate identifiers remain accepted, because a bound that rejects a real upstream id is an outage rather than a fix; tests cover the EXACT MAXIMUM, MAXIMUM PLUS ONE, and the serialized response, since a bound tested only from above does not prove the boundary is where it is claimed to be; reason and error output does not echo the oversized value; and NO SECOND INDEPENDENT BOUND VOCABULARY IS INTRODUCED. Note while working: playbackSessionCandidateSchema also carries unbounded uri and mimeType. They are not named in the reviewer's acceptance and are not in scope by default -- if they warrant bounds, say so and let the reviewer rule rather than adding them quietly.",
      "notes": "Created 2026-09-19 on gpt-architect's round-50 direction, reviewing at f83bc65015851b97ff9af86249b144cd57ea8047. Filed from the PL-0708 approval. apps/web's playbackSessionCandidateSchema (session/contract.ts, around lines 258-262) independently restates id and providerId as unbounded strings and does not derive from streamCandidateSchema.",
      "owner": "gpt-architect",
      "gateResults": {},
      "implementationAgent": "gpt-architect",
      "updatedAt": "2026-09-20T12:55:49.924Z",
      "implementationBaseSha": "20edec3aa3f33dbd58aa405d1210f56ce54c207f"
    }
  ],
  "review": [
    {
      "id": "PL-0308",
      "priority": "P1",
      "lane": "Backend",
      "status": "REVIEW",
      "title": "Production composition registers the catalog ingestion runtime",
      "dependencies": [
        "PL-0305"
      ],
      "allowedPaths": [
        "apps/web/src/lib/catalog-source-registry.ts",
        "apps/web/src/lib/catalog-source-registry.test.ts",
        "apps/web/src/lib/server-bootstrap.ts",
        "apps/web/src/lib/server-bootstrap.test.ts",
        "apps/web/src/instrumentation.ts",
        "apps/web/package.json",
        "docs/CATALOG_SOURCE.md"
      ],
      "reviewDependencies": [
        "apps/web/src/lib/catalog-ingestion-source.ts",
        "packages/catalog-ingestion/src/index.ts",
        "packages/media-inspection/src/node/pinned-fetch.ts"
      ],
      "preferredAgent": "claude-backend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit",
        "security-review",
        "rights-review"
      ],
      "acceptance": "A hosted deployment that is configured for a real catalog source actually gets one: something in the production composition root calls registerCatalogIngestionRuntime with a runtime built next to the Node pinned-fetch construction. PL-0305 made the application CAPABLE of hosting the real source and the reviewer accepted that as complete; what remains is that NOTHING CALLS THE REGISTRATION, so a hosted deployment still answers no_metadata_source_configured -- a true statement about the deployment rather than about the registry, and the exact gap this task owns. Required: the runtime is built from the existing pinned-fetch transport rather than a second fetch path; NO CREDENTIAL OR API KEY IS INTRODUCED, since the initial source needs none and an ambient key would be the hidden credentialed source PL-0305's review forbade; an unconfigured deployment must STILL refuse by name rather than silently acquiring a default; and a regression must drive the composition root and observe a configured resolution, because a registration nobody has watched fire is not known to work. OUT OF SCOPE: persistent scheduling and storage, which is PL-0309, and the rights register, which is not a coding task. NOTE BEFORE CLAIMING: the exact composition-root file may not exist yet; the declared surface names the likely homes and must be corrected before the claim rather than widened after, on the PL-0710 precedent.",
      "notes": "Created 2026-09-20 on gpt-architect's round-52 direction when it approved PL-0305: 'Make sure the existing handoff has explicit ownership for ... If any is currently only prose with no task, create a narrow task before it can be forgotten.' PL-0305 is NOT to be reopened for any of these. Follow-up 1 of 4. Point 3 of the round-52 verdict.",
      "owner": "claude-backend",
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-09-20T15:23:14.746Z",
          "by": "claude-backend",
          "commitSha": "f932ebe66c99cbb4fdf15f631d1464b16115aadc",
          "evidence": "npx turbo run typecheck --force at the integrated head, exit 0, 11 of 11. Also lint 0 and build 0. npm ci --dry-run exit 0."
        },
        "unit": {
          "status": "pass",
          "at": "2026-09-20T15:23:14.858Z",
          "by": "claude-backend",
          "commitSha": "f932ebe66c99cbb4fdf15f631d1464b16115aadc",
          "evidence": "npx turbo run test --force, exit 0, 20 of 20, 0 cached, 2642 passed 1 skipped (2617 before this wave). server-bootstrap.test.ts 16 passed; apps/web src/lib plus the entry point 162 passed. THE ENTRY POINT WAS PROVED, NOT ASSUMED, AND IT CORRECTED THIS LEAD. My pre-claim surface correction moved instrumentation.ts to apps/web/ on the belief Next reads it from the app root; the implementer replayed Next 16.3.1's own findPagesDir and getFilesInDir and showed rootDir = path.join(pagesDir || appDir, '..') resolves to apps/web/src for a src/app project, so a file at the app root is NEVER LOADED -- and it REFUSED TO WRITE THE FILE AT MY DECLARED PATH rather than produce something Next never loads sitting where the wiring appears to be. The surface was corrected on that evidence and the probe now resolves instrumentationHookFilePath to /src/instrumentation.ts against the real tree, with a root-placed fixture resolving nothing as the control. VALUE-RED on the entry point: flipping the runtime guard from nodejs to node gives 2 failed 14 passed, 'runs the bootstrap in the Node runtime and the registry then resolves configured' and 'says so at startup when nothing is configured'. The ORIGINAL red for the composition root could only be module-resolution -- 'Cannot find module ./server-bootstrap', no tests collected -- because the gap this task owns is the ABSENCE OF A CALLER: there was no root to hand a wrong value, and the behaviour a value-red would assert already passed. Stated rather than smoothed. THE CREDENTIAL ASSERTION EARNED ITS KEEP IMMEDIATELY: it is checked in two halves, every declared name against /KEY|SECRET|TOKEN|PASSWORD|CRED|AUTH/i and the module's own source text against the declared set so a second undeclared read cannot hide, and it failed on the first green run because a comment named LIBERTY_BUILD_TARGET -- 'expected [LIBERTY_BUILD_TARGET, ...(8)] to deeply equal [...(7)]'. The pinned transport is asserted by REFERENCE IDENTITY, expect(runtime.provider.transport.fetchImpl).toBe(nodePinnedFetch), because a second fetch path that happened to work would satisfy any behavioural check. rightsRegister is noRightsBasisEstablished by reference, so a configured deployment publishes nothing and says per record why."
        }
      },
      "surfaceCorrectedBeforeClaim": "CORRECTED 2026-09-20 BEFORE THE CLAIM, which is what this task's own notes said must happen: the round-52 declaration named likely homes rather than verified ones. Verified against the tree now. REMOVED: apps/web/src/instrumentation.ts -- Next reads instrumentation.ts from the APP ROOT, not from src/, and neither path exists today; apps/web/instrumentation.ts is the correct home and replaces it. ADDED: catalog-source-registry.test.ts and server-bootstrap.test.ts, because acceptance clause 4 requires a regression that drives the composition root and the declaration carried no test file at all -- a task that cannot write a test cannot satisfy its own acceptance, which is the same defect PL-0708 and PL-0305 each hit once. ADDED apps/web/next.config.ts, because Next may require instrumentationHook to be enabled for the instrumentation entry point to run on this version and the implementer must be able to check rather than assume. docs/DEPLOYMENT.md does not exist and is KEPT as a create-if-needed path rather than dropped: a deployment that must register a runtime is a deployment fact, and if the implementer judges the operator note belongs in docs/CATALOG_SOURCE.md instead, the unused entry comes back out at review as PL-0501's unused CI entry did.",
      "implementationAgent": "claude-backend",
      "updatedAt": "2026-09-20T15:23:14.976Z",
      "implementationBaseSha": "f932ebe66c99cbb4fdf15f631d1464b16115aadc",
      "surfaceCorrectedTwiceBeforeCompletion": "SECOND CORRECTION 2026-09-20, mid-implementation, AND IT REVERSES THE FIRST ONE. My pre-claim correction moved instrumentation.ts from apps/web/src/ to apps/web/ on the belief that Next reads it from the app root. THAT WAS WRONG AND THE IMPLEMENTER PROVED IT rather than asserting it: Next 16.3.1 discovers the file in exactly one directory, non-recursively, at rootDir = path.join(pagesDir || appDir, '..'), so for an app with src/app and no pages/ the directory is apps/web/src and a file at apps/web/instrumentation.ts is NEVER LOADED. It replayed Next's own findPagesDir and getFilesInDir over this tree and over two throwaway fixtures differing only in the file's location, and the fixture with the file at the app root produced no instrumentationHookFilePath at all. It then REFUSED TO WRITE THE FILE AT THE DECLARED PATH, on the ground that a file Next never loads, sitting where the wiring appears to be, is worse than no file. That is the right call and the surface is corrected to match the evidence, which is exactly the bar the human commander set this round for touching a removed path. REMOVED as proved unnecessary: apps/web/instrumentation.ts (the wrong path); apps/web/next.config.ts (Next 16.3.1 needs no opt-in -- next/dist/server/config.js deprecates experimental.instrumentationHook with 'instrumentation.js is available by default', so the file earned no edit); docs/DEPLOYMENT.md (the operator note is a fact about this source and belongs in docs/CATALOG_SOURCE.md, which already carries the licensing decision and the rights position, and the repo has no other deployment-runbook content for a new file to sit beside). ADDED: apps/web/package.json, because the app now imports @liberty/media-inspection for nodePinnedFetch and does not declare it -- the import resolves through the root workspace symlink so tsc and vitest are green and NO GATE CATCHES IT, which is precisely why the manifest must say what the app imports."
    },
    {
      "id": "PL-0310",
      "priority": "P2",
      "lane": "Frontend",
      "status": "REVIEW",
      "title": "A configured catalog with nothing usable does not look empty",
      "dependencies": [
        "PL-0305"
      ],
      "allowedPaths": [
        "apps/web/src/lib/catalog.ts",
        "apps/web/src/lib/catalog.test.ts",
        "apps/web/src/app/page.tsx",
        "apps/web/src/components/catalog/**"
      ],
      "reviewDependencies": [
        "apps/web/src/lib/catalog-source.ts",
        "apps/web/src/lib/catalog-source-registry.ts",
        "apps/web/src/lib/catalog-ingestion-source.ts"
      ],
      "preferredAgent": "claude-frontend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit"
      ],
      "acceptance": "The user-facing catalog path preserves the distinction the port now makes between no_records_usable and catalog_empty. apps/web/src/lib/catalog.ts maps an empty array to 'empty' and therefore COLLAPSES THE TWO, which is the one place PL-0305's four-state work stops short of the user. The two states have different causes and different remedies: catalog_empty means the source has nothing, while no_records_usable means records exist and every one of them was withheld -- almost always for want of an operator rights basis, which is an operator action rather than a user problem. Required: catalog.ts consumes describeCatalog through requireCatalogDescription rather than inferring state from an empty array; a source that does not implement describeCatalog still works and is not reported as a failure, because the method is optional on the port by design; the user-facing copy for the withheld case does NOT promise the content is coming, since nobody has committed to obtaining those rights; and the withheld reasons are not leaked verbatim to the user, because they name internal policy vocabulary. A regression must drive both states through the real loader and observe different answers.",
      "notes": "Created 2026-09-20 on gpt-architect's round-52 direction when it approved PL-0305: 'Make sure the existing handoff has explicit ownership for ... If any is currently only prose with no task, create a narrow task before it can be forgotten.' PL-0305 is NOT to be reopened for any of these. Follow-up 3 of 4. Point 6 of the round-52 verdict.",
      "owner": "claude-frontend",
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-09-20T15:23:15.089Z",
          "by": "claude-frontend",
          "commitSha": "f932ebe66c99cbb4fdf15f631d1464b16115aadc",
          "evidence": "npx turbo run typecheck --force, exit 0, 11 of 11; npx tsc --noEmit in apps/web exit 0. The type split is load-bearing: CatalogLoadOutcome makes cause REQUIRED so a loader branch that forgets it is a compile error and page.tsx's Record over CatalogEmptyCause is total, while the wider CatalogLoadResult keeps cause optional so handler.test.ts's hand-built literals -- outside this task's surface -- still compile. Both are built from the same shared members so they cannot drift."
        },
        "unit": {
          "status": "pass",
          "at": "2026-09-20T15:23:15.205Z",
          "by": "claude-frontend",
          "commitSha": "f932ebe66c99cbb4fdf15f631d1464b16115aadc",
          "evidence": "npx turbo run test --force, exit 0, 20 of 20, 0 cached, 2642 passed 1 skipped. catalog.test.ts 33 passed (24 before); the six related suites 130 passed. TWO REDS OF DIFFERENT KINDS, REPORTED AS SUCH. Red 1 is a genuine value red: 'AssertionError: expected undefined to be catalog_empty' on a cause asserted against the unchanged loader. Red 2, the both-states regression, COULD NOT BE A VALUE RED THROUGH THE EMPTY PATH and the implementer said so plainly -- before the change the CatalogSource seam accepted only CatalogHomeResponse | null, which by construction carries no cause, so there was no input that would make the pre-change loader's two empty answers differ; the collapse was total, which IS the defect. What it produced instead was 'expected { status: error } to not deeply equal { status: error } -- Compared values have no visual difference'. NO WITHHELD VOCABULARY REACHES A RENDERER: readHomeCatalogFrom discards answer.withheld entirely and a test asserts JSON.stringify(result) contains none of rights_basis_not_declared, availability_not_stated, Q42 or Q7. TWO GUARDS BEYOND THE ASK: a source answering records_available whose records all have rights null is reported no_records_usable rather than catalog_empty, because works exist so 'the source has nothing' would be false; and catalog_empty from an answer with complete false is downgraded to cause_not_stated, because a prefix cannot support 'there is nothing there'. The implementer also DELETED 'No titles are currently available in your region', which asserted a cause nothing on the home path establishes, and flagged that as a judgement call rather than slipping it in. A source implementing no describeCatalog still loads and is not an error -- asserted, not assumed. NOT COVERED AND SAID SO: no test renders page.tsx, because apps/web has no React testing library and vitest runs environment node with no .test.tsx anywhere; the copy guarantee is enforced one level in, at the loader, and the does-not-promise property is a judgement published for the reviewer."
        }
      },
      "implementationAgent": "claude-frontend",
      "updatedAt": "2026-09-20T15:23:15.317Z",
      "implementationBaseSha": "f932ebe66c99cbb4fdf15f631d1464b16115aadc",
      "reviewDependencyAdded": "apps/web/src/lib/catalog-ingestion-source.ts added to reviewDependencies 2026-09-20, on the implementer's own report. requireCatalogDescription -- which this task's acceptance names by name as the thing catalog.ts must consume -- lives in that file, and the declaration did not list it, so the approval would have fingerprinted a reviewed surface that omitted the accessor the whole task turns on. A read, not a write: reviewDependencies widens what an approval covers without reserving the path."
    },
    {
      "id": "PL-0710",
      "priority": "P0",
      "lane": "Security",
      "status": "REVIEW",
      "title": "Provider outbound HTTP resolves, classifies and pins its destination",
      "dependencies": [
        "PL-0702"
      ],
      "allowedPaths": [
        "packages/net-policy/**",
        "packages/provider-sdk/**",
        "packages/media-inspection/**",
        "tsconfig.base.json",
        "turbo.json",
        "package-lock.json",
        "docs/SECURITY.md",
        "docs/ARCHITECTURE.md"
      ],
      "reviewDependencies": [
        "packages/contracts/src/shared/ids.ts",
        "apps/web/src/app/api/v1/playback/session/authorized-candidates.ts"
      ],
      "preferredAgent": "claude-security",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit",
        "security-review",
        "rights-review"
      ],
      "acceptance": "THE CANONICAL HOST AND NETWORK POLICY MOVES TO A DEPENDENCY-LEAF PACKAGE, @liberty/net-policy, and both existing classifiers consume it. This replaces the original design, which was 'provider-sdk adopts media-inspection's resolve-and-pin control'; that is preserved in acceptanceOriginal and is NOT to be implemented. WHY THE DESIGN CHANGED, because the reason is the whole point: PL-0709 had to import provider-sdk's classifyHost from media-inspection BY DEEP RELATIVE PATH to write its agreement test, since classifyHost is not exported from packages/provider-sdk/src/index.ts and that package's exports field is a bare ./src/index.ts with no subpaths. The reviewer accepted that import FOR THAT TEST ONLY AT THAT TREE and ruled it is not an acceptable permanent boundary. Making media-inspection depend on provider-sdk, or provider-sdk on media-inspection while media-inspection imports provider-sdk to compare, CREATES OR INVITES A WORKSPACE DEPENDENCY CYCLE -- PL-0710 has provider-sdk adopting media-inspection's authoriseFetchTarget, so the arrow already points the other way. A lower shared layer with NO dependency on provider-sdk or media-inspection is the shape that works. REQUIRED, extraction half: the canonical host and IP classification vocabulary and its normalisation move into @liberty/net-policy; provider-sdk consumes that shared classifier; media-inspection consumes that shared classifier; PL-0709's deep cross-package test import is REMOVED; and the agreement test becomes unnecessary as two implementations become one -- replace it with tests of the shared classifier PLUS package-boundary tests proving both consumers actually import it, because an extraction nothing imports is a third copy rather than a merge. The package name may follow repository convention, but the package must be a dependency leaf. REQUIRED, resolve-and-pin half, unchanged from the original and each clause a separate way host-literal checking fails: DNS resolution before the outbound connection; EVERY resolved address classified, not just the first; refusal if ANY resolved address is private, loopback, link-local or reserved, unless an explicitly permitted local-only path applies; the connection PINNED to the approved resolved address so DNS rebinding cannot change the destination between check and connect; resolution, classification and pinning repeated PER REDIRECT HOP; and TLS CERTIFICATE HOSTNAME VERIFICATION AND SNI REMAIN AGAINST THE ORIGINAL HOSTNAME, NOT THE PINNED IP -- pinning the transport must not turn into verifying the wrong name. Regressions required for public-name-to-private-IP, DNS rebinding, mixed public and private answer sets, and translated-address cases. ALSO IN SCOPE BECAUSE THE EXTRACTION PUTS IT IN PLAY: pin.ts's normaliseHost does not fold the DNS root label. It is safe today because both sides of that comparison come from the same URL object so they carry or omit the dot together, and a mismatch refuses the connection -- but it is a third canonicaliser, and this is the task that removes the others. WHY THIS IS P0: F1, F7 and F8 were three host spellings in one function, and F12 was a fourth spelling in a SECOND function written specifically to avoid duplicating the first. The duplication got in anyway -- not as a copied classifier but as a copied assumption about what a hostname string is. A check comparing a string against literals is only ever as complete as the list of spellings whoever wrote it thought of; resolve-and-pin classifies the address a resolver returned, which has one spelling. FOLDED IN 2026-09-20 from the PL-0305 handoff's remaining off-surface item, because this task already owns both packages and the packaging metadata: @liberty/media-inspection's exports field has no ./http subpath, so importing @liberty/catalog-ingestion from apps/web pulls hls.ts into the program and needs an ambient m3u8-parser shim reached by a triple-slash reference in the ingestion package's index. That reference is the smallest honest fix from inside that package and is not the fix. THE SAME DEFECT SHAPE AS THE ONE THIS TASK EXISTS FOR: provider-sdk's classifyHost is unreachable from outside its package for exactly the same reason, a bare ./src/index.ts exports field with no subpaths, which is why PL-0709 had to import it by deep relative path. Give @liberty/media-inspection the subpath exports its consumers actually need and remove the triple-slash workaround, and make sure @liberty/net-policy is published with the subpaths ITS two consumers need rather than repeating the omission in the new package.",
      "notes": "Created 2026-09-18 on gpt-architect's round-46 instruction, reviewing PL-0702 at bbfaa5a997e6ee146271f26e5b2546b7e67e47d2. The reviewer required that no finding in docs/SECURITY_REVIEW_PROVIDER_URL.md remain at OPEN-OUT-OF-SURFACE, and that the deferral be carried by a real task with the proper owning surface rather than by a note. Register entry R1. gpt-architect required this to be a PREREQUISITE OF PL-0302 (first production provider), in its words: host-literal checking is not sufficient once real production provider/network access arrives. PL-0302's dependencies were amended accordingly in the same change. The reviewer was equally explicit that it does NOT block PL-0502 or local fixture playback, so nothing else was gated behind it.\n\nRound-50 amendment: the original design named in acceptanceOriginal must NOT be implemented. PL-0302's dependency on this task stands and is NOT to be broadened to local fixture playback or to PL-0502, per the reviewer's standing instruction.",
      "owner": "claude-security",
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-09-20T14:39:58.237Z",
          "by": "claude-security",
          "commitSha": "20edec3aa3f33dbd58aa405d1210f56ce54c207f",
          "evidence": "npx turbo run typecheck --force at the integrated head, exit 0, 11 of 11 tasks (10 before; @liberty/net-policy adds one). Also lint 0 (11/11) and build 0 (11/11), neither required by this task's gate list, and npm ci --dry-run exit 0 so manifests and lock agree after a workspace package was added and two consumers gained a dependency. THE DEPENDENCY SHAPE IS THE POINT AND IT IS MECHANICALLY TRUE, read out of the manifests rather than off a diagram: @liberty/net-policy declares dependencies {} -- an actual leaf, no @liberty, no third-party, no node:*; @liberty/media-inspection declares contracts, net-policy, xmldom and m3u8-parser and does NOT declare provider-sdk; @liberty/provider-sdk declares contracts, media-inspection and net-policy. The provider-sdk -> media-inspection edge exists and is acyclic because media-inspection does not depend back, which is exactly what part A's extraction made possible. Root package.json was never needed and was never touched, confirming the round-53 surface correction."
        },
        "unit": {
          "status": "pass",
          "at": "2026-09-20T14:39:58.354Z",
          "by": "claude-security",
          "commitSha": "20edec3aa3f33dbd58aa405d1210f56ce54c207f",
          "evidence": "npx turbo run test --force, exit 0, 20 of 20 tasks and 0 cached, 2617 passed 1 skipped, AT TURBO'S DEFAULT CONCURRENCY. Baseline 2441 over 18 tasks; +176 across both halves -- part A +124 (net-policy 100 new, media-inspection 226 to 240, provider-sdk 223 to 233) and part B +52 (provider-sdk 233 to 275, media-inspection 240 to 250). Two test tasks were added because turbo's test dependsOn ^build, so a new package contributes both. npm run test:scripts 0 (38+67+16+35) and repo:validate 0. PART B'S VALUE-RED IS THE WHOLE CASE FOR THE TASK: resolve-and-pin.test.ts against the unmodified provider-sdk gives 33 failed 2 passed, and the headline assertion is 'refuses a public name whose only answer is private' failing as 'expected false to be dns_resolved_private_address' -- the fetch SUCCEEDED against a name resolving only to 10.0.0.5. The hole was demonstrated, not described. The 2 that passed red are the static loopback refusals, pre-existing correct behaviour part B must not change. After implementation 35 passed. CLAUSE 7 WAS PROVED RATHER THAN ASSERTED, with a real handshake: a self-signed certificate whose ONLY SAN is DNS:localhost and which carries NO IP SAN, trusted for the file's lifetime, driven through fetchJson and nodePinnedFetch pinned to 127.0.0.1 -- 200, server-observed servername localhost, Host localhost:port -- and beside it the counterfactual RUNS FOR REAL, the rejected hostname:127.0.0.1 design against the same server and certificate asserting ERR_TLS_CERT_ALTNAME_INVALID. Without that second assertion the first would also pass against a transport verifying nothing. That block is describe.skipIf(!opensslAvailable) and it RAN here rather than skipping, and an unconditional source-invariant test in pinned-fetch.test.ts backs it up when openssl is absent. MUTATION: eight mutants, seven killed at both levels. M5 -- the rejected design, hostname set to the pinned address with servername dropped -- killed 7 tests including the real handshake and both ClientHello captures. TWO SURVIVORS REPORTED STRAIGHT rather than smoothed: M8 (pinned lookup answers any hostname) is killed by media-inspection's 4 tests but survives the entire 275-test provider-sdk suite, judged defended-rather-than-covered because the only route to a mismatch is handing hop N's pin to hop N+1, which is M3 and dies at the provider level; and part A's recorded survivor in createPinnedLookup's null guards is NOT re-closed by M8 and its comment saying so was left intact."
        }
      },
      "acceptanceOriginal": "Provider outbound HTTP adopts the existing @liberty/media-inspection resolve-and-pin control (authoriseFetchTarget / nodePinnedFetch) instead of classifying host literals. Required, and each clause is a separate way host-literal checking fails: DNS resolution happens BEFORE the outbound connection; EVERY resolved address is classified, not just the first; the request is refused if ANY resolved address is private, loopback, link-local or reserved, unless an explicitly permitted local-only path applies; the connection is PINNED to the approved resolved address, so DNS rebinding cannot change the destination between the check and the connect; redirects repeat resolution, classification and pinning PER HOP; and TLS hostname verification and SNI are not regressed -- pinning to an address must still present and verify the name. Tests are required for public-name-to-private-IP, DNS rebinding, mixed public/private answer sets, and translation-prefix cases. WHY THIS IS P0 AND WHY IT IS FILED NOW: F1, F7 and F8 are three defects of ONE SHAPE in one function -- a host spelling the string comparisons did not anticipate, each reading as public, none findable by reading. A check that compares a string against literals is only ever as complete as the list of spellings whoever wrote it thought of. Resolve-and-pin classifies the address a resolver returned, which has one spelling.",
      "architectureAmendedBeforeClaim": "AMENDED 2026-09-19 BY gpt-architect's explicit round-50 direction, BEFORE THE CLAIM, and the reviewer was equally explicit that it must be done before rather than after: 'Amend PL-0710's allowedPaths BEFORE CLAIM to include the new shared package and the minimum workspace/package metadata needed to create it. Do not widen it after implementation has already started.' The surface accordingly grew from ['packages/provider-sdk/**','docs/SECURITY.md'] to include packages/net-policy/** (the new leaf package), packages/media-inspection/** (the second consumer, and the home of the deep import being removed), and the minimum workspace metadata: package.json and package-lock.json because npm workspaces is 'apps/*, packages/*' and a new package needs the lockfile regenerated, tsconfig.base.json for path mapping, turbo.json for the pipeline, and docs/ARCHITECTURE.md because a new package in the dependency graph is an architecture fact. THIS IS A WIDE SURFACE AND IT IS RESERVED BEFORE THE WORK STARTS ON PURPOSE -- an extraction that discovers its consumers mid-flight is exactly the case the no-widening-after-start rule exists for.",
      "surfaceCorrectedBeforeClaim": "CORRECTED 2026-09-20, BEFORE THE CLAIM: root package.json REMOVED from allowedPaths. I declared it in the round-50 amendment as 'the minimum workspace metadata needed to create the package', and that was a guess rather than a finding. THE EVIDENCE SAYS IT IS NOT NEEDED: root package.json declares workspaces as the pure globs ['apps/*','packages/*'], so packages/net-policy is picked up with no edit; it has no script naming any package path and no @liberty dependency; and tsconfig.base.json carries NO compilerOptions.paths at all, so there is no path map to extend either -- packages resolve through the workspace links. package-lock.json, which genuinely must be regenerated, stays. BOTH FACTS ARE TRUE AND BOTH ARE STATED, because only one of them is a good reason: the entry was unnecessary, AND it was the single path blocking this P0, because PL-AI-0008 has declared root package.json since round 47 and has sat in REVIEW without a verdict since. Removing a needed path to dodge a reservation is the move this project does not make; removing an unneeded one I declared speculatively is correcting my own over-declaration, and the right time to do it is before the claim rather than after. FLAGGED TO THE REVIEWER RATHER THAN TREATED AS SETTLED -- if gpt-architect reads this as manufacturing readiness, the entry goes back and PL-0710 waits for PL-AI-0008. THE IMPLEMENTER IS INSTRUCTED THAT IF ROOT package.json TURNS OUT TO BE NEEDED, IT MUST STOP AND SAY SO rather than reach for it, because a widening after implementation starts is what the round-50 amendment forbade.",
      "implementationAgent": "claude-security",
      "updatedAt": "2026-09-20T14:39:58.468Z",
      "implementationBaseSha": "33195d593695618b779ab6c97a50817a2694e8df"
    },
    {
      "id": "PL-AI-0008",
      "priority": "P1",
      "lane": "Coordination",
      "status": "REVIEW",
      "title": "An agent instruction file has provenance, or it is not an instruction",
      "dependencies": [],
      "allowedPaths": [
        "scripts/validate-repo.mjs",
        "control/README.md",
        "coordination/AI_OPERATING_MODEL.md",
        ".gitignore",
        "scripts/test-validate-repo.mjs",
        "package.json",
        ".env.example"
      ],
      "preferredAgent": "claude-lead",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "unit",
        "security-review"
      ],
      "acceptance": "Repository validation detects any AGENTS.md or CLAUDE.md under a working directory that is not on an explicit, reviewed allowlist, and the authoritative instruction hierarchy is stated somewhere an agent reads before it starts. THE CONCRETE CASE: apps/web/AGENTS.md is GENERATED BY next dev and instructs agents to read node_modules/next/dist/docs/ before writing code. Nothing acted on it, and it is almost certainly benign. The finding is not that file -- it is that framework tooling can write an instruction file into a directory agents treat as authoritative, which makes a dependency's postinstall a path into the control plane's instruction surface. Required: the allowlist names each legitimate instruction file and who owns it, so an unowned one is a validation failure rather than a judgement call; generated instruction files are either gitignored and removed or allowlisted after being read, and the choice is recorded per file; validation fails on an unexpected file rather than warning, because a warning in a passing run is not a control; a regression plants an unexpected AGENTS.md and observes the failure, since a detector nobody has seen fire is not known to work; and the instruction hierarchy is written down -- which file wins, and that nothing under node_modules is ever authoritative. WHAT THIS TASK MUST NOT DO: silently delete a file a framework needs, or treat allowlisting as review. An allowlisted generated file must have been READ by a human or an agent that reports what it says.",
      "notes": "Created 2026-09-18 on gpt-architect's round-46 instruction, reviewing PL-0702 at bbfaa5a997e6ee146271f26e5b2546b7e67e47d2. The reviewer also required, separately from the register dispositions, that no finding in docs/SECURITY_REVIEW_PROVIDER_URL.md remain at OPEN-OUT-OF-SURFACE, and that the deferral be carried by a real task with the proper owning surface rather than by a note. This one came out of the PL-0702 subagent flagging the file rather than acting on it, which is the behaviour the instruction-source boundary asks for; the reviewer agreed it is a real control-plane supply-chain surface and directed that it NOT block PL-0502. Lane is Coordination and preferredAgent was corrected from claude-infra to claude-lead at the claim: claude-infra does not advertise the Coordination lane, and the control plane refused the claim rather than silently substituting an agent, which is the behaviour allowAutomaticReviewerSubstitution=false exists for.",
      "owner": "claude-lead",
      "gateResults": {
        "unit": {
          "status": "pass",
          "at": "2026-09-18T04:36:41.805Z",
          "by": "claude-lead",
          "commitSha": "2ef2a3ec6a241d1620fd603a505c2f1dd8953dff",
          "evidence": "Re-run by the lead rather than taken from the implementer, all with --force so a cached pass cannot stand in for this tree. node scripts/test-validate-repo.mjs exit 0, 16 assertion groups, the suite this task adds. node scripts/test-ai-control-plane.mjs exit 0, 67 scenarios. npm run test:scripts exit 0 end to end -- env 38, control plane 67, validate-repo 16, dispatcher 35 -- which is the first time that chain has been green in this session: it was exiting 1 at its FIRST link before this task, because LIBERTY_BUILD_TARGET was in turbo.json globalEnv and undeclared in .env.example since PL-0501's round-45 work, and PL-0501 was reviewed and approved with that test never run because test:scripts was not in the gate set. Verified pre-existing by stashing every change and running against a clean HEAD. npx turbo run typecheck --force exit 0, 10/10, 0 cached. npx turbo run test --force exit 0, 17/17, 0 cached, 2329 passed 1 skipped -- unchanged from the base, which is correct: this task adds no workspace tests. npx turbo run lint --force exit 0, 10/10. npm run repo:validate exit 0. RED-THEN-GREEN RE-RUN BY THE LEAD, not quoted from the implementer: planting packages/contracts/AGENTS.md makes node scripts/validate-repo.mjs exit 1 with 'agent instruction file with no recorded owner: packages/contracts/AGENTS.md'; removing it returns exit 0. A detector nobody has watched fire is not known to work."
        }
      },
      "implementationAgent": "claude-lead",
      "updatedAt": "2026-09-18T04:36:41.924Z",
      "implementationBaseSha": "2ef2a3ec6a241d1620fd603a505c2f1dd8953dff",
      "surfaceWidenedDuringImplementation": "WIDENED 2026-09-18 by claude-lead, mid-implementation, on the implementer's request rather than by the implementer's own hand: the subagent stopped and asked instead of relocating its test to fit the declared surface, which is the behaviour the path rules exist to produce. Three paths, each with its own reason. scripts/test-validate-repo.mjs -- the test's only natural home. The precedent is exact: scripts/test-validate-env.mjs tests scripts/validate-env.mjs, both plain node with node:assert, because turbo run test only visits workspaces and scripts/ is not one. Writing the test anywhere else would have been fitting the evidence to the declaration. package.json -- one edit, adding the new suite to the test:scripts chain, because a regression that no gate runs is not a control. .env.example -- AND THIS ONE IS NOT COSMETIC. Adding the suite to test:scripts revealed that the chain ALREADY EXITS 1 at its first link: scripts/test-validate-env.mjs fails because LIBERTY_BUILD_TARGET is in turbo.json globalEnv and undeclared in .env.example. Verified pre-existing by stashing every change and running it against a clean HEAD -- it fails there too. It was introduced by PL-0501's round-45 globalEnv addition, and PL-0501 was reviewed and approved with that test never run, because test:scripts is not in the gate set this lead had been running. Leaving it would have made the new detector unreachable behind an unrelated red test, which is the same defect one level up. .env.example is reserved by no active task; PL-0003, which declares it, is DONE."
    },
    {
      "id": "PL-AI-0010",
      "priority": "P1",
      "lane": "Coordination",
      "status": "REVIEW",
      "title": "A property suite gets a timeout of its own, not vitest's default",
      "dependencies": [],
      "allowedPaths": [
        "packages/contracts/vitest.config.ts"
      ],
      "reviewDependencies": [
        "packages/contracts/src/stream-candidate.property.test.ts",
        "packages/media-engine/vitest.config.ts",
        "turbo.json"
      ],
      "preferredAgent": "claude-lead",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "unit"
      ],
      "acceptance": "packages/contracts declares its own vitest testTimeout, with the reason for the value written down, so its fast-check property suite is not running against a default it can exhaust under load. THIS CLOSES AN OPEN ITEM RATHER THAN OPENING ONE. Round 52 recorded a single observed-but-unreproduced failure in stream-candidate.property.test.ts and the lead could not reproduce it across ten seeds; the PL-0710 part-A work reproduced it deterministically and it was never seed-dependent. It is CONCURRENCY-dependent: the box has 2 cores, turbo's default concurrency is 10, the new @liberty/net-policy package takes the graph from 18 test tasks to 20, and 'refuses a licence endpoint that is not https, whatever else it is' -- which runs in 1.73s standalone -- measured 5.4s and 7.4s under that graph against vitest's 5000ms default. Characterised rather than re-rolled: the same tree at --concurrency=4 passes, and the same tree filtered to 19 tasks at default concurrency passes. Required: the timeout is justified against the suite's measured standalone cost and the headroom a loaded machine needs, not picked round; the config explains that a property suite's cost is a function of run count and machine load rather than of the code under test, so a default sized for unit tests is the wrong instrument; and NOTHING ABOUT THE PROPERTY ITSELF IS WEAKENED -- not the run count, not the generators, not an assertion. Raising a timeout to hide a genuinely slow property would be the same mistake one level out, so if the suite is slow for a reason the code should answer for, say so instead. Out of scope: a turbo concurrency cap, which is a CLI flag rather than a turbo.json key and is a CI decision.",
      "notes": "Found 2026-09-20 during PL-0710 part A. The failing command is npx turbo run test --force at turbo's default concurrency, reproduced 2 of 2. packages/contracts has no vitest.config.ts at all today. packages/media-engine/vitest.config.ts is the house precedent for a package-local config that exists to keep a machine-dependent assertion out of the incidental run, and its header states the reasoning this task should follow.",
      "owner": "claude-lead",
      "gateResults": {
        "unit": {
          "status": "pass",
          "at": "2026-09-20T13:45:34.715Z",
          "by": "claude-lead",
          "commitSha": "20edec3aa3f33dbd58aa405d1210f56ce54c207f",
          "evidence": "npx turbo run test --force AT TURBO'S DEFAULT CONCURRENCY, exit 0, 20 of 20 tasks and 0 cached, 2565 passed 1 skipped. That is the command that matters, because it is the one that was failing. npx vitest run --root packages/contracts alone: 8 files, 202 passed. THE RED IS THE SUBAGENT'S, NOT MINE, AND THE RECORD SAYS SO. The agent that found this reproduced the failure two times out of two at default concurrency -- 'Test timed out in 5000ms' on 'refuses a licence endpoint that is not https, whatever else it is' -- and characterised it rather than re-rolling: 2 cores, turbo default concurrency 10, the new net-policy package taking the graph from 18 test tasks to 20, that test measured at 5.4s and 7.4s under the graph against a 1.76s standalone cost for the whole 31-test file. The same tree passed at --concurrency=4 and passed at default concurrency filtered to 19 tasks. I THEN RAN THE RED DIRECTION MYSELF AND IT DID NOT REPRODUCE: with this file removed, the same command on the same tree exited 0. So this is NOT a demonstrated red-to-green repair, and the config comment now says that in terms. It is headroom for a suite measured to be running at roughly a third of the budget it was held to, with no margin, which is a latent fragility whether or not it fires on a given afternoon. Round 52's single unexplained failure in this same suite is BEST EXPLAINED by this and is not thereby proven to be it; that item stays open rather than being closed on a hypothesis. Nothing about the property was weakened -- run count, generators and assertions are untouched, and the only file changed is a new packages/contracts/vitest.config.ts."
        }
      },
      "implementationAgent": "claude-lead",
      "updatedAt": "2026-09-20T13:45:34.835Z",
      "implementationBaseSha": "20edec3aa3f33dbd58aa405d1210f56ce54c207f"
    }
  ],
  "reservedExternal": [],
  "recommended": [
    {
      "id": "PL-AI-0006",
      "priority": "P0",
      "lane": "Architecture",
      "status": "READY",
      "title": "Contract module boundaries",
      "dependencies": [
        "PL-AI-0004"
      ],
      "allowedPaths": [
        "packages/contracts/**",
        "packages/media-engine/**",
        "packages/provider-sdk/**",
        "apps/web/**"
      ],
      "preferredAgent": "claude-lead",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "repo-validate",
        "unit"
      ],
      "acceptance": "Shared vocabularies live in their own leaf modules, domain contracts live in one module each, index.ts is a re-export-only compatibility barrel rather than the authoritative public surface, and a wildcard exports map lets a new domain contract be added without touching package metadata or the barrel; no cycle-driven z.lazy remains and structural regressions prove the boundary rather than merely reflecting it; no schema behaviour changes",
      "owner": null,
      "gateResults": {}
    },
    {
      "id": "PL-0402",
      "priority": "P0",
      "lane": "Backend",
      "status": "READY",
      "title": "Profile model",
      "dependencies": [
        "PL-0405"
      ],
      "allowedPaths": [
        "apps/web/src/**",
        "packages/contracts/**",
        "packages/**"
      ],
      "preferredAgent": "claude-backend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit",
        "integration",
        "security-review"
      ],
      "acceptance": "Profiles are modelled above authentication rather than inside the auth library, with the active profile carried alongside the session rather than inside the identity record; profile scoping is present in the first migration rather than retrofitted, because that is the part that is expensive to reverse; profiles can be created and selected with authorization tests",
      "owner": null,
      "gateResults": {}
    },
    {
      "id": "PL-0303",
      "priority": "P1",
      "lane": "Provider",
      "status": "READY",
      "title": "Provider health contract",
      "dependencies": [
        "PL-0301"
      ],
      "allowedPaths": [
        "packages/provider-sdk/**",
        "packages/contracts/**",
        "packages/observability/**"
      ],
      "preferredAgent": "claude-backend",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "typecheck",
        "unit"
      ],
      "acceptance": "A provider with zero observations reports unknown with a null observed success rate and a zero sample count, never a mid-range score and never a pass; any prior used to rank an unobserved provider is labelled as a prior rather than presented as measured availability; degraded-but-usable is a state distinct from up and down; health output is pure over the observations plus an explicit policy version, with no hidden wall-clock decay; and health never affects entitlement, so a provider at perfect health still cannot make an unauthorized candidate eligible",
      "owner": null,
      "gateResults": {
        "typecheck": {
          "status": "pass",
          "at": "2026-08-20T16:02:03.512Z",
          "evidence": "turbo run typecheck --filter @liberty/provider-sdk exit 0 under pinned Node 22 resolved via npx node@22."
        },
        "unit": {
          "status": "pass",
          "at": "2026-08-20T16:02:03.607Z",
          "evidence": "turbo run test --filter @liberty/provider-sdk exit 0 under pinned Node 22: zero observations yielding unknown with a null observed rate and a zero sample count, a prior labelled rather than presented as measurement at the same ranking number as a measured value, the degraded band held distinct from up and down, purity over observations plus an explicit policy version with Date.now replaced by a thrower to prove no wall clock is read, whole-report permutation invariance under generated policies, monotonicity in each count, and a compile-time guard that no health type can carry entitlement."
        }
      }
    },
    {
      "id": "PL-AI-0002",
      "priority": "P1",
      "lane": "Coordination",
      "status": "READY",
      "title": "GitHub bridge for cross-agent review",
      "dependencies": [
        "PL-AI-0001"
      ],
      "allowedPaths": [
        ".github/**",
        "control/**",
        "scripts/**",
        "docs/**"
      ],
      "preferredAgent": "claude-lead",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "architecture-review",
        "security-review"
      ],
      "acceptance": "GitHub becomes the shared state bridge for Claude Desktop and OpenAI review without manual file copying",
      "owner": null,
      "gateResults": {},
      "implementationAgent": "claude-lead",
      "updatedAt": "2026-08-17T01:53:44.285Z",
      "implementationBaseSha": "b157a5846d45568f516240f581d6b317a544aa12"
    },
    {
      "id": "PL-AI-0009",
      "priority": "P1",
      "lane": "Coordination",
      "status": "READY",
      "title": "release clears the implementation base, or a re-claim inherits a stale one",
      "dependencies": [],
      "allowedPaths": [
        "scripts/ai-control-plane.mjs",
        "scripts/test-ai-control-plane.mjs",
        "control/README.md"
      ],
      "preferredAgent": "claude-lead",
      "reviewAgent": "gpt-architect",
      "qualityGates": [
        "unit"
      ],
      "acceptance": "ai:release clears implementationBaseSha, so a task returned to an unowned queue and later re-claimed records the base of the round that actually implements it. OBSERVED, not hypothesised: PL-0710 was claimed and started at 33195d593695618b779ab6c97a50817a2694e8df, released with nothing written when its implementing agent was terminated, and re-claimed at HEAD 20edec3aa3f33dbd58aa405d1210f56ce54c207f -- and start reported 'started from 33195d593695', preserving the stale base rather than capturing HEAD. CLAUDE.md says implementationBaseSha is the exact lower bound of the first review range and that the range validator refuses a base that is either wider or narrower, so a preserved base makes the published window wider than the implementation. The rule release already follows is the argument: it discards gate results because 'evidence belongs to one implementation round under one owner', and a base is evidence of where that round began. Required: release clears the base, and ai:unblock is checked for the same defect and fixed if it has it; a scenario in scripts/test-ai-control-plane.mjs claims, releases, re-claims at a different HEAD and asserts the second base is the second HEAD, because this defect is invisible until exactly that sequence occurs; and the fix must NOT let start overwrite a base on an ordinary re-start of a task that was never released, since that is the falsification --reconcile-existing exists to prevent. Do NOT hand-edit any existing task's recorded base as part of this work; PL-0703 was blocked for that move and the historical records stay as they are.",
      "notes": "Found 2026-09-20 by the lead while re-claiming PL-0710. The harm in that instance was bounded and is recorded rather than repaired: 20edec3 touches only control/ and coordination/ and NOTHING under PL-0710's allowedPaths, so base..HEAD is one commit wider in count but identical in reviewed-surface content. The record is imprecise, not false about what changed.",
      "owner": null,
      "gateResults": {}
    }
  ]
}
